# 任务书 · 第二轮

**在你的会话里说一句「我是 X」认领角色，然后只做对应那一节。**
产品（另一个会话）会在这个文件里更新分配。

## 方向变了，先读这条

**店主定了新方向：面向全球 C 端用户。** 垂类可以中外混，但重心从「中国职业场景」
移到「全球普通人也会用的场景」。已因此下架三件（党政公文 / 德国鉴定式民法 /
法律中文润色）——**它们质量没问题，是定位不匹配**，留档随时可翻回。

连带后果：**英文版必须能独立成立**。已补齐 167 件的 `tagline_en` / `why_en` / `limit_en`，
`title_en` 正在补（138 件在跑）。

宪法三份：`docs/PRODUCT.md` · `docs/CURATION.md` · `docs/DECISIONS-2026-08-19.md`（31 条裁决）。
**三条红线**：不许编造（读不到就空着）· 数字要真实（跑不动就说跑不动）· 不要 commit / push。

当前：**在架 167** · 局限 167/167 · 备料齐 64 · 有存档正文 153 · `--strict` 0 error。

---

## 【内容】

**一、继续挑货，按全球 C 端方向。**
候选池 `editorial/candidates.json` 6083 个，机器只做了筛掉和排序——**挑不出，挑那关是人在把**。

已挑过（别重复）：中医四家 · 命理风水 · 起名 · 粤语填词 · 花粉 · 钓鱼 · 养老社保 ·
手账 · 字帖 · 跑团 · 剧本杀 · 考研英语 · 工资计算 · 报销 · 券商报税。

**没碰的大头**：内容创作 959 · 中国职场 889 · 心理情绪 215 · 学科竞赛 196 ·
基础教育 144 · 游戏二次元 102 · 地方生活 96 · 宠物母婴 86 · 饮食健身 23。
**优先全球 C 端能用的**：宠物母婴、心理情绪、饮食健身、游戏二次元、手工、旅行、家庭、学习方法。

**每一件都必须先读完 `SKILL.md` 正文再写文案**，读不到就换一件，不许凭仓库描述编。

**二、那 96 个 SOFT 标记的「绕反爬」候选**，逐个读正文和 `scripts/` 判。
判据：**随件附带专门绕过目标站防抓措施的脚本 = 出局，目的正当和如实披露都不豁免行为本身。**

**三、对照组结论。** 30 条英文垂直词的收录率跑完了没有？
`PRODUCT.md` 第二节现在顶着「这是假设不是结论」的标注上线，**它是全项目最重要的未知数**。

---

## 【开发】

**一、把「年龄回填」跑起来。** `.github/workflows/fetch-ages.yml` 已经推上线了
（token 现在有 `workflow` scope）。去 Actions 页手动触发，用的是内置 `github.token`，
1000 次/小时，需要约 285 次。`age_source` 三档规则见 D19。

**二、英文侧的守卫缺口。** 我们刚把英文从「回落」变成「一等公民」，但
`check_curation.py` 十项里**没有一项管英文**。至少该有：
`title_en` 缺失 · 英文文案里出现星数（刚清掉 5 处，会复发）· 英文回落成 `name` 标识符。

**三、`skill_count > 40` 的 7 件**（82/73/62/57/52/52/48）——那条 WARN 一直挂着。
收窄口径落在 ingest（`find_skill_mds` 排除 `tests/ golden/ fixtures/`），
但**判「哪几件是夹具」要看仓库内容，那是内容的地盘**，你只在构建期把可疑的摆出来。

---

## 【用户】（UX）

**一、英文版实机过一遍。** 刚补完英文，请在 `?lang=en` 下逐屏看：
标题会不会太长撑破卡片 · 中英混排的空隙 · 「点评 / 但是」两个标签在英文下的字距 ·
`zhchip`（中文标）现在应该一个都不出现了，确认。

**二、详情页的「点评 / 但是」刚改成两条平行**（同字号同墨色，两个平行标签），
桌面和手机各看一次，间距对不对。

**三、间距规矩已经写进 CSS 注释**：任何直接落在 `#main` 里的块，
上边距一律 `var(--s4)`，手机断点 `var(--s3)`，跟 `.todayline` / `.flow` 对齐。
加新块前先量兄弟。

**四、favicon 换成箸标识了**（`icon.svg` + `<link rel="icon">`，站此前从未声明过 favicon）。
顺手看看 apple-touch-icon 在 iOS 上加到桌面是什么样。
