# 任务书 · 2026-08-19

产品分派。**只做和你角色对应的那一节**，动手前先回产品一句「我是 X」，避免撞车。

## 共享背景

仓库 `/Users/jerryma/Desktop/skill/repo`（remote `github.com/woowoeth/skill`，线上 `https://ourword.ai/skill/`）。
**先读 `docs/PRODUCT.md`（产品定义）和 `docs/CURATION.md`（选品标准）——这两份是本项目的宪法。**

一句话：叫 SKILL.md 的文件是**百万量级、数不清**，**检索不是瓶颈，判断才是**。不做目录站，只做判断层。

> 这份任务书原来写的是「生态有 266 万条」。**这是我发给三位同事的口径，错的口径被我复制了三份。**
> 266 万是 SkillsMP 的自报库存，不是 skills.sh 的，更不是世界。详见 README 和 docs/PRODUCT.md。
> ⚠️ **下面这段「差异化是中文垂直」已经在 2026-08-20 被证伪，不要照它干活。**
> 对照组实测：目录站对英文垂类的收录率 0.48%、对中文垂类 0.49% —— **空白在长尾，不在语言。**
> 我们仍然大量收中文的东西，但那是**能力优势**不是**结构性壁垒**。
> **最新口径看 `docs/TASKS-round3.md` 的「共享背景」，以及 `docs/PRODUCT.md` 第二节。**

~~差异化是**中文垂直的结构性空白**——大市场全是英文爬虫，中文创作者的东西在那边搜不到。~~

四条不可动摇：
1. 卖判断，不卖索引
2. 差异化是中文垂直
3. 网站是给人看的橱窗，skill 是给 agent 的入口
4. 判断这关永远是人在把

~~**当前真实状态**：上架 153 件 · 仓库共 712 件 · 未上架 559 件 · 125/153 件有局限。~~
**这组数是 2026-08-19 的，早就过期了**（8-20 实测：在架 226 · 仓库 843 · 局限 226/226）。
**不要引用这里的数** —— 现取：`python3 scouts/check_curation.py`。

### 三条红线，违反即作废
- **不许编造。** 读不到就空着。空着是诚实，编造是欺骗。
- **数字要真实。** 跑不动就说跑不动，不要估算、不要凑数。
- 不要 `git commit`、不要 `push`。

---

## 【内容】

### 一、修 21 件 `path` 错误（最高优先级，用户可见的 bug）
153 件里 21 件（13.6%）取不回 `SKILL.md`：20 件的 `path` 字段是**空字符串**——
ingest 把子目录里的 skill 当成了根目录单品；1 件仓库已消失（`ourword-ai/pixelpad`）。

**后果：这些商品的 `install.copy` 命令是错的，用户照抄装不上**，同时方法正文缺失。

已自动探到真实位置的 5 件，可直接用：

| id | 现在 | 应为 |
|---|---|---|
| `ruochenlyu-beijing-pollen-monitor` | `''` | `skills/beijing-pollen-monitor` |
| `lijigang-ljg-skills` | `''` | `skills/ljg-writes` |
| `redfox-data-redfox-community` | `''` | `skills/multi-copywrite-alchemy` |
| `heygen-com-hyperframes` | `''` | `skills/hyperframes` |
| `zexuanw958-svg-travel-plan-viz` | `''` | `travel-plan-viz` |

其余 16 件 `kind=collection`，逐个探真实子目录。
改 `skills/*.json` 的数据 + `scouts/skill_scout.py` 的 ingest 逻辑，
**并把这 21 份 SKILL.md 正文补进 `methods/` 存档**（现 126/153，补完应到 147）。

### 二、把封面落地
上一轮已产出 `editorial/covers.json`：154 件全有候选，其中
**79 件 `is_real_artwork=true`**（README 头图 / assets 里的产出物示例），
56 件只是 GitHub OG 卡片，18 件沿用已有。

产出物图才是品味的证据，OG 卡只是比渐变色块强。设计一个落地方式
（**不要直接覆盖 `skills/*.json` 的 `cover`**，走 editorial 层或 feed 构建时合入），
并报告真产出物图的**实际可用率**——有多少张打开是 404、或者只是个无意义的 logo。
**请实际抽验，别只信 URL 存在。**

### 三、补完剩下 28 件局限
21 件是上面 path 错的那批，修好就能读正文；另 7 件内容合格但超了 120 字上限。
写法照抄 `editorial/curation.json` 里已有的 125 条——
**每条必须引正文里能指出出处的具体事实**（脚本文件名、依赖版本、明写的自我约束、语言、触发条件）。
禁止通用免责：「仅供参考」「请自行判断」一律判假。

**不要动**：`index.html`、`design/`、`seo/`、`schema/`、`scouts/scout_lib.py`

---

## 【开发】

### 一、实现「备料」徽章（方案已批准，直接做）
产品判断是**杀掉星数**——标准里写着「星数会亏待新发布、垂直、非英语的好项目」，
然后首屏印 `★ 0`，这是自我否定。153 件的星数是极端双峰
（在架 152 件时实测：0 星 12 件、1–99 星 13 件、100+ 星 127 件），根本不构成刻度。

替代信号是**「作者做到哪一步」**，从 `methods/` 已存档的正文本地算，零新增爬取。

> **订正（开发，2026-08-19 收工时）。** 本节原先写的那组数
> ~~references 83/126 · scripts 69/126 · 边界说明 57/126 · 适用场景 44/126 · assets 35/126 · evals 仅 6/126~~
> **作废，别再引用**：产出它的判定口径没有留下来，反推不了。
> 现行口径写在 `scouts/prep_signals.py` 模块头，**跑 `python3 scouts/prep_signals.py` 随时复算**——
> 这才是可对账的那一份。分母是「当时有正文存档的件数」，会随存档补齐而变，所以不要抄成定数。

- **主徽章三档**：≥3 条证据 =「备料齐」；1–2 条不显示；0 条不显示。
  **只有正向档，没有负向档**——一个只有一段提示词的极简 skill 可能恰恰是好东西，
  给它印「备料少」，就是把星数的错误换个马甲再犯一次。
- **副信号「店长读过」** = `limit_zh` 非空（现 125/153）。
- `stars` 在 feed 里保留供排序和门槛，但**不再有任何前端出口**。
- **evals 要回头改标准**：~~`docs/CURATION.md` 把「带 evals」写进准入~~
  **前提有误（产品已认，2026-08-19）**：`CURATION.md` 全文从来没有出现过 evals，
  「带 evals」只在 `README.md` 的「作者在乎吗」定义里 —— 那是定义，不是门槛，两者被说混了。
  所以没有「降级」这个动作。实际做的是：`CURATION.md` 第四节**新增**「加分项，不是门槛」，
  把六条备料信号一律钉死在加分项上，并写明「定量的门槛必须先量生态命中率再定」；
  `editorial/feedback.json` 的 `patterns` 记的是真实病因——抬头「用没量过命中率的指标当刻度」。
  实测 `evals/` 只有 2 件（约 1%）。

### 二、救回 7 条超长局限
内容合格但超 120 字被校验挡下。判断：放宽上限到 140，还是提供修剪工具。你定，说明理由。

### 三、`SEO_PATCH_INDEX` 恢复开关
等 UX 的新 `index.html` 落地后开回来。**现在别动 `index.html`。**

**不要动**：`index.html`、`design/`、`scouts/skill_scout.py`、`editorial/curation.json`

---

## 【用户】（UX）

`design/index.html` 是上一轮的半成品原型（深色编辑部杂志风）。**结构是对的**：
今日头条（493 字长判断）→ 本期精选 7 件（每件带「但是」）→ 名录。三件事必须处理：

### 一、修首屏阻塞
整页把第一次绘制阻塞在四个 fetch 上，滚到中段是整屏空白。
**硬 bug，无论最后什么配色都得修。**

### 二、拒收榜整块撤掉
店主决定「先不做，等真数据攒够」。现在页面上那 6 条是产品给的**假数据**，
而页面同时写着「已写理由 0 条，不拿数字充数」——自相矛盾。
**一个卖诚实的站展示假造的拒收记录，是自杀。** 顶部「拒了 6 条」一并去掉。

真实可用的只有三个数：**过手 712 / 上架 153 / 未上架 559**。版式上留个位置，将来再上。

### 三、设计前提变了，重看
上一轮的指令是按「多数卡片没有图」设计——**这个前提已经不成立**。
`editorial/covers.json` 里 154 件全有封面候选，**79 件是真产出物图**。
**图现在是常态，不是例外**，请重新考虑版式。

### 视觉调性先停手
店主对「深色编辑部 vs 浅色北欧黑白」都不满意。产品提了三个更实质的方向
（产出物即封面 / 局限即版式 / 中文排版做到英文站抄不了的程度），**还在等他拍板**。

在那之前只做与底色无关的部分：
- 排版骨架：字号阶梯、行高、中英混排间距、标点处理
- **「点评 / 但是」这一对的版式关系**——全站最重要的信息结构
- 有图 / 无图两种卡片的统一节奏

顺带修掉 `index.html:160` 和 `:180` 两条死链——指向 `github.com/woowoeth/skill-store`，
**那个仓库不存在**，正确的是 `github.com/woowoeth/skill`。

**不要动**：`scouts/`、`schema/`、`seo/`、`editorial/`。你的地盘是 `design/` 和最终的 `index.html`。
