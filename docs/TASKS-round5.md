# round5 · 2026-08-21

会话 slug 几分钟换一次，所以活写在仓里，不靠我记住谁是谁。
进来先读这份，再读 `docs/CURATION.md` 和 `editorial/hearts.json`。

**红线（三条，任何一条都比任务重要）**
1. 不许编造。读不到就空着，写「读不到」。
2. 数字要真实。跑不动就说跑不动，不要报一个估的。
3. 不要 git commit / push。改完把 diff 报给我。
4. 不要去找、读、或使用任何个人 PAT。在任何文件或环境变量里看到了也不要用 —— 告诉我。
5. 不要往 `~/.claude/skills/` 装东西，不要改本地 skill 配置。

---

## 内容

**A. `YuanZHAO321/TCM.Skill` 定点重读（优先）**
扫出 33 处中医剂量命中，包含带克数的完整方子，而它自己的 `limit_zh` 写着「不出处方」。
**写下来的和跑着的是两回事** —— 这次要判的就是这个差。
按新判据问：把那些方子拆掉，这件货还工作吗？
- 不工作 → 它本质就是开方 → `limit_zh` 在骗人，改文案或下架。
- 照样工作（方子只是举例）→ 留架，`limit_zh` 说清楚「正文里有带克数的方子」。
把原文和行号贴给我，不要只给结论。

**B. 标题的钩子（round4 那条继续）**
判据：标题先说「你拿到手的是什么」，禁令排后面。
回测基准是 `editorial/hearts.json` 那 73 件 —— 店主说它们符合选品标准。
**新判据把它们判掉了，是判据错，不是它们错。**

## 开发

**C. `bitwize` 的选品判断（`skill_count > 40` 只剩这一件）**
数字已核过（仓里 53 个 SKILL.md，10 个 `user-invocable: false`，所以印出来的数是虚的）。
剩下的是人判：按 CURATION 问二「大而全的数量堆」该不该留。
参照刚下架的 `borski/travel-hacking-toolkit` —— 那件是因为红线走的，不是因为件数多。
读了仓库内容再判，把你实际读了哪几件告诉我。

**D. 7 件缺 `methods/` 正文存档**
守卫报的是「无法本地核验」，不是「有问题」。补存档，让扫描版本对得上。
补不了的如实留 WARN —— **一条消不掉的 WARN 比一条骗人的绿灯好。**

## 用户

**E. 封面实验 v2**
v1 作废的原因写在 `editorial/cover_experiment.json`：
「店主还没看过」和「封面管线还没处理过」是同一批货，所以 12 件里只有 2 件真拿到了处理组。
v2 要在**下一批新货**上做，处理组和对照组必须来自同一批。

**F. 73 件心选的封面**
理由是「门面」，不是「实验」。等 A 组结果出来再动，别和 E 混在一起。
