---
name: zhongguose-palette
description: >
  中国传统色调色板。从 zhongguose.com 的 526 个具名色里选出能直接落地的配色，
  按「铺满的底 / 成块的那层 / 最小面积那一点」分配角色，输出 CSS 变量与 UI token。
  用户提到配色、色板、中国色、传统色、国风、故宫、青花、水墨、敦煌、宋瓷、江南、
  漆器、年画，或要给网站/App/海报/服装/空间选中国风颜色时使用。
  也在用户做前端、artifact、图表而需要一套中国风颜色时被组合调用——本技能是颜色的
  唯一真源，先出色再交给 frontend-design / artifact-design / dataviz 做版式。
  用户说「配个中国风的颜色」这种没有细节的话也要直接给方案，不要反问专有名称。
  禁止发明 hex，禁止输出泛称「中国红」。
---

# 中国传统色调色板

**把用户的原话交给 `ask`，不要自己先翻译成参数。** 用户说「茶叶小店的网站，安静一点」，
你就原样传进去。你自己译成 `--scene 宋瓷 --mood 雅` 是这条路唯一会静默失效的地方：
映射漂了，症状看起来像「颜色选得不好」，你会去调评分权重，而真正错的是意图识别。

```bash
python scripts/palette.py ask "茶叶小店的网站，安静一点"
```

脚本回读一行 `解读: 网页界面 · 素净耐看 · 浅色底`，然后给 2 套带色条的方案。照着念。

## 路由

| 用户在做什么 | 跑什么 |
| --- | --- |
| 说了一句话，想要配色 | `ask "<原话>"` |
| 反馈「太素了」「红少一点」「改深色」 | 先读 `references/tweaks.md`，再 `tweak "<原话>" --mood <当前档>` |
| 说「就这套」 | `pick "<底+块+点>" --media ui --pair-dark` |
| 给了两个色要补全 | `complete <色A> <色B>` |
| 问某个色名 | `info <色名>`（别名会解析：石青 → 群青） |
| 给了库外 hex | `snap "#xxxxxx"` |
| 提到梅兰竹菊、荷花、远山、水纹、回纹、卷草、藻井、团花这类纹样 | `ask` 会自己认出来并给点缀。细则读 `references/motifs.md` |
| 要看有哪些纹样 | `motifs`（可加 `--scene` / `--category`） |
| 要做图表 | 读 `references/dataviz.md`，用 `pick` 产出的 `chart` 段 |
| 要和别的设计技能配合 | 读 `references/handoff.md` |
| 自己已经说了行话（青花、雅、主色辅色） | 可以直接用 `generate --scene/--mood`，并跟着用他的词 |

## 说话的方式

**不要对用户说这些词**：主场、结构色、点缀色、五行、相生相克、荆浩、后素功、彩度、
明度带、间色正色、ΔE、LCH、token、氛围、场景、媒材。这些是引擎内部的过滤条件，
不是用户脑子里的概念。让用户在「雅」和「清冷」之间选就是让他猜。

**要大声说的**：色名和 hex（「鱼肚白 #f7f4ed」是用户最想要的东西）、铺在哪、
多大面积、字用什么色、一条具体禁令。

**回声允许**：用户先说了「青花瓷」，你就可以说青花；他先说「主色辅色」，就用他的词。
用户的词就是对的词——不纠正他的表达习惯，只不主动灌输。

**出处照带但翻成物象**：脚本的 `来处` / `当心` 两行已经是白话版，原样念。

### 哪些输出能照念，哪些要翻

只有 **`ask` 和 `tweak`** 的输出是干净的白话，可以照着念。selftest 用术语表守着它们。

其余子命令的输出是**给你看的参考件**，里面本来就有行话（五行、彩度、明度带、
ΔE、LCH、token、场景、媒材）。那是引擎对你说的话，不是对用户说的话——
照念 `info 石青` 会漏出「五行/彩度/LCH」。

| 命令 | 输出定位 |
| --- | --- |
| `ask` / `tweak` | 白话，照念 |
| `info` / `snap` / `complete` / `motifs` / `search` | 参考件，你读完翻成物象再说 |
| `pick` / `generate` | 参考件，转达时只说色名、hex、铺在哪、面积、字用什么 |
| `resolve` | 纯内部，永不面向用户 |

`info 石青` 该怎么翻，`references/examples.md` 例 4 有完整示范。

## 什么时候才问

默认零提问。问题**永不阻塞交付**——同一条消息里必须已经有一套能用的方案。

脚本会在末尾打 `歧义:响/静`，只有这一种情况值得追问一句，而且要先给方案再问：
先按素净那条给两套，然后问「那个红是想铺开来一眼看见，还是只留一点点？」
绝不问「你要市井还是空灵」。

**这些都不要问**：信号少（那是缺省该起作用的地方）、多个方向并列（各给一套让他看）、
别名解析（告知即可，不等回答）、媒材没说（缺省网页）、用户给的品牌色太响
（直接钉在标点位并说明理由）。

## 纹样是点缀，不是第四个色

用户要梅兰竹菊、荷花、远山这类中国风纹样时，纹样**借用已有 token 的颜色**，
不引入新色。一枝用点缀色画的梅，它**就是**那 3%，不是额外加的一块红。

- 墨量与铺色面积是两套账。只有借彩度色（点缀 / 成块那层）才折价计入那层的额度，
  借中性色（描边、面层、墨色）零计费。
- 压在正文下时看 alpha 上限，不是看借哪个 token（`references/motifs.md` 有实测表）。
- 密纹想铺大面必须配开光——留出素白开窗写字，把可读性交给版式而不是透明度。
- 成套的不可混：四君子是四条屏语汇，一屏一种；梅配竹而无松会被读成残缺的岁寒三友。
- 季节错最容易被识破：荷只属夏，岁末页面画荷即失手。
- 五爪龙、四爪蟒、十二章、补子禽兽等第有礼制含义，不作普通标记——
  拒绝时给替代路径（要龙的气质用夔纹或卷草），不要只说不行。

## 三条不能破的

1. **只出色库里的色。** 每个可见色都要能在 `references/colors.json` 里按中文名找到。
   要库外的色就 `snap`，不许插值，不许用 Tailwind 默认蓝灰粉充数。
2. **三色不等权。** 底最大、成块那层居中、那一点最小。脚本给的 `pixel_pct` 才是
   建议铺色面积——标点通常只有 3% 上下，一枚朱文印能压住整张宣纸。
3. **不许暗中换色。** 被否决的方向要说一句原因；为了满足某个要求调整了整套档位
   （例如为了给金把素净调成艳），必须明说。

完整的 16 条与常见失败在 `references/rules.md`。改评分或分类后跑 `python scripts/selftest.py`。

## 深色

用户说「暗色 / 深色模式 / 夜间」时走 `--dark`：深底做场，不是把浅色方案整页反相。
深底必须安静（L\*≤22 且低彩），纯黑和纯白都不在库里——最近的是燕颔蓝 `#131824`
和雪白 `#fffef9`。亮暗两套必须各自独立跑一次，`pick --pair-dark` 会一起产出。

## 别的文件

| 何时读 | 读哪份 |
| --- | --- |
| 微调用户的反馈 | `references/tweaks.md` |
| 和其他设计技能交接 | `references/handoff.md` |
| 纹样点缀（花卉/风景/几何） | `references/motifs.md` |
| 做图表 | `references/dataviz.md` |
| 完整铁律、常见失败 | `references/rules.md` |
| 用户问「为什么这样配」 | `references/theory.md` |
| 用户问色名典故、禁忌 | `references/culture.md` |
| 对齐输出话术 | `references/examples.md` |

色库本体 `references/colors.json`（526 条），原始抓取件 `references/source.json`。
这两个文件里的 hex 不要改。

需要 Python 3.10+，无第三方依赖。工作目录是本 skill 根目录。
Windows 中文乱码时先设 `PYTHONUTF8=1`。
