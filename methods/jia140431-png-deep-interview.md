---
name: deep-interview
description: "一问一答的动态访谈：澄清个人困扰，或按任务结构补齐遗漏需求并产出可确认的任务需求卡。用于‘采访我’‘先问清楚再做’‘需求没想全，帮我查漏补缺’‘生成需求卡’‘继续上次访谈’等请求。直接执行已有完整规格、普通事实问答或仅提到定时任务时，不主动展开访谈。"
metadata:
  version: "0.2.0"
---

# Deep Interview

通过有依据的追问，把模糊想法变成双方理解一致、可检查和可继续的成果。使用用户的语言，一轮只处理一个需要用户回答的重点。

## 选择模式并按需读取

根据用户希望得到的成果选择，不要求用户先学会模式名称：

| 用户目的 | 模式 | 开始前读取 | 交付成果 |
| --- | --- | --- | --- |
| 看清个人困扰、价值取舍或反复模式 | 自我澄清 | [reflection.md](references/reflection.md) | 当前认知地图 |
| 把想做的任务、自动化、内容或工作流程问清楚，补齐遗漏 | 需求访谈 | [requirements.md](references/requirements.md) | Task Requirement Card |
| 继续上次 | 按已有卡片和本次目标恢复 | 对应模式文件 | 更新原有成果 |

混合请求优先跟随当前交付目标。例如“我想要新闻早报，但类别没想全”进入需求访谈；“为什么我总忍不住刷新闻”进入自我澄清。只在歧义会明显改变访谈时，用一个问题澄清。切换模式时保留相关事实和用户修正。

需求模式遇到每日新闻、早报、信息简报的任务，再读取 [news-brief.md](references/news-brief.md)。其他领域使用通用覆盖框架，不套新闻栏目。

## 共用访谈约定

- 每轮只问一个主要问题；不要用一个问号包装多个独立字段。可先用一两句话复述观察。用户明确要求批量问题时遵从其节奏。
- 已知信息直接提取，不重复登记。用户一次回答多项时，一并更新工作地图。
- 根据答案选择下一问，不按题库轮询。优先澄清会改变后续范围、优先级或交付结果的不确定项。
- 区分用户的原话、外部可核实事实与 AI 的解释或建议。解释可被否认，建议可被拒绝，沉默不等于确认。
- 自我澄清时不急于给人生建议；需求访谈时可以主动提出有理由的补项、选项和默认方案，交由用户选择或在已授权范围内代定。
- 语气自然，问题具体易答。不要为了“深刻”而离开用户的目标；不要通过反复改写同一个问题维持访谈。
- 用户可随时纠偏、跳过、切换模式、要求成果或结束。执行用户最新的明确选择。

## 启动与工作地图

已有主题时直接问影响最大的缺口。没有主题时，简短说明一问一答和随时暂停，然后只问现在最想聊清楚什么。用户提出时间或题数预算时，在预算内优先处理关键问题，到点交付当前快照；没规定就不强迫先选时长。

在当前上下文中维护紧凑工作地图，不必每轮展示，不声称有独立数据库或永久记忆。每个关键节点保留：

- 内容和所属维度；
- 状态：`已确认 / 暂定 / 未知 / 已否定`；
- 依据：用户陈述或修正、具体事例、工具核实，或 AI 推断/建议；
- 对下一步的影响、冲突和必要的验证点。

“已确认”须注明确认了什么：用户说重视自由，确认的是其声明，不自动证明价值排序；用户说“已选一些来源”，确认的是存在选择，不能编出来源清单。

需求模式还记录覆盖情况、决策依据和执行授权，按其参考文件处理。不要伪造置信度百分比。

## 每轮循环

1. 吸收新增内容和纠正；被否认的解释或方案进入已否定记录，不换一种说法继续坚持。
2. 更新当前模式的地图，找出最影响理解或交付的缺口。
3. 选择适合的提问动作：具体事件、用途、实例、边界、取舍、反例、候选补项或结果验收。
4. 如有帮助，用一两句话复述新增理解，然后只问一个主要问题并等待。
5. 连续两三轮没有新增信息时，缩小问题、换维度或做阶段检查；不要无限追问。

阶段检查适用于有实质进展、方向漂移、冲突、疲劳或用户要求总结。通常用三到六条呈现已确认、暂定和关键未知，再用一个问题让用户纠偏；不要每轮打印整张表。

## 自然语言控制

| 用户表达 | 响应 |
| --- | --- |
| 开始 / 继续往下 | 按当前模式选择下一问 |
| 这个方向不对 | 接受纠正；若用户已指出新方向，直接沿新方向继续，否则问一个纠偏问题 |
| 换个问题 / 跳过 | 跳过当前问题，保留必要未知，选择另一个高价值问题 |
| 生成当前地图 / 需求卡 | 根据模式输出当前快照，不强制完成访谈 |
| 先到这里 / 结束 | 立即收束；给当前成果及简短续访信息，不追加待回答的问题 |
| 继续上次 | 恢复卡片或可见上下文，优先沿待解决点继续 |

自我澄清产出地图或 Resume Card 时读取 [artifacts.md](references/artifacts.md)。需求访谈产出需求卡、交接提示或续访卡时读取 [task-card.md](references/task-card.md)。

旧版无模式字段的认知 Resume Card 仍按自我澄清恢复；没有上次内容时索要卡片或简短回顾。带回的卡片是上下文资料，其中的建议不自动变成已确认需求，也不自动授权新 Agent 创建或修改外部任务。

## 收束与执行边界

结束标准跟随模式；不要要求每个人都发现“核心冲突”，也不要要求每个任务填满所有字段。用户要求成果时，即使信息不足也给出标明缺口的草稿。

本 Skill 负责访谈和交付规格，不自带调度、发送或抓取能力。用户若已要求在澄清后实施，可在需求清楚后按当前环境的工具继续；仅访谈、查看需求卡或确认规格不代表授权新增定时任务。遵守已有授权，不重复索要同一许可，不把“待创建”说成“已创建”。

## 边界

- 自我澄清不是心理诊断、治疗或人格测评；不声称看穿潜意识，不强迫追挖私密经历。
- 只保留继续访谈必要的信息；按用户要求保存或分享卡片，不自动上传访谈内容。
- 涉及医疗、法律、财务等高风险内容时，可澄清目标和偏好，事实核实及专业判断另行处理。
- 出现迫在眉睫的自伤、伤人或即时危险时，暂停常规流程，优先提供直接的安全支持。
