---
name: x-bookmarks-to-obsidian
description: 将 Chrome 插件导出的 X/Twitter 收藏或书签 CSV 去重、按用途分类，并生成带文章原链接、作者页、精准短标签、圆形头像、可置顶作者卡片，以及未读→学习→实践内容管道的 Obsidian 收藏库。用户要求整理、迁移、更新或复刻 X 收藏夹到 Obsidian 时使用；不用于按账号抓取热门推文。
---

# X 收藏夹 → Obsidian

把用户已经导出的 X 收藏 CSV 变成可持续更新的 Obsidian 收藏库。浏览器导出和本地整理是两个阶段；未经当轮明确授权，不读取用户的私人 X 收藏页面。

## 路由

- 用户已经提供 CSV：直接检查输入、预览，然后构建。
- 用户尚未导出：读取 [references/export-guide.md](references/export-guide.md)，指导其用当前 Chrome 的收藏导出插件生成 CSV。若用户明确要求由 Agent 操作浏览器，再使用可用的 Chrome 控制能力；只读取收藏，不点赞、转发、关注或发布。
- 用户要求按指定账号抓热门推文，而不是整理自己的收藏：改用 `$x-to-obsidian`，不要使用本 Skill。

## 构建流程

1. 确认 CSV 路径和目标 Obsidian vault。目标必须是现有 vault（包含 `.obsidian`）。名称默认使用 `x收藏夹`，库内位置默认使用 `X收藏夹`；用户另行指定时以用户为准。
   - 不要假定用户 vault 里有 `03Resources` 之类的个人目录结构。未指定位置时不传 `--output-folder`，由脚本自行决定。
   - 脚本会在新位置没有收藏库、而 `03Resources` 下存在同名托管收藏库时自动沿用旧位置，并在输出中给出提示。看到该提示时照常继续，不要重复生成一套新的。
   - 只有用户明确要求迁移位置，才显式传入 `--output-folder`。
2. 先执行预览，不写文件：

```bash
python3 "<SKILL_DIR>/scripts/build_x_bookmarks.py" \
  --input "/absolute/path/x-bookmarks.csv" \
  --vault "/absolute/path/to/vault" \
  --dry-run
```

3. 向用户报告原始行数、去重后数量、重复数、作者数、字段识别方式和预计输出位置。输入明显错位、原文 URL 缺失率超过 5%，或作者字段无法识别时，停止并说明问题。
4. 用户的请求已经明确要求创建或更新收藏库时，预览通过后直接构建，无须重复确认：

```bash
python3 "<SKILL_DIR>/scripts/build_x_bookmarks.py" \
  --input "/absolute/path/x-bookmarks.csv" \
  --vault "/absolute/path/to/vault" \
  --name "x收藏夹"
```

5. 若同名输出已存在，脚本会保护非本 Skill 生成的文件。只有已确认这些目标就是要更新的收藏库时才添加 `--update`。不要删除旧条目或用户新增笔记；更新只覆盖同 ID 的托管条目、索引和 Base，并保留作者的 `pinned` 值。
   - 更新条目时必须保留用户填写的 `status`、`priority`、`next_action`、`review_on`、`reviewed_at`、`practice_note` 和 `outcome`。
   - 新增条目默认进入 `status: 未读`，并把首次导入当天写入 `imported_at`；旧条目更新时不得刷新首次导入日期。
6. 头像默认从 CSV 中的公开头像 URL 下载到 vault。网络不可用时继续使用默认头像并报告失败数。用户不希望联网时添加 `--no-download-avatars`。
7. 用 Obsidian 打开 `<name>-作者索引.md` 验证实际渲染。交互式星标需要 Dataview 插件并开启 JavaScript 查询；没有 Dataview 时仍保留 Base 表格和作者笔记。

## 固定输出

在 `<vault>/<output-folder>/` 下生成：

- `<name>.md`：主入口与统计。
- `<name>.base`：文章数据库，标题可直接打开原 X 帖子。
- `<name>条目/`：每篇收藏一条 Markdown 笔记。
- `<name>-作者索引.md`：四列作者卡片；圆形头像、姓名、X ID、收录篇数、最多三个短标签和星标置顶。
- `<name>-作者索引.base`：作者数据的 Base 备用视图。
- `<name>作者/`：每位作者一页，列出用户收藏的全部文章。
- `<name>作者头像/`：本地头像与默认头像。
- `<name 去掉“夹”>工作台.md`：每天处理昨日新增、历史未读、到期提醒与正在实践的入口。
- `<name 去掉“夹”>工作台.base`：未读、重点学习、待实践、实践中、今日提醒和已完成视图。

## 内容管道

- 固定状态：`未读` → `待学习` / `待实践` → `实践中` → `已完成`；无须继续处理的内容设为 `已归档`。
- 固定优先级：`普通`、`重要`、`核心`。
- 进入 `待实践` 时优先填写一句可执行的 `next_action`；需要再次出现时填写 `review_on`。
- 学习或实践过程写在独立笔记中，并通过 `practice_note` 关联，不把大量个人笔记覆盖进托管条目正文。
- 每次增量导入只给新增条目设置默认管道值，必须保留所有旧条目的人工判断。

## 分类与标签原则

- 先按“用户拿它能做什么”分类，不使用宽泛的 topic 栏。
- 作者标签从其被收藏文章的标题与摘要综合提炼，每位最多 3 个，每个最多 6 个字。
- 标签优先包含：专业方向、内容形式、具体工具或能力，例如 `AI视觉`、`X增长`、`教程型`、`Codex`。
- 所有视觉、网页设计、UI/UX、图像生成相关内容统一使用 `AI视觉`；不要使用“搞设计的”。
- 证据不足时少贴标签，不根据作者名字臆测身份。

## 验收

- 原文 URL 去重，重复 status ID 只保留一条。
- 每个标题链接都指向原 X URL。
- 条目数、作者页汇总数与索引统计一致。
- 所有作者标签为 1–3 个且每个不超过 6 个字。
- 头像为圆形且完整居中；卡片在宽屏下一排 4 张。
- 星标仅改变 `pinned`，点击后作者移动到第一排；不得改变其他卡片内容。
- 重跑时保留已有置顶选择。
- 重跑时保留旧条目的管道状态、提醒日期、行动和结果；只有新条目进入“未读”。
- 最终报告写入路径、原始数、去重数、作者数、头像成功/回退数，以及 Dataview 是否可用。

## 边界

- 收藏夹属于私人数据。只有用户当轮明确要求时才能读取或导出。
- 不读取、打印或保存 Cookie、Local Storage、密码或令牌。
- 不绕过 X 的登录、验证码、访问控制或限流。
- 不因整理收藏自动下载帖子媒体；只保存 CSV 已包含的文字、元数据、头像 URL 和原文链接。
- 不静默覆盖非本 Skill 管理的同名文件，也不删除旧文件。
