---
name: short-drama-scriptwriter
description: AI 制作的竖屏剧 / AI 漫剧（非真人拍摄）的全流程编剧工作流，从立项选题、剧本圣经设计、分集场次执行稿到 Markdown 交付包，含状态门禁与制作增强整理。当用户提到抖音 / TikTok 短剧、AI 漫剧、立项选题、锁题、世界观 / 人物 / 场景道具、故事梗概 / 大纲 / 节拍、分集大纲、场次执行稿、剧本圣经、复盘、交付清单、production-enhanced，或要为这类项目创建 / 续写 / 改编 / 复盘 / 交付时使用。不适用于真人拍摄短剧、分镜、视频提示词、配音脚本或音效表生成。
---

# AI漫剧策划与剧本生成

本文是 skill 的入口路由。详细规则、产物等级、状态机、写作方法都在 `references/`，按 tier 标记按需加载。

## Quickstart

新项目第一句对话推荐这样说：

> "我想做一个 [国内 / TikTok 出海] [题材] AI 漫剧，目标 [集数] 集、单集 [秒数] 秒。"

例：`我想做一个国内 AI 漫剧，校园复仇题材，30 集左右，单集 60-90 秒。`

这一句必须同时给出 `发行平台` / `目标市场` / `题材方向` / `集数规格` 四字段，缺任一字段视为仍在对话态 `T01`，不会创建项目目录；详细判定见 [references/01-立项细则.md](references/01-立项细则.md) "锁题确认信号" 段。

之后会按 `/立项` → `/策划` → `/剧本` → `/复盘` → `/交付` 推进；每个阶段进下一步前需要独立质检通过。

中途接手已有项目：贴一份 `项目状态.json` 内容到对话里，会按 [references/17-状态流转与阶段门禁.md](references/17-状态流转与阶段门禁.md) "恢复现场"段恢复进度。

发起独立质检（用户原句必须包含 `clean-context` 或 `独立质检 / 隔离质检 / 做质检放行` 任一词，否则模型只做内建自检）：

> "对当前 EP001-EP005 批次做 clean-context 剧本质检"
> "对策划基底做 clean-context 独立质检"
> "用 subagent 做大纲质检"（仅在用户明确授权代理或并行复检时使用 `subagent`，否则默认 `clean-context`）

模糊说法（"再帮你检查一下 / 你看看有没有问题 / 再看看"）不算复检指令，只触发内建自检；质检放行只看独立复检结果。模型必须把用户原句逐字 quote 进 `质检检查点.md` 和 `项目状态.json.qcEvidence.<gate>.evidenceInstruction` 字段。运行环境怎么真正起隔离任务见 [references/18-过程质检与独立复检.md](references/18-过程质检与独立复检.md) "运行环境适配"段。

## 适用边界

只服务 `AI 制作的竖屏剧 / 漫剧`，不服务真人拍摄短剧。

固定边界：
- 策划层用中文；出海项目只有 `剧本对白 / 屏幕字 / 必要制作语种字段` 用目标语，未指定时美国英语首版默认英文
- 平台口径：国内写 `抖音`，出海写 `TikTok`；目标市场单独锁（`国内 / 美国英语首版 / 泛欧美 / 东南亚 / 拉美 / 指定地区`）
- 所有交付文件统一 Markdown；最终剧本正文统一 `场次执行稿`
- 本 skill 只到 `场次执行稿` 这一层；分镜、视频提示词、配音脚本、音效表交下游流水线，本 skill 仅在 `production-enhanced` 包里补齐资产命名、场景/道具卡等对接资料
- AI 制作能力**不作为题材降级理由**；奇幻、异世界、末日、规则世界、大场面都可以入选，选题只判断视觉锚点 / 角色识别 / 世界规则 / 情绪引擎是否清楚
- 用户没有明确确认创作方向前，`/立项` 只在对话中做调研、比较和候选汇总；不创建项目目录、不落任何立项文件、不进入 `T02`

## 核心目标

产出有吸引力、可交付、能让观众继续看的剧本，不是完成模板。

- 立项能说明观众为什么点开、为什么追下去
- 策划能说明人物欲望、关系张力、反派压迫和阶段回报如何持续升级
- 角色不止能说明功能位（欲望 / 反击方式），还能说明心理位（盲点 / 怕被看穿 / 标志性反应 / 内在弧光 / 关系质感）——心理位不到位，正文写出来必然换剧也成立
- 分集大纲能说明每集为什么必须存在；批次写作单进一步把每集拆成 `5-7` 个戏剧单位 × 在场 / 外在动作 / 内在心理 / 关系参数变化 / 可见后果，含金量在这一层而不是大纲一句话里
- 正文能在场面里呈现开场抓力、可见冲突、动作推进、情绪变化和尾钩拉力，并且每集落齐 `潜台词对手戏 / 心理瞬间外化 / 主角代价选择` 三件套
- 交付包能让制作方直接执行，不需要反查工作稿
- 全链路本地化深度成立——题材 / 矛盾 / 压迫机制 / 爽点 / 视觉锚点 / 对白都长在目标市场真实社会纹理里，不是直译版

商业化原则：
- 商业化默认**后置**；不用 IAA / IAP / 付费阶梯倒推选题或大纲
- `卡点` 在剧作层先理解为"让观众继续看的具体戏剧问题"，不是默认广告位或付费点
- 用户明确要求投流、IAA、IAP、平台付费或商业化大纲时，再读取 [references/24-卡点设计.md](references/24-卡点设计.md) 追加商业字段

本地化贯穿全流程，详细方法分布：跨市场 trope 重写矩阵在 [references/02-策划细则.md](references/02-策划细则.md)；选题词库在 [references/15-国内选题与设定词库.md](references/15-国内选题与设定词库.md) / [references/16-美国英语市场选题与设定词库.md](references/16-美国英语市场选题与设定词库.md)；对白本地化在 [references/12-单集写作协议.md 第七章](references/12-单集写作协议.md)；本地化深度门槛在 [references/13-质量检查表.md](references/13-质量检查表.md)。

## 双轨入口

- `流程视角`（本文）：按固定流程跑（T01-T18）；适合新项目从头跑
- `场景视角`（[references/22-写作方法索引.md](references/22-写作方法索引.md) 〇 章）：按"我现在遇到什么问题"找方法；适合中途接手 / 卡壳排查 / 局部优化

新项目默认走流程视角，22 不在启动加载；接手已有项目或某一步卡住时再加载 22 查方法。

## 加载策略

每个 reference 顶部标 `tier`，**该标记是加载分级的单一真相源**。按下面四类按需加载：

- `core`（启动加载）：本文 + [references/21-产物矩阵与创建时机.md](references/21-产物矩阵与创建时机.md) + [references/17-状态流转与阶段门禁.md](references/17-状态流转与阶段门禁.md)
- `on-demand-index`（卡壳 / 中途接手 / 局部优化时按需加载）：[references/22-写作方法索引.md](references/22-写作方法索引.md) —— 不在启动加载，避免上下文埋链接膨胀
- `phase:/<阶段>`（进入阶段时加载）：见下文 `阶段路由` 表
- `phase:跨阶段质检`（涉及质检放行时加载）：[references/18-过程质检与独立复检.md](references/18-过程质检与独立复检.md) + [references/19-质量门禁.md](references/19-质量门禁.md)
- `on-demand`（出现具体问题时加载）：剩下的所有 reference，每次按当前痛点选一个，不要一次性全读

**LLM 上下文不能被显式卸载**——进入下一阶段时，前阶段的 phase 文件仍在上下文里。长项目建议在阶段切换处用新会话，通过 `项目状态.json` 和 `质检检查点.md` 恢复现场，避免上下文越积越多。

大文件按"快速定位"导航：
- 策划问题先读 `02-策划细则.md` 顶部"快速定位"，按 T 节点展开
- 密度问题先读 `11-密度设计.md` 顶部"快速定位"，按时长 / 开篇 / 节奏定位
- 单集正文问题先读 `12-单集写作协议.md` 顶部"快速定位"，按写作环节定位
- 复盘质检问题先读 `13-质量检查表.md` 顶部"快速定位"，按阶段定位

## 主流程

四段：`立项与选题 → 剧本圣经设计 → 分集剧本创作 → 交付整理`
剧作主链：`创意 → 梗概 → 大纲/情节点 → 剧本初稿`

T01-T18 任务包、产物等级、创建时机的**单一真相源**是 [references/21-产物矩阵与创建时机.md](references/21-产物矩阵与创建时机.md)；本文不重复维护。
状态机、`currentStep`、门禁节点和回退规则的**单一真相源**是 [references/17-状态流转与阶段门禁.md](references/17-状态流转与阶段门禁.md)。

执行原则：
- 立项阶段先做市场判断 / 选题沟通 / 目标确定；用户未明确确认最终方向和必要立项信息前，只停留在对话讨论
- 选题策略不默认推高概念——先判断 `主流稳态 / 类型微创新 / AI-native 视觉型 / 高概念创新` 哪种适合用户，再给候选；高概念只是可选策略之一
- `T05-T07`（世界观 / 角色 / 场景道具）是同一轮故事基底包，按 [references/02-策划细则.md](references/02-策划细则.md) 的"同轮迭代七步法"执行（含主角心理位 + 关系质感）；不是三个孤立填表
- `T08-T09` 稳定后再校准 `T05-T07`，不提前写百科式世界观或资产大全
- `分集大纲.md` 是正文前**硬门槛**；未全量完成且未通过 `大纲质检`，不进入 `/剧本`
- `/剧本` 默认按批次写；每批先写 `1-2` 集代表样稿，通过后再铺满本批
- `/复盘` 是显式准入动作，不替代前面阶段的创作判断
- `/交付` 只整合正式源文件，不在交付阶段重写剧情

如果用户已有创意 / 梗概 / 大纲，从对应节点切入；不要为流程完整反向补一堆不影响梗概和初稿的设定文件。

## 阶段路由

| 阶段 | 目标 | 必读（phase） | 按需读（on-demand） | 主要正式产物 |
| --- | --- | --- | --- | --- |
| `/立项` | 市场判断 / 选题沟通 / 目标确定 | 01, 06 | 国内补 07，出海补 08 | 项目简报 / 锁题摘要（用户确认后落盘）；改编项目按需 故事理解 |
| `/策划` | 拆出剧本圣经的分项策划 | 02, 09 | T07 资产基底或视觉系统问题加载 23；商业化时 24 | 项目设定 / 世界观设定 / 人物小传 / 故事梗概 / 故事大纲 / 故事节拍表 / 阶段大纲（≥30 集）/ 分集大纲 |
| `/剧本` | 基于通过的大纲写分集正文 | 03, 12 | 格式问题 10；密度问题 11；AI 视觉/规则后果问题 23；商业化时 24 | `02-剧本/第XXX集.md` |
| `/复盘` | 显式复检（含交付前全剧通读） | 04, 13 | — | 复盘报告；问题多时 改稿指令单 |
| `/交付` | 整合通过的策划和正文 | 05, 14, 20 | — | 完整剧本总稿 / 剧本圣经 / 交付清单；`production-enhanced` 时补制作理解稿 / 参数表 / 海外分场表 / 信息流切片说明 |

每个创作节点后接一个独立质检节点；质检不通过不进入下一节点。完整状态机和回退规则见 [references/17-状态流转与阶段门禁.md](references/17-状态流转与阶段门禁.md)。

`T15 全剧体验通读复盘`（用户/编剧/导演三视角）是 `/复盘` 必出动作，发生在交付前；详见 [references/13-质量检查表.md](references/13-质量检查表.md)。

## 状态与门禁

- 用户确认进入 `T02` 时初始化 `项目状态.json` 和 `90-内部工作稿/质检检查点.md`
- 阶段门禁以这两份文件为单一真相源
- 当前上下文里的自检只算建议，**不算门禁放行**
- 独立复检的判定标准、可机器验证证据、运行环境（Claude Code / Codex / 普通会话）适配方法见 [references/18-过程质检与独立复检.md](references/18-过程质检与独立复检.md)
- 质检放行结论必须写入 `90-内部工作稿/质检检查点.md`
- 上游文件实质改动后，下游 `qcStatus` 必须回退；详见 17 的 `changeControl`

## 模板字段三档分级（MUST / SHOULD / MAY）

正式模板字段按 RFC 2119 风格分三档：

- `MUST`（必填）：缺字段视为流程不完整；独立质检判 `需修改`
- `SHOULD`（默认填）：跳过须在阶段回复或质检检查点写明跳过原因
- `MAY`（条件触发）：常规项目可不填；只在命中触发条件时才需要

反作弊红线：
- 不允许把跳过的字段填成 `待定 / 暂略 / 后补`
- SHOULD / MAY 字段在条件不满足时填了**也不加分**
- 字段填得满 ≠ 完成度高；评估完成度只看 MUST 字段是否实质填写

详细判定（含字段命中条件示例和质检写法）见 [references/19-质量门禁.md](references/19-质量门禁.md) 和 [references/18-过程质检与独立复检.md](references/18-过程质检与独立复检.md) 的字段分档审查段。

## 强规则

- 题材未锁定前，不进入 `/策划`
- `T06 人物小传` 的"心理位 / 内在弧光 / 关系质感"三组字段缺失或填成抽象口号时，不进入 `T08`；详见 [references/02-策划细则.md](references/02-策划细则.md) 人物小传职责段
- 没有全量 `分集大纲.md` 且未通过 `大纲质检`，不进入 `/剧本`
- 剧本正文必须基于已通过的大纲生成
- 每批正文必出 `90-内部工作稿/批次写作单-EPxxx-EPxxx.md` 的"单集戏剧单位深拆"——每集 `5-7` 个戏剧单位、五层（在场 / 外在动作 / 内在心理 / 关系参数变化 / 可见后果）填齐，跳过此层不进入正文
- 每集必须同时落地 `戏剧浓度三件套`（潜台词对手戏 ≥ 1 段 / 心理瞬间外化 ≥ 1 处 / 主角代价选择 = 1）；缺任一项独立 `剧本质检` 判 `需修改`；判定方法见 [references/12-单集写作协议.md 第三·补章](references/12-单集写作协议.md)
- 每批正文未过 `剧本质检`，不进入下一批
- `项目状态.json` 和 `质检检查点.md` 缺失时，不能把项目视为真正过门禁
- 同一上下文里的自检不能写成门禁放行；没有独立复检证据时 `qcStatus` 保持 `未检查` 或既有状态
- 海外项目策划层统一中文，英文只进入剧本对白和必要制作字段
- `完整剧本总稿.md` 只汇总正式分集 `场次正文`，不包含 `本集创作简卡 / 本集回填 / 复盘 / 检查单 / 批次写作单`
- `剧本圣经.md` 只在交付阶段生成，只整合正式策划内容；如果汇总了场景/道具章节，仍保留独立场景/道具卡作为制作资产卡 source-of-truth

## 模板与目录

- 默认目录：`00-立项 / 01-策划 / 02-剧本 / 03-交付 / 90-内部工作稿`，创建时机以 [references/00-项目目录与最终交付.md](references/00-项目目录与最终交付.md) + [references/21-产物矩阵与创建时机.md](references/21-产物矩阵与创建时机.md) 为准
- 主模板见 `assets/templates/`（27 份）；流程门禁模板：`23-项目状态模板.json` + `24-质检检查点模板.md`
- 模板原则：正式产物模板只保留结构骨架；工作稿只在当前阶段真实需要时启用；方法论只放 `references/`，不复制进项目产物；如果模板字段开始压过正文内容，先删字段、再补场面

## 脚本

脚本只做交付汇总、单集硬阈值机械检查和反作弊红线扫描，**不做戏剧判断**。质检放行必须由模型按 `references/18` 读实际稿件完成。

```bash
# 交付汇总（默认验门禁；--draft 跳过校验生成草稿；--check-mode 只比 mtime）
python3 scripts/build_full_script.py projects/<project-slug>

# 单集硬阈值检查（叠加屏幕字 / 画内可读性 / 规则词频次 / 前置比例 / 有效行数）
# --project-dir 自动从 项目状态.json 的 qualityThresholds 加载阈值覆盖
# --require-bypass-justification 低于行数门槛时强制校验"本集承载替代证据"段
python3 scripts/check_episode.py \
    --project-dir projects/<project-slug> \
    --regulation-words "命格,暗文,命债,见证人" \
    --require-bypass-justification \
    projects/<project-slug>/02-剧本/第001集.md

# 批次写作单 mtime 时序检查（防止"先写正文再回填批次写作单"）
python3 scripts/check_batch_plan.py projects/<project-slug>

# 反作弊红线机械检查（扫 00-立项 / 01-策划 / 03-交付 下所有 .md，
# 检测字段值是否填了 待定 / 暂略 / 后补 / TBD / N/A 等违禁词，
# 或留了 [MUST]/[SHOULD]/[MAY] 模板标记没去掉）
# --strict-must 额外校验 MUST 字段不为空 / 不缺失
python3 scripts/check_planning.py projects/<project-slug> --strict-must
```

脚本输出可作为独立复检的"硬阈值证据附件"，但**不替代独立复检的戏剧判断**。详见 [references/18-过程质检与独立复检.md](references/18-过程质检与独立复检.md) 的"可机器验证的 clean-context 证据"。
