---
name: repo-unbox
description: "开箱任意代码库，生成中文交互课程——讲给零编程基础、正在学 vibe coding 的中文学员听。Turn any codebase into a beautiful, interactive single-page HTML course in Chinese, for non-technical learners whose first barrier is English. 触发词：开箱这个项目、把这个项目做成课程、讲讲这个代码库、这份代码是怎么跑的、生成课程、代码库导览；also 'turn this into a course', 'explain this codebase interactively', 'teach this code', 'interactive tutorial from code', 'codebase walkthrough', 'make a course from this project'. 产物是一个自包含的 HTML 目录：滚动式模块、动画图示、内嵌测验、代码与中文逐行对照、以及要求学员动手写的输出题。"
---

# 代码库 → 中文课程

把任意代码库变成一门好看的交互式课程。产物是一个**目录**：预置的 `styles.css`、`main.js`、
自托管字体、每个模块一个 HTML 文件，以及拼装好的 `index.html`。双击就能在浏览器里打开，
不需要任何安装，**没有任何外部网络请求**。

## 第一次被触发时

如果用户还没指定代码库，先自我介绍：

> **我可以把任意代码库变成一门交互式课程，用中文讲，不需要你懂编程。**
>
> 告诉我看哪个项目就行：
> - **本地文件夹** —— 比如"把 ./my-project 做成课程"
> - **GitHub 链接** —— 比如"用 https://github.com/user/repo 做一门课"
> - **当前项目** —— 如果你已经在项目目录里，说"把这个做成课程"就行
>
> 我会把代码读一遍，理清它是怎么运转的，然后生成一门带动画图示、代码逐行中文对照、
> 测验和动手输出题的单页课程。全程在你的浏览器里跑，不联网。

如果给的是 GitHub 链接，先克隆到本地（`git clone <url> /tmp/<repo-name>`）再开始分析。
说"这个代码库"就用当前目录。

## 学员是谁

**完整画像见 `references/content-philosophy.md`，那是内容层的唯一权威，开始写之前必须读。**
一句话概括：中文母语、零编程基础、正在学 vibe coding、**英文是和技术并列的第一道障碍**。

他的目标不是成为工程师，而是：看懂代码在干什么、看懂英文报错、能用准确的术语指挥 AI。
所以这门课有两条不可妥协的原则：

1. **语言关**：代码里的英文单词本身要被解释，不只是概念
2. **输出验证理解**：每个模块结尾至少一道输出题，看得懂不算，说得出才算

## 为什么从成品往回讲

学 vibe coding 的人处境很特别：**东西已经跑起来了，是 AI 帮他写的，他自己看不懂。**
他缺的不是"先学会什么才能做出东西"——他已经做出来了。他缺的是"我做出来的这个到底是什么"。

所以这门课不从概念进，从他的手指进：那个按钮他点过，那条待办他见过。
课程要做的只是把幕布拉开，让他看清点下去之后发生了什么。悬念不在"会发生什么"，
在"凭什么"。

每一屏都要先站住"这跟我有什么关系"，再讲"它怎么运转"。
而"跟我有什么关系"的答案永远落在三件他明天就用得上的事上：
**把话说准了指挥 AI、出问题时知道该先看哪个文件、判断一件事该让 AI 做还是自己改。**
接不上这三件事之一的模块，砍掉或者重写。

产物是目录而不是单文件，是有意的：CSS/JS 从不重新生成，每个模块独立写（输出短、质量高），
拼装出的 `index.html` 离线可开、零配置。

---

## 流程

### Phase 1：读懂代码库

写任何 HTML 之前，先把代码库读透。读完关键文件，追清数据流，认出"角色表"（主要组件/模块），
理清它们怎么通信。这一步花的功夫，后面会成倍还回来。

**按课程需要什么来读，不要按"通读一遍"来读。要带走的六样：**

- **一个学员亲手做得出来的动作**，从界面一路追到落盘。第一个模块靠它开场
- **角色表**：哪几个文件各管一摊——谁碰硬盘、谁只管画、谁只管转发
- **它们之间的接口**：谁调谁、传什么、走哪条路
- **原作者留下的注释和 git 记录**：他当时在防什么、踩过什么。这是最好的教学材料，别跳过
- **真实跑得出来的报错**：课末那一节要用原文，编不出来
- **技术选型和它的理由**：学员将来要照着做同类判断

**自己搞清楚这个应用是干什么的**——读 README、入口文件、界面代码。
不要让用户解释产品，他自己可能也不熟。课程开头要用大白话讲清"这东西是干嘛的、为什么有意思"，
然后再往下钻。第一个模块必须从一个**具体动作**开始——
"你在输入框里打了'买牛奶'，点了添加——接下来发生了什么。"

### Phase 2：设计课纲

**4–6 个模块。** 只有当代码库真的有那么多值得讲的概念时才做到 7–8 个。
少而扎实永远好过多而稀薄。

叙事线永远从学员已知的（界面上的行为）走向未知的（底下的代码）。像镜头推近：
先拉开讲体验，再一层层剥开。

把模块当成"学员心里的那个问题"来排，不是当成知识点来排：

| # | 他心里的那个问题 | 这一模块之后他能做什么 |
|---|---|---|
| 1 | 我做出来的这东西，到底是什么？ | 从他亲手做过的一个动作追进代码，后面所有内容才有地方挂 |
| 2 | 里面都有谁？ | 认全角色，他才说得出"这段逻辑该放 X，别塞进 Y" |
| 3 | 它们之间怎么传话？ | 看懂数据走的那条路，他才查得动"为什么页面上没显示" |
| 4 | 哪些东西不在我这台机器上？ | 分清本地和外部，他才判断得了什么会花钱、会限流、会挂在别人手里 |
| 5 | 写代码的人当时在防什么？ | 认出缓存、重试、兜底这些手法，他才会主动向 AI 要 |
| 6 | 报错的时候我该看哪儿？ | 有了找问题的顺序，他才跳得出和 AI 反复"还是不行"的死循环 |
| 7 | 全都连起来是什么样？ | 有了整张图，他才决定得了下一步该加什么 |

**这是菜单，不是清单。** 按代码库挑：一个简单的命令行工具 4 个模块就够，不必凑 7 个。
一个模块如果回答不了上面任何一个问题，它就不该存在。

**每个模块必须包含：**
- 3–6 屏（模块内部的小节）
- 至少一处代码 ↔ 中文逐行对照（**每个模块都要有，校验按模块查，不是按全课查**）
- 正文中文**不少于 800 字**（校验按模块查）
- 至少一个交互元素
- 一两个"原来如此"提示框，讲通用的计算机原理
- 一个专属比喻（从 `content-philosophy.md` 的中国日常场景库里挑，**禁用餐厅、禁用美式场景**，
  且比喻不许在模块之间重复）
- **一道输出题**，类型必须在写 brief 时就定好（`retell` / `instruct` / `explain`）

**全课程必须出现的元素（一个都不能少）：**
- **输出题** —— 每个模块至少一道。`scripts/validate.mjs` 会卡住没有输出题的模块。
- **群聊动画** —— 全课至少一处。组件之间像微信群一样对话，这是最抓人的元素之一。
- **数据流动画** —— 全课至少一处。任何代码库都有数据在流动，找出来动起来。
- **代码 ↔ 中文对照** —— 每模块至少一处，没有例外。
- **测验** —— 每模块至少一道（选择题/场景题/拖拽/找 bug 都算），作为输出题之前的热身。
- **找 bug** —— 全课**至少 2 处**。让学员亲手点出错误行，是零基础最需要的主动环节；
  全课只有一处，读起来就只是装饰。
- **术语气泡** —— 每个技术术语、每个英文单词，在每个模块内首次出现时都要有。

**不要把课纲拿去审批，直接建。** 用户要的是课程，不是策划案。
自己把课纲想清楚，然后开始写。要改，他看到成品会说。

**课纲定了之后，选一条建课路径：**

- **简单代码库**（单一用途的命令行工具、小型 web 应用、库、只有一个入口、模块数 ≤5）
  → 直接进 Phase 3
- **复杂代码库**（全栈应用、多服务、内容量大、monorepo、模块数 ≥6）
  → 先做 Phase 2.5，再进 Phase 3

### Phase 2.5：模块 brief（仅复杂代码库）

代码库复杂时，先给每个模块写一份 brief，再动手写 HTML。这一步的价值是：
写模块时不必再回头翻代码库，上下文能一直保持干净。

模板见 `references/module-brief-template.md`，内容规则见 `references/content-philosophy.md`。

**每个模块写一份 brief 到 `course-name/briefs/0N-slug.md`，包含：**
- 教学线索（比喻、开场钩子、核心洞察）
- **预先摘好的代码片段**（从代码库里原样复制，带文件路径和行号）
- 交互元素清单：**按文件名列出**要用的元素（如 `elements/code-pair.md`、
  `elements/chat.md`），细到能照着做。写模块的人只会读你列出的这几个文件
- **输出题类型和它的 3–4 条对照清单**
- 前一个模块和后一个模块各讲了什么（好接上下文）

代码片段预先摘出来是省 token 的关键：写模块的时候完全不用再读代码库。

### Phase 3：建课

产物是一个**目录**。你只写两样东西：`_base.html` 和模块文件。其余全部由构建脚本负责，
不要自己复制、不要自己生成。

**目录结构：**
```
course-name/
  _base.html       ← 你写（外壳：标题、主色、导航点）
  modules/         ← 你写
    01-intro.html
    02-actors.html
    ...
  briefs/          ← 模块 brief（仅复杂代码库，建完可删）
  styles.css       ← 构建脚本复制
  main.js          ← 构建脚本复制
  _footer.html     ← 构建脚本复制
  fonts/           ← 构建脚本复制（自托管，无 CDN）
  index.html       ← 构建脚本拼装（不要手写）
```

**第 1 步：定制 `_base.html`** —— 把 `references/_base.html` 复制到
`course-name/_base.html`，只替换两个占位符，**别的一个字都不要改**：

- `{{KC_COURSE_TITLE}}`（出现两次：`<title>` 和导航条标题）→ 课程标题纯文本
- `{{KC_NAV_DOTS}}` → 每个模块一个按钮，顺序与模块一致：
  ```html
  <button class="kc-nav__dot" type="button" data-kc-target="kc-m1" data-kc-label="模块标题"></button>
  ```
  `role`、`aria-label` 由 `main.js` 在运行时补，不用写。

**没有配色可选。** 配色、字号、间距全部由 `styles.css` 决定，构建脚本原样复制进去，
你不挑也不改。

**第 2 步：写模块** —— 一次写一个，写完一个再写下一个。
一口气写完所有模块会导致后面的模块越来越薄——这是最常见的质量塌方方式。

先读 `references/content-philosophy.md` 和 `references/gotchas.md`。

**不要读 `output/` 下其他课程的产物当参考。** 那些是别的代码库的课，
照着它们对齐格式的结果是：比喻、开场、句式一起被搬过来，你这门课会变成上一门的翻版。
元素的写法看 `references/interactive-elements.md`，那才是规范；`output/` 里的是成品，不是模板。
每个模块写成 `course-name/modules/0N-slug.html`，只包含
`<section class="kc-module" id="kc-mN" data-kc-tone="a" data-kc-metaphor="...">` 这一块及其内容。
`data-kc-tone` 在 `a` 和 `b` 之间**逐模块交替**（第 1 个 `a`、第 2 个 `b`、第 3 个 `a`……），
相邻两个相同校验会卡——那是学员判断"我进入下一个模块了"的唯一视觉信号。
模块内部分屏用 `<section class="kc-screen">`，模块头三件：
`kc-module__number` / `kc-module__title` / `kc-module__subtitle`。
不要写 `<html>`、`<head>`、`<body>`、`<style>`、`<script>` 标签。

交互元素的写法：**先看索引** `references/interactive-elements.md`（61 行），
从里面挑出这个模块要用的几种，**只读那几个** `references/elements/<名字>.md`。
一门课通常只用 6–8 种，**不要把 `elements/` 下 18 个文件全读一遍**——那是这份 skill 最大的一笔冤枉钱。
排版规则见 `spec/typography-zh.md`；具体数值全在 `references/styles.css` 顶部的
token 块里，要查就直接 grep 它。

复杂代码库有 brief 时，按 brief 逐个写：每个模块只需要它自己的 brief、
`content-philosophy.md`、`gotchas.md`，以及 brief 里点名的那几个
`references/elements/*.md`。**不需要**再读代码库（片段已在 brief 里）。

写完全部模块后，回头做一次一致性检查：导航点和模块对得上、模块之间的过渡连得上、
语气没有跳变、比喻没有重复。

**第 3 步：构建** —— 执行：
```
node <SKILL_DIR>/scripts/build.mjs course-name --source <代码库路径>
```
`<SKILL_DIR>` 是这份 SKILL.md 所在的目录。全局安装的话通常是
`~/.claude/skills/repo-unbox`；在本仓库里开发就是仓库根目录，可以直接写
`node scripts/build.mjs`。本文档里所有 `references/...` 路径也都相对于 `<SKILL_DIR>`。
`--source` 是必填的：每个代码块都要拿去和这个代码库逐字比对。缺了它构建直接失败，
没有绕过去的办法——这是“代码一字不改”唯一的机械保障。
脚本会把 `styles.css`、`main.js`、`_footer.html`、`fonts/` 复制进课程目录，
按文件名顺序拼出 `index.html`，补上漏掉的 `lang="en"`，然后跑校验。
**校验不过就退出非零**——那意味着课程是坏的，不是脚本挑剔。改模块 HTML，重跑。

有三个数值得看：
- **`output tasks: N across M modules`** —— 每个模块都得有，少了会直接校验失败
- **`lang="en" backstop: N added`** —— 大于 0 说明模块 HTML 没按
  `interactive-elements.md` 的语言规则写
- **`code blocks verbatim and correctly cited`** —— 失败会指出是哪个 `文件:起止行` 对不上
- **`numeral claims to eyeball`** —— 脚本判不了对错，只把所有“几个/几行/几条”的句子列出来，
  **你必须自己回去数一遍**（中英文的计数单位不一样，翻译过来的数量十有八九是错的）
- **校验清单** —— 每条失败都会打出具体是哪里错了

### Phase 4：检查和打开

构建通过后，在浏览器里打开 `index.html`，带用户过一遍，问他对内容、设计和交互的意见。

**关键规则：**
- **不要往课程目录里写** `styles.css`、`main.js`、`_footer.html`、`fonts/`——那是构建脚本的地盘
- 模块文件只包含 `<section>` 的内容，不含任何外壳
- 滚动吸附、模块高度、字重、行高这些**一概不用你管**，`styles.css` 已经定死了
- 交互元素的 JS 全在 `main.js` 里，靠 class 名和 `data-kc-*` 属性自动接上，
  写法见 `references/elements/<名字>.md`
- 群聊、输出题、选择题、拖拽匹配这四种容器需要 `id`，且**整页唯一**；
  数据流演示需要 `.kc-flow` 上的 `data-kc-steps='[...]'` JSON
- 输出题需要 `id`、`data-kc-kind`、`data-kc-min`，以及 3–4 条对照清单
- 找 bug 的反馈区、架构图的说明区**一律不带 `id`**——一门课出现两处就必然撞名
- **代码块是硬规则**：每个代码块必须是源文件里**连续的若干行**、**逐字复制**，
  并在 `kc-code-pair__source` 里标注 `文件名:起止行`（如 `src/store.js:45-55`）。
  允许整体去掉公共缩进，不许改相对缩进。原作者的注释是代码的一部分，不要删。
  要展示不相邻的代码，就开两个代码块，各自连续，**永远不拼接**。
  写完自己跑构建，校验通不过不算写完。
- 标点用大陆规范：引号 `“”` / `‘’`，不用 `「」`
- 每个 `<section class="kc-module">` 必须有 `data-kc-metaphor="<这个模块的比喻>"`，
  一门课里的比喻**不许重复**，校验会卡
- 输出题的 `data-kc-min` 不得低于 60；对照清单里**至少一条**要点名一个英文的东西：
  `retell` / `instruct` 类型点文件名或函数名；`explain` 类型点**这个概念的英文原词**
  （那道题本来就要求学员别用术语，逼他写文件名是和题意打架）
- `data-kc-steps` 的说明文字里**不能出现英文单引号**，会把属性提前截断，动画会静默失效

---

## 视觉身份

整体是**一本暖色的手账**：米白纸底、朱红做主色、留白多、代码块深底。

**但这一节你什么都不用做。** 配色、字号、行高、间距、动效全部写死在
`references/styles.css` 里，由构建脚本原样复制进课程目录。
你不挑主色，不写 `<style>`，不覆盖任何值。

需要知道的只有这几条，因为它们会影响你**写内容**的方式：

- **每屏至少一半的面积不是段落。** 连续三段以上纯文字没有任何视觉元素，视为不合格
- **相邻模块底色交替**，靠 `data-kc-tone` 的 `a`/`b` 控制，你必须逐模块交替着写
- **字体只有 400 和 700 两档。** 霞鹜文楷没有别的字重，也没有真斜体，
  所以正文里**不要用倾斜表示强调**，用粗体或换个说法
- **正文每行 30–40 个汉字**已经由版心宽度保证，你不用数，但别把长句堆成一大坨
- **代码永远不横向滚动**，长行自动折行

要查具体数值，grep `references/styles.css` 顶部的 `@layer tokens` 块；
排版规则的完整表述在 `spec/typography-zh.md`。

---

## 参考文件

`references/` 里是详细规范。**用到哪一步再读哪一份**，不要一开始全读，那会把上下文塞满。

- **`references/content-philosophy.md`** —— 学员画像、语言关三层格式、输出题、比喻库、
  视觉密度、术语气泡、测验设计、课程末尾固定块。Phase 2.5 和 Phase 3 必读。
- **`references/interactive-elements.md`** —— 交互元素**索引**，列出 18 种元素
  各自的文件名和适用场景。Phase 2 挑元素时读它。
- **`references/elements/*.md`** —— 每种元素一个文件，含完整 HTML 写法和规则。
  **只读你这个模块用到的那几个。**
- **`references/metaphor-fallback.md`** —— 中国日常场景的比喻兜底表。
  **只有当项目题材接不上某个概念时才读它**——读了就会用，一整门课的比喻会全变成通用款。
- **`references/gotchas.md`** —— 常见翻车点清单。Phase 3 和 Phase 4 读。
- **`references/module-brief-template.md`** —— Phase 2.5 的 brief 模板，只有复杂代码库用得上。
