---
name: bid-workflow-skill
description: 投标文件全流程工作流：从招标文件提取投标目录、生成文件夹与DOCX、生成资料清单、按清单整理项目资料包、编制非技术标，到分阶段优化技术标。用于处理招投标相关PDF、Word、TXT、文件夹和企业资料，并执行预览、审核、缺失标记和原件保护。
---

# 投标文件工作流

## 使用原则

- 先识别用户要执行的功能，只读取该功能对应的参考文件；用户同时提出多个功能时再按依赖关系分阶段执行。
- 当前有效的招标文件、正式答疑/澄清/补遗、企业原始资料和用户明确确认的信息是事实来源。参考样例只用于学习结构或版式，不把项目名称、人员、证件、日期、参数或资料要求带入当前项目。
- 分开报告“已确认”“待确认”“缺少数据”“冲突”和“无法读取”。来源冲突时分别列出，不擅自选择。
- 保留原始文件。修改、复制和生成成果时使用独立输出目录，不覆盖、删除、移动或重命名输入资料。
- 任何功能缺少关键文件、最终版本或必要审批时暂停，说明缺少什么以及下一步需要用户做什么。
- 不编造证书、业绩、人员、项目事实、报价、日期、参数、签名、印章、二维码或页数内容。
- 编制或修改投标文件时，待签章、待填写、待选择或事实不确定的位置，应将该位置所在的原有行字体标红；不得额外添加“待确认”“待签章”等提示文字、说明段落或注释。
- 真实招标文件、企业资料、个人信息和项目成果不得写入本 Skill 包。

## 功能路由

| 用户意图 | 功能 | 必读参考 |
| --- | --- | --- |
| 提取目录、确认投标文件组成 | 功能 1 | [references/01-directory-extraction.md](references/01-directory-extraction.md) |
| 按目录 TXT 创建文件夹和 Word 文件 | 功能 2 | [references/02-directory-generation.md](references/02-directory-generation.md) |
| 列出编制投标文件需要准备的资料 | 功能 3 | [references/03-materials-checklist.md](references/03-materials-checklist.md) |
| 按资料清单从企业资料中整理项目资料包 | 功能 6 | [references/06-materials-packaging.md](references/06-materials-packaging.md) |
| 填写商务标、资信标或其他非技术标内容 | 功能 4 | [references/04-nontechnical-drafting.md](references/04-nontechnical-drafting.md) |
| 格式化、逐篇优化或调整技术标总页数 | 功能 5 | [references/05-technical-optimization.md](references/05-technical-optimization.md) |

用户明确指定功能编号时按编号执行；没有指定时根据意图路由。不要因为上一个功能完成而自动执行下一个功能。

## 功能关系

```text
招标文件
  └─ 功能1 → Word目录审核稿 + UTF-8目录TXT
                    └─ 功能2 → 文件夹 + 标题DOCX

招标文件
  └─ 功能3 → 投标文件资料清单
                    └─ 功能6 → 经审核的项目资料包
                                      └─ 功能4 → 非技术标独立成品

技术标Word + 页数要求
  └─ 功能5 → 格式化稿 → 逐篇审核稿 → 页数调整最终稿
```

这些功能可以单独调用。上游成果不是当前任务必需时，不强制补跑上游功能。

## 标准接口

### 功能 1 → 功能 2

功能 1 同时输出内容一致的 Word 审核稿和 UTF-8 TXT。TXT 树形区域只使用：

```text
[文件夹] 文件夹名称
  [文件夹] 子文件夹名称
    [Word文件] 文件名称.docx

依据和备注
- 文件名称：招标文件页内第X页；条件为“若有”
```

只有 `[文件夹]` 和 `[Word文件]` 行是结构节点。“依据和备注”之后的页码、来源、条件和说明不创建为文件。

### 功能 3 → 功能 6 → 功能 4

- 功能 3 输出当前项目需要准备什么，并区分企业现有资料、需要企业确认的信息和可按模板编制的文件。
- 功能 6 建立“清单项—候选原始文件—匹配证据—风险/缺口—状态”的对应关系，经用户审核后复制成项目资料包。
- 功能 4 只使用已经确认的招标依据、模板和企业资料事实填充非技术标副本。

## 人工审核门

| 功能 | 审核前允许 | 明确通过后允许 |
| --- | --- | --- |
| 功能 2 | 解析 TXT、显示完整预览和异常 | 创建文件夹和 DOCX，再扫描核对 |
| 功能 6 | 扫描企业资料、匹配候选、显示预计复制清单 | 复制确认文件并生成资料包索引 |
| 功能 4 | 扫描/OCR、列出事实、缺少数据、待确认和冲突 | 按独立文件编制；每完成一份再交用户审核 |
| 功能 5 | 完成当前阶段或当前文章并展示检查结果 | 当前文章通过后进入下一篇；全部通过后进入页数调整 |

“继续”“可以看看”或修改意见不等于“审核通过”。审批对象发生变化时重新预览。

## 可选脚本

功能 2 在有 Python 环境时优先使用以下通用脚本：

- `scripts/parse_bid_tree.py`：解析 TXT 并输出预览；
- `scripts/create_bid_structure.py`：没有 `--approved` 时只预览，有该参数才创建；
- `scripts/check_bid_structure.py`：创建后核对目录、DOCX 和标题。

没有脚本能力时，仍按参考文件执行可完成的阅读、分析和预览；不能自动创建 DOCX、解析 PDF、OCR、渲染或统计页数时，明确报告所需工具。涉及 Word 成品时必须渲染逐页检查，不能只以“文件能打开”作为合格标准。

## 每次结束时报告

报告本次功能、输入文件和版本、事实依据、已完成项、审核状态、待确认项、缺少数据、冲突或无法读取项、输出位置、验证结果、原文件未修改情况和下一步动作。
