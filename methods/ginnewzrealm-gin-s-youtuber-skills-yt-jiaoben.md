---
name: yt-jiaoben
description: Use when 选题已通过 yt-fenxi 立项（confirmed），需要把资料包转化为可拍摄的 20-30 分钟 YouTube 长视频脚本；按"好脚本客观定义+四模块三轨工程模板"施工，含原爆点筛选、B-Roll 清单、法律口径预审、机械指标扫描。不适用：选题分析（归 yt-fenxi）、资料补采（归 yt-ziliao）、视频剪辑执行（归人工/剪辑工具）。
---

# yt-jiaoben 脚本工程

## 概述

对**已立项选题**做脚本生产：输入契约校验 → 原爆点筛选（人拍板）→ 产前准备（B-Roll 清单+法律口径）→ 四模块三轨装配 → 硬指标扫描 → 人审 → 交付（飞书 doc + 本地存档 + 有限回填）。

核心原则：

1. **定义即法律**——好脚本标准见 `references/good-script-definition.md`（用户提供权威版）。模板和本文件与之冲突时，以定义为准。
2. **真实可考**——每个画面配公开凭证，禁伪现场感（HR-1/HR-5）；素材不够回 yt-ziliao 补搜，**脚本阶段禁止自己上网搜**。
3. **工程化交付**——三轨制（听觉/视觉/控制），画面描述 100% 客观动作，剪辑师零沟通成本可执行。
4. **机器数指标，人拍创意**——A1-A6 硬指标机器强制（scan_script.py），原爆点 Top3/钩子方案/CTA 由人拍板。
5. **路径不硬编码**——token/ID/目录一律读 `config.yaml`（初始化时复制 config.template.yaml 填写），禁止缩写。

## 何时使用 / 不适用

| 场景 | 归属 |
|---|---|
| "给这个选题写脚本" / 选题已 confirmed 进入脚本阶段 | ✅ 本技能 |
| 选题还没立项（未过 C 闸门） | ❌ 先回 yt-fenxi |
| 资料报告缺失/完整度 <60 | ❌ 先回 yt-ziliao 补采 |
| 改脚本（人审反馈后修订） | ✅ 本技能（版本号 +1） |
| 剪辑/配音/字幕执行 | ❌ 人工或剪辑工具（本技能只出 brief 级标注） |

## 铁律（违反即返工）

- **禁编造**：事实/数据/引语/画面均无凭证不出场（HR-4/HR-5）；凭证格式 `[凭证 N] 引用+出处`。
- **禁越权写状态**：选题状态只读（config `state_readonly`），脚本写完**不改**选题状态（选题表无"脚本撰写"状态值，写即违规）。
- **空则填、不覆盖**：回填「脚本字数」等字段时已有内容绝不冲掉。
- **禁离场词**："以上就是本期全部内容/感谢收看/点赞关注/下期再见"——出现即硬拦截。
- **禁自我调研**：所有素材来自 yt-ziliao 报告 + yt-fenxi 分析卡；缺失→回 yt-ziliao 补采桥接。
- **token/ID 原样全文**：从 config.yaml 取，缩写必 400；回填字段前必须先 `+field-list` 查真实字段类型——字段名带"链接"≠URL 类型（实测「选题脚本链接」是 text，只收纯字符串；格式细节见 yt-ziliao/references/lark-field-formats.md）。
- **每次阶段跳转同步更新脚本文件头 `当前阶段` 字段**；断点恢复只读文件续跑。

## Progress

```markdown
脚本工程进度（选题编号 T-XXXX-XXX）
阶段 1/6：输入契约校验 [自动]
- [ ] Step 0 N9 九字段 + 报告完整度≥60 + config 就绪 [自动]
- [ ] Step 0.5 老选题 N9 契约补建（九字段缺失时，从 yt-fenxi 分析卡+池记录反推，人确认后写入） [自动+确认]
阶段 2/6：原爆点筛选
- [ ] Step 1 挖 5-10 候选画面（yt-ziliao 报告+fenxi 反常识点） [自动]
- [ ] Step 2 五维评分（≥4.0 进 Top3 备选） [自动]
- [ ] Step 3 人拍板开场画面 [硬闸门] [可回环]
阶段 3/6：产前准备
- [ ] Step 4 B-Roll 素材清单（按时间轴，来源销号制） [自动]
- [ ] Step 5 法律口径预审（消费敏感主体清单，逐句标 [法审]） [自动] [需确认]
阶段 4/6：脚本装配
- [ ] Step 6 元数据头 + 四模块骨架 [自动]
- [ ] Step 7 三轨正文填充（含拍点/凭证/情绪曲线） [自动]
阶段 5/6：质量闸门
- [ ] Step 8 scan_script.py 硬指标扫描 [自动]
- [ ] Step 9 软维度评分 + 预期留存档位标注 [自动]
阶段 6/6：人审与交付
- [ ] Step 10 人审拍板 [硬闸门] [可回环]
- [ ] Step 11 交付：飞书 doc + 本地存档 + 回填脚本字数 + 风格决策记录 ≥3 条 [自动]
```

**阻塞提示模板**：
```markdown
⚠️ 当前阻塞：等待你拍板（阶段 2/6 Step 3 / 阶段 6/6 Step 10）
你可以：
- 回复"选候选 N"→ 进入装配
- 回复"都不满意 + 说明"→ 回 Step 1 重挖
- 回复"先改方向"→ 回 yt-fenxi 改角度（本技能不改选题方向）
```

**回环映射**："扫描硬拦截"→ 阶段 4 Step 7；"法审不通过"→ 退回 yt-fenxi 重评选题（终止）；人审修改意见 → 对应阶段回退。

## 执行流程

### 阶段 1：输入契约校验

读取 yt-fenxi 分析卡 N9 输出契约九字段（record_id/选题标题/选定切入角度含视角层/核心钩子/目标受众/反常识点清单/待验证项清单/资料采集链接/涉及敏感主体清单）。校验：
- 九字段齐全 + yt-ziliao 报告链接有效 + 报告完整度 ≥60 → 通过
- 缺项/完整度不足 → 报缺失清单，指明回 yt-fenxi 或 yt-ziliao，**终止不硬跑**
- config.yaml 缺失 → 从 config.template.yaml 复制，走初始化问答（池 token/存档目录/飞书文件夹/频道阶段）后重跑

**老选题 N9 契约补建（Step 0.5，必跑）**：N9 输出契约 2026-09-09 才建立，此前 confirmed 的老选题池记录必然缺字段（T-2026-008 实战踩过：跑到阶段 2 才发现契约不齐，只能停下来补）。此时**不停机硬跑、不跳过**：从 yt-fenxi 分析卡 + 池记录已有字段反推九字段，列出补建清单（字段名→取值来源→拟填值）找人确认后写入分析卡，再进阶段 2。补建本身不改选题方向，不触发重新立项。

### 阶段 2：原爆点筛选

按 `references/story-original-explosion.md` 执行：从 yt-ziliao 报告五个来源挖 5-10 个候选开场画面，五维评分（画面/冲突/基因揭示力/凭证/代入），≥4.0 进 Top3 备选。**铁律 2 前置**：无凭证候选直接淘汰不参评。Top3 + 落选理由（50-100 字/个）呈人拍板。

### 阶段 3：产前准备

1. **B-Roll 清单**（quality-hardrules.md §二）：视觉轨需求逐条落清单（时间码/画面/类型/来源/状态），yt-ziliao 报告内素材优先，报告外标"自行获取+合法途径"，拍摄前销号。
2. **法律口径预审**（§三）：消费 fenxi 敏感主体清单，六口径逐项过；涉敏感主体台词标 `[法审]`；结论三态（通过/修改后通过/不通过），写入交付物 Part 2。机器只标雷区不排雷，人终审。

### 阶段 4：脚本装配

按 `references/script-template.md` 产出结构，按 `references/writing-playbook.md` 的工序施工：

1. **元数据头**九字段 + 三视角检查
2. **素材卡片化 + 卡槽归类**（writing-playbook §二）：素材先做成单点卡片（一张一事带来源），强行归入 5-7 个叙事卡槽——卡槽逻辑不通顺前不写连接词
3. **四模块骨架**（Hook ~1min / 主体 6-8 子节点 ~23min / 高潮 ~3min / 无缝 CTA ~1min；20-30 分钟档总字数 5600-8400）
4. **逆向出稿**（§二）：先写终极 Payoff → 反推前置条件 → 写 Hook → 填中场连线
5. **三轮降维修改**（§二）：Draft1 骨架（可到 8500 字）→ 冷却（同会话内至少隔一轮对话）→ Draft2 剪枝压到目标字数（砍 ≥15%）→ Draft3 朗读拆句 + 配双轨
6. **情绪曲线按事实梯度法**（§一）：事实分 L1-L4 逐步释放；25 分钟节拍表为 v0 默认；中段（3000-5000 字）凭证 <2/千字 = 塌陷，补或砍
7. **三轨正文**：听觉（台词≤20 字/句、停顿重音符号、SEO 词）+ 视觉（客观动作、B-Roll 编号、时长估算）+ 控制（情绪、拍点、凭证、可砍级）
8. 钩子按 `references/hook-retention-playbook.md` 五变体选 3 种出**三版对比表**（频道阶段决定开放范围）；rehook 每 2-3 分钟、手法轮换；子节点按"次好点第一"排序；视觉轨画面按 `references/material-to-visual.md` 五类转换表写，同步生成 B-Roll 清单行

### 阶段 5：质量闸门

1. `python3 scripts/scan_script.py <脚本> --duration <分钟>`——硬拦截全部清零
2. 软维度评分（原爆点五维 + 划停力 + 赛道契合，0-100）+ **预期留存档位标注**（B1/B2/B3 对应档，标注"未经实证，待 fupan 校准"）

### 阶段 6：人审与交付

人审（硬闸门）：展示评分表+扫描报告+待决策项（开场画面/钩子变体/法审结论），等明确拍板。通过后：
- 飞书 doc：按 script-template.md §四 交付物结构建文档（标题白名单清洗后 create，见 write 规范），存入 config `doc.folder_token`
- 本地存档：`paths.output_dir/<选题编号>-v<版本>.md`
- 回填：「脚本字数」空则填不覆盖；状态**不动**
- 汇报：一句话交付摘要 + 预期留存档位 + 待 fupan 校准项

## 数据契约

| 项 | 约定 |
|---|---|
| 输入 | yt-fenxi N9 九字段 + yt-ziliao 报告 URL + 完整度 ≥60 |
| 时长档 | 只保留 20-30 分钟一档（拍板）；字数 5600-8400（280 字/分 ±10%） |
| 输出 | 飞书 doc（结构见模板 §四）+ 本地 md 存档 + 脚本字数回填（空则填） |
| 下游 | fupan 消费：段落时间码坐标（留存掉粉点映射用）；制作四件套消费：B-Roll 清单+分镜 |
| 校准 | 发布后 B1-B3 回流，滚动修正 wpm_anchor/软维度权重/子节点配比（记入 config 变更历史） |

## 依赖

| 技能 | 强度 | 说明 |
|---|---|---|
| yt-fenxi | 硬依赖（输入） | 消费 N9 输出契约；法审不通过时退回它重评 |
| yt-ziliao | 软依赖（凭证） | 素材缺口回它补采；桥接失败时本次脚本只能写到"凭证齐"为止 |
| lark-doc / lark-base | 硬依赖（交付） | 建飞书文档/回填字段，经 Skill() 调用不直连 API |

## 常见错误

| 错误 | 正确做法 |
|---|---|
| 资料不够就自己上网搜 | 回 yt-ziliao 补采桥接；脚本阶段零自研 |
| 画面写"气氛紧张/十分愤怒" | 写客观动作："他把手中的笔放下，三秒没有说话" |
| 写完把选题状态推到"脚本撰写" | 状态只读；池里没这个选项，写即越权 |
| CTA 三件套全要 | 单一动作：跳推荐视频（连续观看优先） |
| 用"我觉得/大家都知道"增强说服力 | 数据+引语；观点必标归属方 |
| 金标准锚点空着就不管评分 | 锚点未建期，评分旁强制标"未经实证"；锚点建成后重评 |
| 风格靠 AI 默认输出 | 每期记录 3 个风格决策（runtime/style-log.md），3-5 期后固化 voice-profile |
| 人审说"感觉不对"就全文重写 | 先查 writing-playbook §四 反馈翻译表，只改诊断出的结构位置 |

## 修改轮次规则

1. **轮次定义**：每次用户给出反馈并等待新稿 = 一轮；总上限 3 轮
2. **人机分工**（意图/技术双轴，writing-playbook §四）：用户只能改**观点/故事走向/语气风格**；结构/节奏/指标层（拍点/视听比/字数/事实梯度）归 AI，用户只验收——防止改 A 坏 B
3. **版本管理**：每轮定稿存独立文件 `runtime/.drafts/<选题编号>-v<N>.md`，不覆盖；"回到 v2" = 拿 v2 文件继续改，禁止凭记忆重写
4. **3 轮后仍不满意 → 停止改稿**，回 yt-fenxi 重评选题角度（是切入角度的问题，不是执行的问题），不继续磨稿

## 风格演化机制（从零起号的风格策略）

频道从零开始，**不预设风格，用演化日志让风格自然涌现**：

1. **每期定稿时**（Step 11 内）：记录 ≥3 个风格决策——这期钩子为什么选这个变体？为什么某段语速/情绪这样处理？为什么用这个转折句型而不是那个？写入 `runtime/style-log.md`（一期一节，含日期/选题编号/决策+一句理由）
2. **每期交付时**：风格对照检查——数"标准 YouTube 腔"占比（"大家好""今天我们来聊聊""让我们一起来看看"类句式），>30% 警告并在 style-log 记一笔
3. **3-5 期后**：回读 style-log，重复 ≥3 次的同类决策 = 风格雏形，固化为 `references/voice-profile.md`（常用句型/禁忌词/节奏偏好/叙事惯用路径），此后写脚本前强制读取
4. **原则**：风格是复盘出来的，不是设计出来的；v0 期宁可结构对但声音平，也不硬凹人设

## 发布后校准机制（第一期起生效）

每期发布后 48 小时-1 周内执行（数据源：fupan 技能或人工截图 YouTube Studio）：

1. **拉数**：前 30 秒留存率、AVP、相对留存曲线的掉粉点时间戳
2. **对照**：把本期软维度评分与真实留存对比——评分高但留存差的环节，找脚本文本原因（铺垫超长？拍点失效？视听比失衡？）
3. **修正**：小偏差记 style-log；同一偏差出现 ≥2 期 → 升级（改 config 参数或把软建议升为硬指标）
4. **锚点**：第 1 期发布后即把本期脚本+真实留存数据注册为首个金标准锚点（`references/gold-standard-anchors.md` 建表），此后每期与锚点对比评分
5. **节奏**：3-5 期后输出一次校准报告（哪些参数改了、依据是什么），经人确认后固化进 config.template
