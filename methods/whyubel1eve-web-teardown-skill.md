---
name: web-teardown
description: >
  扒一个网站的设计系统 —— 不截图、不用视觉模型，直接读 CSSOM 把它量出来。
  任何能跑 JS 的模型都能用。

  当用户说「扒一下这个站」「拆解 / 解剖 / 逆向这个网站的设计」「这个站的配色是什么」
  「提取它的 design token」「它用的什么字体 / 字号阶梯 / 圆角 / 阴影 / 缓动」
  「这个站的设计系统长什么样」「teardown」「抄一下这个站的设计规范」
  「帮我分析这个页面的视觉规范」时使用。给一个网址就能开工。

  产出：一份可发布的「设计解剖图」HTML，含 OKLCH 色板与 token 溯源、
  复合文字样式阶梯、字距曲线、缓动曲线库存、z-index 层级梯、间距网格，
  以及最有价值的一节 —— **系统漂移检测**：token 里写的和页面上实际跑的对不上的地方。

  三条采集通道：本机 Chrome 全自动 / agent 自带的浏览器工具 / 浏览器控制台手工粘贴。
  最后一条零依赖，任何环境都能用。

  不适用：需要看「长什么样」而不是「用了什么值」的任务（那要截图）；
  纯 canvas / WebGL 渲染的页面（没有 CSSOM 可读）；登录后才能看的页面
  （先让用户自己登录，再走通道 B 或 C）。
---

# web-teardown · 把一个网站的设计系统量出来

**核心**：设计系统是有数值的。颜色、字号、字距、圆角、缓动、层级 —— 全都躺在 CSSOM 里。
直接遍历读出来，比任何"看截图猜配色"都准，而且**完全不需要模型有视觉能力**。

> 下文 `$SKILL` 指本 skill 所在目录。装在哪就替换成哪，
> 也可以先 `export SKILL=<路径>` 再照抄命令。

## 这个 skill 最值钱的一节是「漂移」

配色和字体谁都能抓，抓完就是一张频次表，没意思。
真正只有全量测量才能撞见的是这个：**token 里写的，和页面上实际跑的，对不上。**

- 速度 token 定义了 4 档，实测跑出一堆表外的值，用得最多的那个根本不在表里
- 定义了 18 条缓动曲线，实测只有 1 条真的在用，其余 17 条是死库存
- 中性色全部调过色相，唯独一个是纯灰 —— 漏网的那一个
- 三成以上的高频颜色追不到任何 CSS 变量

人眼读 CSS 读不出这些，得靠把整页量一遍再和 token 表对账。
**写报告时把漂移放最前面** —— 它既是最硬的信息，也是唯一让读者觉得"这不是吹捧稿"的东西。

## 流程

### 1. 选采集通道

三条通道，同一个提取器（`$SKILL/assets/probe.js`），选能跑的那条。
详细命令和各家 agent 的工具名对照见 `references/channels.md`。

| 通道 | 适用 | 能力 |
|---|---|---|
| **C · 本机 Chrome** | 有 Node ≥22 + Chrome，能开子进程 | 最全：滚动触发懒加载、多视口、明暗双主题 |
| **B · agent 自带浏览器** | agent 有浏览器/CDP 类工具 | 够用：能拿到完整 CSSOM，滚动要自己发指令 |
| **A · 控制台手工粘贴** | 什么都没有，或者页面要登录 | 零依赖，任何环境都行；只能采当前这一屏状态 |

默认试 C。跑不了就退 B，再跑不了走 A —— **不要因为环境不趁手就不做**。

```bash
# 通道 C：一条命令采完
node $SKILL/scripts/teardown.mjs linear.app --out td.json
```

### 2. 先看置信度，再决定嘴能张多大

`analyze.mjs` 会给出 A/B/C 三级。**这一步不能跳过**，它决定了报告里的每一句话能怎么写：

| 级别 | 判据 | 能说什么 |
|---|---|---|
| **A** 系统可读 | ≥60 个语义 token | 可以写"他们定义了…""这套系统的意图是…" |
| **B** 部分可读 | 15–59 个 | 配色字体以实测为准，token 只作旁证，判断要降调 |
| **C** 仅实测 | <15 个 | **只能写"实测出现…"**，一句"他们打算…"都不许写 |

C 级站（CSS-in-JS 不导出变量的那种）照样能做，但报告的性质变了：
从"解剖设计系统"变成"统计页面用了什么"。**在报告开头就说清楚**，别让读者以为你读到了人家的规范。

```bash
node $SKILL/scripts/analyze.mjs td.json --out an.json
```

### 3. 出报告

```bash
node $SKILL/scripts/report.mjs an.json --out teardown.html
```

模板保证「每次都不难看」。但要当门面作品发出去时，**照 `references/report.md` 手写版面** ——
模板是基线不是天花板，手写才可能"这次特别好"。手写时数据仍然从 `an.json` 取，不要自己编数。

### 4. 写结论的纪律

这个 skill 的产出会被当成事实引用，所以：

- **数字必须来自 `an.json`**，一个都不许估、不许圆整、不许"大约"
- **追不到 token 的颜色就说追不到**，不要替它编一个来源
- **跨域样式表读不到时**（`meta.stylesheetsBlocked > 0`）交互态数据不完整，报告里要标注
- **只采了一个视口/主题就别写"响应式做得好"** —— 没测过的事不能下结论
- 发现一个站做得糙，**照直说**。吹捧稿没人看，而且这类内容的可信度全靠敢说难听话

## 参考文档

按需读，不用一次全读：

| 文件 | 什么时候读 |
|---|---|
| `references/channels.md` | 选通道、通道跑不通、要多视口/多主题/登录态 |
| `references/analysis.md` | 想看懂 `an.json` 的字段，或要自己挖新发现 |
| `references/report.md` | 要手写报告版面，或改模板 |

## 已知边界

- **纯 canvas / WebGL 页面**没有 CSSOM，采不到东西 —— 提前看一眼再动手
- **跨域样式表**（CDN 上的 CSS）读不出 `cssRules`，交互态（hover/focus）会缺
- **登录后的页面**：通道 C 是干净 profile，登不进去；让用户自己在浏览器里登录，走通道 A 或 B
- **一次只采一个页面**。首页和内页的设计系统可能差很多，要全貌就多采几个页面分别出报告
- 采到的是**这一刻这一屏配置下**的结果。改了视口、改了主题，数就变了
