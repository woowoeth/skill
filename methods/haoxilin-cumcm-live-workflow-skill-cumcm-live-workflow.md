---
name: cumcm-live-workflow
description: CUMCM（全国大学生数学建模竞赛）真题全流程实战手册。用于完整打一场数模竞赛（环境准备→读题自查→建模出图→LaTeX论文→交叉审阅→填表→AI自查表终审→清理打包），内含题意理解红线、数值严谨性守则、去AIGC特征清单、写作规范、LaTeX编号与图表规范、2026 AI合规与终审核查框架。触发于用户开始完整做真题、备赛训练、即将正式参赛，或要求按AI自查表对成品论文终审整改。**启动关键词**：国赛、数学建模竞赛、真题全流程、按竞赛标准走完流程、备赛、环境检查/检查环境/准备环境、按自查表审查——命中任一即启动，启用后第一步自动执行环境检查。
---

# CUMCM 真题全流程实战手册

沉淀自多轮真实竞赛实战、交叉审阅与 AI 自查表终审整改，聚焦**可执行的流程、坑位与质量门禁**，通用适配任意 CUMCM 题目。

## 触发条件

**启动关键词**（任一命中即启动本技能）：
- 流程类：完整做真题 / 按竞赛标准走完流程 / 数学建模竞赛实战 / 打国赛 / 备赛训练 / 模拟竞赛
- 环境类：环境检查 / 检查环境 / 准备环境 / 环境就绪
- 终审类：按 AI 自查表审查 / 自查表终审 / 论文终审整改

具体场景：
- 用户开始完整做一道真题（练手或正式比赛）
- 备赛训练：模拟完整流程
- 已出题、要求"按竞赛标准走完流程"
- 拿到成品论文，要求按 AI 自查表终审整改
- **启动后第一步：自动执行 §0 环境检查并报告完成度，再进入读题**

## 工作区约定

- 题目材料放 `PROJECT_ROOT/<题号>/`：原题 PDF、格式规范 doc、附件/
- 建模产物（代码/npz/图）放题号目录；论文放 `题号/paper/`；结果 xlsx 放题号目录
- **附件模板只读**：填写时 load 原模板写单元格另存，不覆盖原附件

## 流程总览（按序执行）

```
0. 环境检查 → 1. 读题+格式规范 → 2. 逐问建模+出图（至少8图）
→ 3. LaTeX 论文 → 4. 交叉审阅（≥2轮）→ 5. 填 result 模板
→ 6. AI 自查表驱动终审 → 7. 清理+打包
```

---

## 0. 环境检查（启用后自动执行，无需用户要求）

### 0.1 自动检测并报告完成度（先做，不询问）
技能一经启用，立即后台探测本地环境，输出**前置条件完成度清单**：

| 组件 | 检测命令 | 通过标准 |
|---|---|---|
| Python | `python --version` | ≥ 3.12 |
| numpy / scipy / pandas / matplotlib / openpyxl | `python -c "import numpy,scipy,pandas,matplotlib,openpyxl"` | 全部可导入 |
| scikit-learn、black（可选） | 同上 | 可导入 |
| LaTeX（xelatex） | `xelatex --version` | MiKTeX 或 TeX Live 可用 |
| ctex 宏包 / 中文字体 | `kpsewhich ctex.sty` + SimSun/SimHei 存在 | 通过 |
| pdftotext / pdftoppm（可选） | `--version` | 通过 |

报告格式示例：
```
[环境检查] Python 3.12.10 ✓ | numpy ✓ | scipy ✓ | pandas ✓ | matplotlib ✓ | openpyxl ✓
LaTeX (xelatex) ✗ 未安装 | ctex 宏包 ✗ | pdftotext ✗（可选）
完成度：6/9  ✓ 必装组件缺失：LaTeX、ctex
```

### 0.2 确认后自动安装缺失组件
把清单交给用户确认（"缺失组件是否自动安装？"），**得到明确确认后**执行：
- **pip 组件**：`pip install X -i https://pypi.tuna.tsinghua.edu.cn/simple`（国内镜像）
- **LaTeX（xelatex + ctex）**：优先 `winget install MiKTeX.MiKTeX`（或提示用户用官方安装器 / TeX Live）；装后需刷新 PATH 或重开终端
- **poppler（pdftotext / pdftoppm）**：`winget install poppler`（可选组件，缺失可跳过，不阻塞）
- 安装完成后**复测**，更新完成度清单直至 100% 或用户手动处理；最终报告"环境就绪，可开始读题"
- 红线：**未获用户确认不得安装任何软件**；涉及系统级变更（装 LaTeX/驱动等）时给出明确提示
- 网络：pip 走清华镜像；若网络受限（如需代理），先探测 `HTTPS_PROXY` 环境变量并询问用户

## 1. 读题 + 格式规范

### 题意理解优先（最高优先级教训，任何题目通用）
- **题干明写的物理事实必须建模进去**：题面给出物体有宽度/厚度/形状 → 碰撞、干涉、间隙类判定必须用其**真实几何**（矩形/多边形/角点-中心线距离判据），严禁简化为"中心线/质点间距"判据——后者会漏检真实干涉，导致终止时刻、临界参数等关键结果整体失真
- **几何初始条件必须用解析公式独立验证，禁止只信单一来源**：凡能用公式从题干数字推出的参数（如半径=螺距×圈数、长度=孔距+2×孔心距、角度=弧长/半径），一律推导核对一遍，不能只信视觉读图
- **后问对前问误差高度敏感**：初始条件错→中间判据错→后续结果连锁全错；问题间耦合必须每步验证
- **无官方答案时的题目自查（真实比赛核心）**，读完题后逐条执行：
  1. **题干数值→解析公式独立推导**：把题面每个数字代入公式算一遍，凡是能算出来的参数不许依赖读图/猜
  2. **单位与几何自洽检查**：所有长度/时间/速度换算成同一单位系；画示意图核对几何关系
  3. **边界情形推演**：取极端参数（最大/最小）走一遍模型，看结论是否符合物理直觉——能抓出大部分"模型没建对"
  4. **多方法交叉验证**：同一结论至少两种独立途径（解析式 vs 数值扫描 vs 特例验证），一致才算数
  5. **把"题面明写的物理事实"列清单**：写进代码注释/文档，建模时逐条对照是否全部体现

### 格式规范要点（以当届官方《格式规范》为准）
- 电子版论文**第 1 页必须为摘要专用页**（页码自摘要页从 1 起、页脚中部）
- 正文页数以当届规定为准（如 2026 修订稿：正文 ≤30 页，附录页数不限）；**无目录**
- 附录 = **支撑材料文件列表 + 全部完整可运行源程序**（缺失可取消评奖资格）。附录结构**必须**如下模板：

```
附录
├── 支撑材料文件列表（三线表：文件 | 用途 | 运行方式）  ← 与压缩包逐项一致
├── 附录A：问题一求解主程序（p1_xxx.py）  ← \lstinputlisting 全文
├── 附录B：问题二……（每个脚本独立一个附录小节）
├── …（一个源文件 = 一个附录小节，全部完整列出，禁止省略/截断/伪代码）
└── 附录N：图表生成脚本 / 检验脚本
```

- **代码完整性铁律**：附录里的每个脚本必须**可直接运行复现论文结果**——`\lstinputlisting` 列出**全文**，不得只贴核心片段、不得用"见代码文件"代替、不得伪代码冒充；脚本依赖的中间数据（npz/xlsx）一并列入支撑材料
- **文件列表一致性铁律**：附录文件列表与支撑材料压缩包**逐项对齐**（文件名、数量完全一致）；压缩包含什么，列表就列什么——zip 有几十个文件而附录只列几个 = 评委抓包点

### 附录与支撑材料一致性检查（提交前必查）
1. 列出压缩包全部文件名 → 与附录列表逐行比对，**数量与名称必须一致**；
2. 抽查附录代码：把 2-3 个脚本复制出来直接 `python xxx.py` 运行，结果必须与论文数值一致（能复现）；
3. 检查是否所有"建模用到的源程序"都进了附录（含图表生成、检验、敏感性脚本，不只是主求解器）；
4. 支撑材料含：代码/ + 图表/(PNG+PDF) + 结果/(xlsx+npz) + AI工具使用详情.pdf；附录列表须覆盖这些目录；
- 电子版 PDF ≤20MB；支撑材料 ZIP/RAR ≤20MB
- 匿名三查：全文/代码/PDF 元数据（author/title 为空）无学校、姓名、赛区、用户名、绝对路径
- 读完后产出《题目分析报告.md》+《术语表格.md》（或按用户要求延后）

## 2. 建模 + 出图（核心循环）

每个问题固定两步：**先明确思路 → 再建模出图**，逐问重复，最后才写论文。

### 出图规范（顶刊级，务必达标）
- matplotlib 300dpi（建议 600dpi 矢量 PDF），**PNG+PDF 双格式**；`savefig(..., bbox_inches='tight')`
- **图表文字强制中文**：标题/坐标轴/图例/colorbar 全部中文（数学符号 x,y,t*,p,R,κ 与标准单位 m/s/s 除外）；`plt.rcParams['font.sans-serif']=['SimHei','Microsoft YaHei']` + `axes.unicode_minus=False`
- **配色统一**：全文一套 PALETTE（推荐蓝橙绿 `#4472C4/#ED7D31/#70AD47`，或 Nature 冷色 `#0072B2/#009E73/#56B4E9`，或美赛 O 奖 `#2E5C8A/#E8743B`）；禁彩虹/荧光/高饱和；对比色区分度≥4.5:1
- 数据图：线条≥1.8pt、标记 6-8pt、浅灰虚线网格 `#E0E0E0`、坐标轴含单位、图例不遮数据；图内文字≥10pt
- 图题在下方居中"图X ..."；图与图题同页，图后紧跟分析文字，禁止孤立图表
- 总图数 ≥10 张（建议 12-15）；每图生成后**视觉模型 QA**（无乱码/贴边/数据矛盾）
- 宽表格（>6列）必须 `\resizebox{\textwidth}{!}` 或 `tabularx`（实测踩坑）

### 数值严谨性红线（实测踩坑，通用）
- **链条/连续系统类题目：不要信单点迭代的"碰撞/临界"结论**。弯曲段求解器会跳分支产生伪解（参数突变、数值爆炸）。必须用：①全局搜索约束可解性 ②时间连续初值重解 ③解析封闭解交叉验证
- **速度场用弧速法/解析传播，不用位置差分**：急弯处差分低估弧速可达 7% 量级
- 解析近似式 ≠ 数值精确解时：以数值为准，解析式放"机理分析"作定性解释，行文不要显得"用数值凑"
- **自建验证基准（无官方答案时）**：用解析特例（退化情形/对称性）、极端参数、第二独立实现建立基准，做完每问先对表

## 3. LaTeX 论文

- ctexart + xelatex，`\documentclass[zihao=-4,UTF8]{ctexart}`（小四正文）
- 全宋体：`\setCJKmainfont{SimSun}`；标题靠字号+加粗区分（SimSun 无粗体，会伪粗，正常）
- 页码必须显式：fancyhdr `\fancyfoot[C]{\thepage}`（ctexart 默认可能不显示页码，实测踩坑）
- 摘要页：标题居中 + "摘 要"居中加粗小标题 + 正文左对齐 + 关键词顶格
- 公式：向量加粗 `\mathbf{A}`、范数 `\lVert`；推导分步 align 展示，代数变换要"一目了然"
- 交叉引用：编译两次确认无 undefined；**所有图/表/式引用一律 `\ref`/`\eqref` 自动编号，严禁硬编码"图N/表N/式N"**（插入新图表后硬编码编号会整体错位）

### 论文结构硬性要求（源自韩中庚《数学建模竞赛论文的写作方法》，逐条对照检查）
- **敏感性分析不可缺少**：模型检验必须有——误差分析（模型/数据/舍入误差）验准确性 + 敏感性分析验可靠性（关键参数小扰动→结论变化量化列表）。**敏感性数值必须实跑脚本得出，禁止凭空填**（且注意扰动响应可能不对称，如实呈现）
- **假设必须"讲理"**：每条假设补必要性+合理性+正文引用，禁止干条目罗列；数量 3~4 条为宜，假设过多显模型脆弱
- **模型评价四要素齐全**：优点（不过分夸张）+ 创新点 + 缺点（不回避）+ 改进方向（针对问题的想法）
- **层次标题排比**：同一层标题用词类型相同、语义相关；标题内禁手写"问题X"前缀（与自动编号重复）
- 评阅四标准：假设合理性/建模创造性/结果正确性/表述清晰性——每章自查
- 摘要"重中之重"：**五段式**——第一段背景（120-150字）直接切入、禁"本文研究了..."空洞开头；第二段起每段段首"针对问题X"逐问独立成段（每段100-130字：方法+量化结果）；收尾段（80-120字）写创新点+三类检验关键词；**总字符≤1000、1页内**；**量化数字≥8个**；不出现复杂公式表格
- 问题重述是"正确解读后的重新表述"而非复制；避免"用软件编程计算得到结果如下表"直接丢图无解释
- **问题分析=叙述推演式**：按"已知→推导→结果"链条叙述各问思路，每问独立成段；"禁止 bullet 罗列"指**段内**不要堆技术选型清单，**不等于取消小节结构**——问题分析仍须作为独立小节存在（见上方结构模板）
- **模型假设=极简**：每条一句话不带解释；"讲理"体现在正文引用处而非假设条目本身

### 论文成品结构模板（生成论文时必须完整照做，禁止删减小标题）
成品必须是一份**章节完整、编号统一**的论文。骨架如下（内容随题目调整，层级不得删减）：

```
摘要（五段式，独立一页）+ 关键词
1 问题重述与问题分析
  1.1 问题背景与任务
  1.2 问题分析（每问独立成段，交代"为什么选该模型"）
2 模型假设与符号说明
  2.1 模型假设（(i)(ii)...）
  2.2 符号说明（三线表）
3 问题一：模型的建立与求解
  3.1 模型建立（必要时 3.1.1/3.1.2 子步骤）
  3.2 模型求解
  3.3 结果与分析
4~7 问题二~五（同上，每问独立章节）
8 模型检验
  8.1 误差分析  8.2 灵敏度分析  8.3 稳健性分析
9 模型评价与改进
  9.1 模型优点  9.2 局限与改进
AI 工具使用声明
参考文献
附录（支撑材料文件列表 + 完整源码）
```

- **每个问题必须有独立章节，章内固定包含"模型建立/模型求解/结果与分析"三个小节**——这是"小标题齐全"的最低要求，任何 AI 味/去模板化修改都不得删除或合并这些小标题
- 章节编号用 LaTeX 自动编号 `1 / 1.1 / 1.1.1`，**禁止手写数字编号**；若团队偏好"问题一/问题二"作章名，用手写汉字节名并与自动编号**二选一全篇统一**，禁止混用
- **结构完整性优先于去 AI 味**：去模板化只作用于句式措辞，不得把结构化内容压成流水段落、不得省掉小标题

### 国奖级硬性指标（对照检查，缺项即扣分）
- **三类检验必须齐全**（误差/灵敏度/稳健性），每类有量化数据+图表，缺一最高省二
- 正文页数 25-30 页为优；不足时扩充优先级：公式推导细化 > 中间结果表 > 多场景对比（≥3组） > 图表 > 复杂度分析
- **编号公式 ≥15 个**（建议 20-30）：变量定义→中间推导→最终表达式→求解算法公式，每步有依据，禁止公式"从天而降"
- **参考文献 15-25 篇**：GB/T 7714、全部真实可检索、近5年≥60%、中英文混合（英文≥30%）；正文有 [1][2] 引用
- 表格 ≥18 张（含中间结果表），全部三线表、不跨页；图片 ≥10 张（结果对比/检验类数据图）
- 每个图表后必须有分析文字；匿名彻底（含文档属性、代码注释）

### 降重与降 AI 味（九类痕迹自查 + 四层改写）
- **AI 味 = 模板化结构 + 过度平滑 + 泛化内容 + 缺真实决策痕迹**；无官方 AI 率合格线，检测只是风险信号，禁止为"0%"机械洗稿
- **九类 AI 痕迹**：句长均匀、段落三点式对称、过渡词密集（首先/其次/再次/综上）、"不是X而是Y"过密、抽象大词（赋能/机制/范式）、内容空泛（提高准确性/增强鲁棒性）、结果永远完美、引用与判断分离、语言水平跳级——逐项自查
- **降重四层**（越上层越有效）：词汇 < 句法 < 逻辑 < 证据/事实；方法：摘要式重写、改信息顺序、抽象-具体转换、引用后解释、用自己的结果替换套话、图表承担信息
- **不能乱降**：公式/变量/模型名/数值/图表编号/算法顺序/引用内容
- **把 AI 从代写者改成审稿人**：让 AI"指出哪些句子空泛、缺少证据，不要替我重写"，而不是"帮我整段重写"
- 评委最认的"人类痕迹"：选择理由、参数来源、失败方案、异常点、边界条件

### 去 AIGC 特征（正文 + 代码，评委会抓）

#### 正文语言（四大维度）
- **弱化路标式过渡**：删"首先/其次/值得注意的是/综上所述/基于上述分析"；章节标题本身是路标，正文直接切入；"A需…；B需…"排比清单 → 改因果链（"因为…所以必须…"，每条交代理由）
- **打破完美对称句式**：长短句交替——复杂推导用长句、关键结论用短句加重；加入作者视角词（"经检验/实际上/写程序前先比较过"）模拟思考停顿
- **注入作者主体性**：模型选择交代权衡过程（"若简化为…误差约10⁻⁴，但递推中误差沿链累积…保险起见用精确式"）；局限要具体化（给出数字和经验判断，忌"未考虑摩擦"式空话）；可留探索痕迹（"一开始只检测…程序跑完显示无碰撞，但和物理直觉相悖…回头检查才发现…"）
- **摘要去模板化**：禁万能套话开头（"随着科技的发展""本文具有重要的理论意义"）与空洞收尾；**分段结构仍严格按五段式**（背景段+逐问"针对问题X"独立段+收尾段），去模板化只改句内措辞，不得合并或删除分段

#### 附录代码（六大特征 + 对策）
- **特征1 教科书式注释**（每行注释、docstring 完美统一）→ 只留"为什么"和"踩坑记录"，删"做了什么"
- **特征2 命名完美主义** → 循环内允许单字母/缩写；不同文件风格允许差异；保留无害临时变量
- **特征3 中英混杂特定句式** → 纯中文或纯英文口语化备注
- **特征4 缺乏个性化痕迹** → 加版本迭代注释（# v2: 修正了碰撞检测的边界bug）；保留被注释掉的旧方案；魔法数字注明来源
- **特征5 输出过于规整** → 打印加非正式进度提示；验证内联在 `__main__` 块；打印与解析/已知答案的对比备注
- **特征6 文件组织过于清晰** → 保留"复制粘贴+微调"的合理冗余，不必强行抽公共模块；代码内注明中间文件依赖
- **红线**：不要故意制造 bug；"不完美"只体现在注释/命名/结构痕迹，数值必须正确
- **精确数字克制**：机器精度级数字只在验证部分出现一次；不罗列精确扫描区间，写"对可行域内不同取值验证"
- 机理小节标题各异，不机械重复；不自夸，删"精确性/理论深度/逻辑闭环"式自我评价

### LaTeX 编号与图表规范（审阅实测，必看）
- **图表引用必须 `\ref`/`\eqref` 自动编号**：统一 `\label{fig:xx}`/`\label{tab:xx}` + 正文"图\ref{fig:xx}"；若已硬编码，编译后用 `pdftotext` 对比实际编号逐一修正
- **双子图标准格式**：图题两行居中——第一行"图X: 总图名"，第二行"(a) 子图1说明；(b) 子图2说明"（caption 内 `\\` 换行）；图内子图标题也要有 (a)(b) 标记；**caption 必须加 `justification=centering`**（否则多行图题第二行左对齐）
- **附录编号体系**：`\section*{附录}`（不编号）+ `\subsection*{附录A：...}`（手写字母），引言部分不占字母编号（避免"两个附录A"）
- **编号体系 v7（章节 vs 列表 vs 公式三档区分）**：
  - 章节标题：无括号数字递进（1 / 1.1 / 1.1.1），LaTeX 自动编号
  - 列表条目（模型假设/任务列表/算法流程）：**`enumerate` + `label=(\roman*)` 罗马小括号** `(i)(ii)(iii)`——**不能用阿拉伯 (1)(2)**：公式编号也是小括号阿拉伯，"式(4)"与"第(4)条"会混淆
  - 公式：`\begin{equation}` 自动 `(1)(2)(3)`；**多行 `align` 必须 `\notag` 只留一个编号**（三行约束不加 \notag 会各占一个公式号且无人引用，公式号整体跳跃）
  - **本节为编号规则的唯一权威**：全文各处提及"编号体系"均以本节为准；若用户提供的格式规范/范文要求"问题一/二"汉字编号，按上面结构模板的裁决执行（与自动编号二选一、全篇统一），不要自行切换
- **TeX 细节**：正文中文里的 `#` 必须转义 `\#`；`\boxed{}` 除非风格要求否则别用
- **竞赛论文禁用"与官方答案一致"表述**：验证必须建立在物理合理性/解析推导/双判据互证上（"与解析推导吻合""双重判据互证"）

## 4. 交叉审阅（关键增值环节）

用户会把论文交给其他 AI 审阅，反馈逐条处理。处理模板：

```
| 意见 | 处理 |
|---|---|
| 每条意见 | 定位→核实（跑代码/视觉QA）→修改→说明 |
```

- **先核实再改**：审阅者可能看错/看旧版——用视觉模型或像素分析确认，如实说明
- 审阅意见分类：①格式/排版（直接改）②数据矛盾（跑代码验证）③逻辑跳跃（补证明）④AI味（删套话）
- 摘要/正文/表格/图注**同步更新**，避免只改一处留下不一致
- 每轮处理完重新编译 + 视觉抽查关键页

## 5. 填 result 模板

- openpyxl load 原模板 → 写数据单元格 → 另存新文件（保留 sheet 名/表头/行列布局）
- 结果保留 6 位小数
- 验证：行数列数与模板一致、无错误值（#VALUE 等）、与论文数值一致
- 行映射通用提示：先按模板表头定位"龙头/第k节/龙尾(后)"所在行（位置表通常 x/y 两行一组、速度表每把手一行），抽查关键单元格与论文表格逐位比对

## 6. AI 自查表驱动终审（2026 新版 AI 合规 + 证据链核查）

触发：拿到成品论文，要求"按自查表审查/自查"或提交前终审时。

### 6.1 2026 AI 新规硬性红线（评委第一关，违者可取消评奖资格）
- **参考文献之前必须有「AI 工具使用声明」**，二选一模板：未使用（"本参赛队在竞赛过程中未使用任何AI工具。"）/ 使用了（"……主要用于【简要用途，如语言润色、代码调试等】，详细使用情况见支撑材料。"）——缺声明即"不符合规定"
- 支撑材料必须含 **`AI工具使用详情.pdf`**（①工具名称/版本 ②使用目的环节 ③提示方式与过程 ④采纳/人工修改/核验情况，语言润色除外）
- **论文、支撑材料、附录文件列表三处必须对齐**
- 摘要必须**单独一页、含每问量化结果、800~1000 字**；正文页数符合当届规范
- 匿名三查：全文/代码/PDF 元数据

### 6.2 自查表 26 条六维度框架（逐条判定：合格/存在问题/待核实）
一、AI 新规合规(4) 二、AI 幻觉与数据真实性(4) 三、建模逻辑与题意适配(5) 四、求解及结果合理性(3) 五、官方写作规范(6) 六、官方排版与页数(4)
（自查表 docx 用 python-docx 提取段落+表格，表格行即 26 条核查条目）

### 6.3 证据链核查法（不轻信论文自述，每条结论都要"能跑出来"）
- **范文/参考解答校准**：若存在获奖范文或公开参考解答，grep 其关键数字与论文逐位对比；多篇参考冲突时 → 独立复算裁决
- **独立复算**：从零实现关键结论（不调用论文代码），同时复算"另一种解读"是否成立 → 判定分歧根源，给论文加限定句防御（明确本文采用哪种约束/判据设定）
- **数据文件抽查**：按模板行映射抽查结果 xlsx 单元格与论文表格逐位比对
- **图表目检**：逐图 read_image 核对图注数值 vs 正文（阈值标注口径、时间范围、图例单位——实测常抓到图文不符）
- **PDF 程序化测量**（PyMuPDF）：每页首行/字数看结构；bbox 检测表格越界（max x ≤ 右边距）；元数据匿名；全文 compact 去空白匹配关键数字（规避字体间距导致匹配失败）
- **摘要字数程序化统计**（汉字数 vs 总字符数口径）

### 6.4 五段式自查报告模板
1. 自查结果汇总表（26 条 × 结果/位置/问题简述/整改建议）
2. 整体合规判定（一句话结论：能否提交 + 是否踩取消资格红线）
3. 问题分级汇总（🔴致命 / 🟠一般缺陷 / 🟡轻微瑕疵）
4. 优先级整改清单（按扣分权重排序，每条给到具体位置+改法）
5. 官方规范适配度与潜在风险预警（含"解释分歧风险"：结论与部分参考解答相左时如何防御）

## 7. 终审整改实操技巧

- **诚实重生成**：图表数据缺失/图文不符时从代码重算重绘，绝不手绘/编造；重生成值与既有结论互证（如扫描交点与二分临界值差异 <0.1%）；扫描数据 npz 存支撑材料供复现
- **敏感性实验必须实跑，且注意非对称性**：扰动 ±1.5% 级别的响应可能正负不对称（临界接触构型对"变宽/变窄"灵敏度不同），如实呈现并解释；脚本入附录，结果存 npz
- **叙事与附录代码一致性**：正文对早期方案/判据的描述必须与附录代码注释一致（如"旧判据得到明显偏早的时刻"而非"全程无碰撞"），评委必对照附录代码抓矛盾
- **误差百分比口径**：明确分母（以真值为基准 vs 以粗采样为基准），统一口径表述，避免"误差13%"歧义
- **编号体系 v7**：列表 `(\roman*)` 罗马小编号区分公式 `(1)(2)`；align 多行公式 `\notag` 只留一个号；图表引用全部 `\ref/\eqref`
- **LaTeX QA 管线**：两遍 xelatex → 日志查 `^!`/undefined/`Overfull \hbox` 分级（正文段落 >15pt 必修，代码附录长行可忽略）→ PDF 文本层抽查关键内容（AI 声明页码、新表页码、量化数字出现次数）
- **宽表排版**：文件列表/长内容表格用 `tabularx` X 列自动换行（比 resizebox 缩字清晰），超宽内容合并行
- **整改后联动检查**：摘要数字 ↔ 敏感性表 ↔ 附录脚本 ↔ 支撑材料 npz 四者一致；自查报告追加"整改完成记录"（含每项修复+成品页码）

## 跨平台代码红线（评委会抓）

- **全部路径用正斜杠** `'题号/xxx'`：Windows/Linux 通吃。禁止反斜杠和混合写法（实测漏网 62 处）
- 提交前 grep 检查：`Select-String -Path *.py -Pattern "\\\\"` 确认无路径反斜杠残留
- 附录代码用 black 格式化（`pip install black` + `python -m black *.py`），格式化后**重跑验证**功能未破坏

## 8. 仓库配套文件与用法（本技能自带工具，直接调用）

### 8.1 可执行验证模板（references/，直接改参数复用）
| 文件 | 何时用 | 用法 |
|---|---|---|
| `references/verify_xlsx_rows.py` | 填 result 模板后 | 校验 xlsx 行映射与数值：`python references/verify_xlsx_rows.py <结果.xlsx>` |
| `references/verify_pdf_metrics.py` | 论文编译后交付前 | PDF 结构/越界/匿名/关键数字体检：`python references/verify_pdf_metrics.py <论文.pdf> 412.47 0.4503 ...` |
| `references/sensitivity_framework.py` | 敏感性分析 | 参数扰动跑批框架：实现 `run_metric` 后运行 |

### 8.2 参考材料（templates/，供下载使用）
| 文件 | 用途 |
|---|---|
| `templates/格式规范.pdf` | 当届官方《论文格式规范》，§1 读题时对照 |
| `templates/AI工具使用规定(2026试行).pdf` | 2026 AI 合规红线，§6.1 终审时对照 |
| `templates/2026数学建模国赛AI自查表.docx` | §6 AI 自查表驱动终审的核查清单 |
| `templates/2026数学建模国赛标准论文Word模板.doc` | Word 排版参考（三线表/页码/样式），LaTeX 论文亦可对照其结构 |
| `templates/2026国赛国奖级论文AI自动优化升级提示词.docx` | 国奖级九大模块升级标准（数据预处理/模型创新/求解/检验/写作/图表/排版）与 🔴🟡🟢 硬性指标核查清单——作为"国奖级指标"章节的展开细节，生成与终审阶段均可对照 |

### 8.3 最小工作示例
- `WORKED_EXAMPLE.md`：通用"水箱注水"案例，演示从题面→解析验证→数值求解→交叉验证→敏感性→结构化交付的完整链路。
- **正式工作前先读一遍**，让"对的样子"进上下文；把案例中的模型/数据换成你的真实题面，其余环节照做。

## 交付物清单

```
PROJECT_ROOT/
├── 题号/paper/main.pdf        # 电子版论文（第1页摘要）
├── 支撑材料.zip               # 代码+图表+结果（含 AI工具使用详情.pdf）
└── 题号/
    ├── 原题.pdf 格式规范.doc 附件/
    ├── p1_*.py ... p5_*.py    # 核心可运行代码（black格式化+正斜杠）
    ├── result1/2/4.xlsx       # 填好的结果模板
    ├── figures/               # 全部图 PNG+PDF
    └── 建模结果汇总.md
```
