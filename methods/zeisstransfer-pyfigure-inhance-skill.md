---
name: pyfigure-inhance-skill
description: 为当前项目首次生成 Python 科学图表，或审查、返工、美化及批量修改已有图表的源码。使用 Matplotlib 和矢量输出，忠实呈现已有建模结果；只审查时不改文件，不用于重新建模、补做统计分析、表格排版或图像生成修图。
---

# Python 科学绘图增强

## 入口与按需读取

先用当前请求判断模式；在开始时简短说明模式和目标范围。

| 请求 | 模式 | 必读流程 |
| --- | --- | --- |
| 根据已有数据或结果生成新图 | 首次绘图 | [first-plot.md](references/first-plot.md) |
| 检查旧图、修改、美化、按意见返工、批修 | 审查与返工 | [review-repair.md](references/review-repair.md) |

两者均读取 [style-export.md](references/style-export.md) 与 [review-checklist.md](references/review-checklist.md)。仅首次选型、图型不明确或需要换图型时读取 [chart-guide.md](references/chart-guide.md)。不要加载另一模式的流程、课程截图、历史审阅材料、测试或 README；它们不参与日常运行。

首次选择配色、用户要求试色/换色或确认项目默认时，另读 [palette-guide.md](references/palette-guide.md)。已有默认且无需改色时只读取项目配色配置，不重复研究网络配色。

“审查、诊断、评价”只读不改；“修改、美化、返工、批修”授权修改指定目标，不代表可以改全项目。模式由任务语义决定，不要求用户记命令。

## 共用底线

- **忠实性第一**：完整呈现任务所需的已有建模结果。不自行改点、删点、补数据、平滑曲线、重拟合或变更统计口径；不新增装饰性符号、阴影、置信区间或显著性标记。已有合法统计要素应忠实保留；必要的轴标签和图例不是装饰。
- **专业性是必要质量要求**：修正粗糙、拥挤、误导或风格割裂的呈现；不把朴素等同于错误，也不以缺少官方评分条款为由忽视质量。不宣称某种外观对应固定奖项。
- **源码出图**：默认 Matplotlib、PDF 正式图和同源 PNG 检查预览，优先 `tight_layout`。不用图像生成工具、Pillow 或其他像素后处理修补科学图表。不为使用本 skill 强行迁移已可正确运行的兼容绘图代码。
- **论文手约定**：中文固定宋体（SimSun），英文和数字固定 Times New Roman；文字必须视觉加粗，显示的坐标轴必须黑色实线且清晰突出，不采用细浅灰轴线。缺字体报告，不自行替换。图内一律不放标题，图题由论文手完成。字体、色条标签方向和间距执行共用样式参考；不能以 rc 已设置或文字未越界代替成图验收。
- **范围受控**：只从已保存结果出图。先检查运行入口的副作用，不运行会重新训练、重拟合或覆盖分析结果的默认主程序。需要新增分析时停止该部分，集中请用户决定。
- **有限迭代**：初次生成或修改后由 Agent 运行并初检；明确运行错误、乱码、裁切或遮挡最多再自动修复一轮，即每个已授权版本最多两次出图尝试。共享故障只按批修一次，不按图重复尝试同一故障。仍失败则停止该部分并说明；不因纯审美偏好反复循环。用户给出新意见才开启新的有界返工。人工明确要求的多次仅换配色是候选版本，不占故障返工次数，也不授权 Agent 自行无限试色。
- **人类验收不代签**：分别说明实际运行、Agent 检查和人工确认状态；看不到成图就标记未完成视觉检查。人工查看后需要改布局时，先针对性调整，必要时更换并说明原因。

## 当前项目作用域

运行部署位置：`<PROJECT_ROOT>/.agents/skills/pyfigure-inhance-skill/SKILL.md`。

1. 以当前调用的项目本地部署路径定位 `PROJECT_ROOT`，与 Codex 当前项目及目标文件归属核对。不是把临时 shell 工作目录或 skill 开发仓库当成目标项目。
2. 若 skill 经符号链接部署，以项目内的安装位置确定候选根，再核对真实目标路径；安装位置或项目归属不明时先询问，不从链接指向的开发仓库反推输出项目。
3. 写入前解析目标真实路径（含已存在父目录的链接），确认在项目内，并避开 `.agents`、`.git` 和 skill 本体。既检查源码，也检查导出、备份与临时文件路径；不硬编码机器路径。
4. 新项目可部署独立副本；生成的项目代码不得引用 skill 安装路径。环境依赖或权限缺失时说明，不擅自全局安装软件或扩大写入范围。

## 完成时交付

简短返回：目标图与源码位置、主要改动、实际检查及未解决项。人工未确认就保持未确认。批量任务逐图列状态，不用“全部合格”掩盖未检查项；备份和比较记录按返工流程处理。
