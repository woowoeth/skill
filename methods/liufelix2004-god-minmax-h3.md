---
name: minimax-h3-video
description: 用 MiniMax H3（海螺 3.0）生成成片级视频的完整方法论与提示词库。当用户提到「海螺」「Hailuo」「MiniMax H3」「H3 生成视频」「AI 视频提示词」「文生视频/图生视频/首尾帧/全能参考」，或需要把参考片逆向拆解成提示词、要把多段 H3 片段拼成 20–60 秒成片、要保持跨段的角色与风格一致性、要写带运镜与原生音频指令的分镜提示词时，使用本技能。也适用于用户只是发来一段视频问「这是怎么生成的」或「帮我做个 29 秒的片子」。
license: MIT
---

# MiniMax H3 视频生成

## 这个技能解决什么

H3 单次生成上限 15 秒。所有「30 秒 / 1 分钟成片」的需求，本质上都是
**分段生成 + 一致性接力 + 拼接**三件事。本技能给出这三件事的标准做法，
外加 5 个已验证跑通的分镜提示词模板。

## 先读硬约束

| 项目 | MiniMax-H3 |
|---|---|
| 单次时长 | **4–15 秒，只接受整数** |
| 分辨率 | 768P / 2K，24fps |
| 提示词上限 | 7000 字符 |
| 全能参考 | 图 ≤9；视频 ≤3（单段 2–15s，总 ≤15s）；音频 ≤3；混合总计 ≤12 个文件 |
| 音频 | 32kHz 立体声与画面同一次生成 |
| 首尾帧 | 0/1/2 张，`role=first_frame` / `role=last_frame` |

完整 API 调用见 `references/h3-api.md`。

## 核心工作流

### 第 1 步：判断要不要分段

| 目标时长 | 做法 |
|---|---|
| ≤15s | 一次生成。**在一条 prompt 里写多个分镜，H3 会自己切**——这是 2.x 时代没有的能力，别浪费 |
| 16–30s | 2 段（15+14 / 15+15）或 3 段（10+10+9） |
| >30s | 按叙事段落切，每段独立可看，别指望长距离一致性 |

**实测结论：**本库 5 个模板里，原本按 3 段（10+10+9）设计的分镜，
直接塞进一条 15 秒 prompt 里生成，H3 完整还原了全部镜头序列和标题卡。
**所以优先考虑「一条 prompt 内多分镜」，而不是无脑切段。**

### 第 2 步：写 prompt

结构固定为四块，顺序不要变（详见 `references/prompt-grammar.md`）：

```
[风格锚点]  画质/画幅/配色/材质/胶片颗粒 —— 一句话定死整体观感
[分镜序列]  镜头1 → 镜头2 → 镜头3，每个镜头后面挂 [运镜] 标记
[音频指令]  环境声 + 动作音效 + 对白/配乐，写清楚在哪一拍落
[负面约束]  不要出现的东西（多余肢体、错误文字、画面抖动……）
```

风格锚点**每一段都要原样重写一遍**，不要靠模型跨段记忆。

### 第 3 步：跨段一致性接力

三件事，缺一件就会飘：

1. **尾帧接首帧** — 上段最后一帧导出为图，作下段 `role=first_frame`
   ```bash
   ./scripts/extract_last_frame.sh segA.mp4 segA_last.jpg
   ```
2. **`role=reference_image`** — 角色定妆图 2–3 张（正面/侧面/特写）
3. **`role=reference_audio`** — 有对白时必传上段音频，否则音色会变

### 第 4 步：拼接

```bash
./scripts/stitch.sh 29 out.mp4 segA.mp4 segB.mp4      # 拼接 + 交叉淡化 + 锁死 29.000s
```

原理与逐条命令见 `references/stitching.md`。

## 提示词库

`prompts/` 下 5 个模板，都是从参考片逆向拆解、再用 H3 跑通验证的。
每个文件含：完整 prompt、API 参数、成片观察、已知坑。

| 文件 | 类型 | 状态 |
|---|---|---|
| `01-final-bet.md` | 红黑白平面矢量动画 / 赌局 | 已验证 15s |
| `02-hero-vlog.md` | 第一人称手持超英 vlog / 电影质感 | 已验证 15s |
| `03-night-comic.md` | 美式硬派漫画 motion comic | 已验证 15s |
| `04-cavalry-opening.md` | 史诗骑队开场 / 长镜 | 待验证 |
| `05-single-take-dialogue.md` | 单镜头对白特写 | 待验证 |

## 常见失败与对策

| 现象 | 原因 | 对策 |
|---|---|---|
| 出来是固定机位死镜头 | 没写运镜 | 每个分镜后挂 `[推进]`/`[横移]`/`[跟随]` 等标记 |
| 段与段人物长得不一样 | 只靠文字描述 | 必须传 `reference_image`，文字描述留不住脸 |
| 段与段音色跳变 | 没传参考音频 | 有人声就必须传 `reference_audio` |
| 画面里出现乱码文字 | 模型自造字 | 负面约束写「不出现任何文字」，标题卡单独做后期 |
| 想要的自创角色被画成某个知名 IP | 描述踩中了训练分布 | 加入明确的差异化特征（配色/材质/剪影），并在负面约束里排除 |
| 拼接处「啪」一声 | 音频硬切 | 用 `scripts/stitch.sh`，带交叉淡化 |

## 合规

本库只收录**自创设定**的提示词。不收录、也不建议写指向具体商业 IP
角色的提示词——那类片子拿不到商用授权，做出来只能自娱自乐。
参考片里若有 IP 角色，拆解时保留其画面语言（配色、运镜、剪辑节奏、
构图法），人物换成自己的设定，风格照样成立。
