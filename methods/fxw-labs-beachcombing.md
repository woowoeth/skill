---
name: beachcombing
description: 赶海潮汐与攻略查询工具。用于回答"去哪里赶海、什么时候去、退潮/低潮时间、能挖到什么、需要带什么、何时安全撤退"。触发词：赶海、退潮、潮汐、低潮、挖蛤蜊、捡海螺、抓螃蟹、大潮、小潮、海边挖货、beachcomb、tide、low tide、tidal flat。聚焦中国沿海。
origin: beachcombing
---

# 赶海 (Beachcombing) — 潮汐与赶海攻略

面向中国大陆沿海的专业赶海查询 Skill：解析"地点 + 时间 + 意向"，输出**可执行的赶海计划**——退潮黄金窗口、大/小潮分析、适用性评分、目标物种挖掘秘籍、出发清单，以及最重要的"安全撤退截止时刻"。

## 何时激活

- 用户提到"赶海 / 去海边挖货 / 退潮 / 低潮 / 潮汐 / 挖蛤蜊 / 抓螃蟹 / 捡海螺 / 抽海肠 / 挖沙虫"
- 用户给出地点（如大连、秦皇岛、青岛、舟山、厦门、北海、三亚等）和时间（如"这周六""明天下午"）
- 用户问"这个点能挖到什么 / 什么时候退潮 / 什么时候适合去赶海"

## 核心原则（每次都必须遵守）

1. **安全撤退时刻置顶**：任何回复顶部第一要素必须是"下一个高潮时间 / 你必须在该时刻前离开滩涂"。绝不因语气活泼而稀释。大潮（朔望大潮/潮差大）给**红色警告**。
2. **诚实原则**：永远给结果，但绝不假装精确。数据源全线不可用时，如实说明"无法提供精确潮时，请以当地潮汐 App 实测为准"，并附农历大潮骨架提示（初一/十五前后为大潮）。
3. **老饕语气**：报告有温度、有经验味，给技巧、时令、吃法；但安全信息保持严肃、无歧义。
4. **地点使用顺序**：内置 11 省热门点库（精确）→ geocoding 兜底 → 就近建议。查不到时优雅降级，不硬答。
5. **评分聚焦潮汐**：适用性评分只看潮汐（潮差 + 低潮时刻是否在白天），天气在报告里展示但不计入评分。
6. **估测数据评分封顶**：未接入真实实测 API 前，天文骨架估算输出的适用性评分强制 ≤ ★★。

## 报告结构（templates/report.md）

```
① ⚠️ 安全撤退截止时刻（最顶，真实数据/红色大潮警告）
② 📍 地点概览（坐标、滩涂类型、到达提示）
③ ⏰ 目标时段：低潮时间 + 黄金窗口（大潮 ±2h / 中潮 ±1.5h / 小潮 ±1h）
④ ⭐ 适用性评分 ★ 分值 + 一句理由（估测源最高 2★）
⑤ 🌊 潮型与大潮指示（农历月相、大/中/小潮）
⑥ 🦀 目标物种（按地点预置/滩涂推断，含方言别名 + 挖掘技巧 + 老饕吃法）
⑦ 🌤️ 天气与风浪（Open-Meteo，仅展示不评分；大风浪≥6级给警示）
⑧ 🎒 出发清单口诀
⑨ 🛡️ 合规与安全提示（休渔/保护区/防陷/防滑，带免责）
⑩ 🧭 可选下一步（换日期/换地点/附近点/导出 iCal 日历）
```

## 工作流

1. **解析输入**：从自然语言提取 `地点、时间、意向`。地点可为精确景点或城市。
2. **定位地点**：
   - 先在 `db/INDEX.md` 查内置热门点（覆盖全国 11 个沿海省市、51 处经典赶海点）。
   - 命中省级文件 → 读取对应数据（`db/<province>.md`）。
   - 未命中 → 调用 `bin/beachcomb.py geocode <地名>` 解析坐标，并在 `db/species.md` 中按滩涂类型推断物种，同时给出同省 1~2 个邻近内置点建议。
   - 内陆城市 → 提醒"该地无海岸线"，推荐最近海岸点（如北京推荐天津东疆港/秦皇岛浅水湾）。
3. **拉取数据**：
   - 潮汐：调用 `python3 bin/beachcomb.py tide <lat> <lon> <date>`（返回高低潮时刻、黄金窗口、昼夜标识及适用性评分）。
   - 天气：调用 `python3 bin/beachcomb.py weather <lat> <lon> <date>`（返回气温、天气现象、风力等级及风浪警报）。
4. **生成报告**：套用 `templates/report.md` 输出结构化 markdown。
5. **日历导出**（若用户需要）：调用 `python3 bin/beachcomb.py ical <date> <low_time> [title] [location]` 输出标准 `.ics` 提醒。

## 潮汐数据降级链路

| 层级 | 来源 | 状态 | 评分上限 |
|------|------|------|----------|
| 1 | 用户提供的潮汐 API key（WorldTides/Storm Glass） | 可选增强，`BEACHCOMB_TIDE_API` | 1~5 ★ |
| 2 | Open-Meteo Marine / 免费海洋波浪源 | 自动联网融合，提供海况与波高警报 | 1~2 ★ (带海洋特征) |
| 3 | 农历半日潮天文骨架 (高潮1/2 + 低潮1/2) | 离线自适应兜底，醒目标注"⚠️ 估算 ±1h" | 强制 ≤ 2 ★ |

## 环境与依赖

- Python 3.10+
- 纯标准库实现（stdlib only），零第三方 pip 依赖。
- 自动化测试与校验：
  ```bash
  python3 -m unittest discover tests   # 运行全量单元测试
  python3 scripts/validate_db.py       # 校验 11 省数据库一致性
  ```

## Non-Goals（v1 明确不做，被问及时坦率说明）

- ❌ 钓鱼预报（钓潮与钓点是另一套指标体系）
- ❌ 专业冲浪 / 潜水深度海况预报
- ❌ 赶海生物拍照/图像识别（需 CV 模型支持）
- ❌ 精细法律核对（休渔期 / 自然保护区红线以当地官方公布为准，本 Skill 仅作合规提示）
- ❌ 本地调和常数绝对毫米级潮位预报（宁给骨架+诚实声明，杜绝虚假精确）

## 免责声明

本 skill 提供辅助信息与经验分享，是一种出行便利工具，不构成法律或安全建议。海况险恶、潮汐变化迅速、当地法规均以官方现场公告为准。**出海赶海请务必：结伴同行、看官方潮汐表、涨潮前提前撤离、远离陌生深淤泥与湿滑险礁。**