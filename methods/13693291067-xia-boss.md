---
name: xia-boss
description: |
  虾集·总指挥（流水线编排 / Orchestrator）专用 skill。基于 DramaClaw 虾系 5 个功能模块——虾料 xialiao-ingest、虾塘 xiatang-characters、虾镜 xiajing-episodes、虾格 xiage-styles、虾导 xiaodao-assistant（已内置本包 modules/）——的交接链设计，把它们各自的 Handoff 机制串成一条"自动推进 + 用户确认门控"的流水线。共享时只需打包本 skill 目录，无需附带其它 skill。
  触发词：虾集总指挥、总指挥、流水线调度、流程编排、orchestrator、conductor、继续下一步、下一步干什么、按流程走、自动调用下一个、接力、handoff 接力、进度总览、现在做到哪一步、串起虾系、虾系流水线、还原流程编排、生产流程编排、草图提示词、草图画风、草图只描述场景、分镜草图、草图动作、草图表情、三层提示词、资产引用、图1图2、参考图编号、全片情绪曲线、情绪曲线、情绪节点、呼吸区、BGM 重点、P1 基线、风格锁定、摄影系统、声音圣经、生产事实、国家年代、人物小传、空间链、威胁规则、视觉母题、依赖门禁、变异程度、识别锚点、剧情锁定状态。
  自包含触发词：打包虾系、共享虾系、虾系打包、自包含、只打包一个 skill、集成虾系、内置模块、modules 目录。
  小说入口触发词：小说项目路径、小说路径、这个小说、整篇小说、小说世界观、梳理世界观、世界观设定、知识图谱、执行第一章、写第一章、单章内容、只有单章、小说改短剧、小说转分镜。
  大纲入口触发词（★ 2026-09-05 新增；★ 二版重排）：战斗大纲、分集大纲、故事框架、大纲直入、先写剧本、虾剧、剧本创作步、写成稿、大纲项目、只有大纲。大纲入口流程（★ **统一剧本先行：剧本创作在第 1 步、虾料之前**）：用户只给大纲/故事框架（无成稿剧本）→ ① **虾剧·剧本创作**（调度内置模块 `modules/xiaju-script/`＝【短剧分集剧本生成器】整个内置：备齐输入[总大纲/人物设定/世界观/每集梗概/前后集衔接/伏笔清单，缺项先向用户要]→五阶门控→按 90s 情绪循环+颗粒度三档写各集成稿→落盘 `outputs/xiaju/ep{NNN}.md`→台词七维→剧本反推资产清单）→ ② **虾料摄入成稿剧本**（梳理世界观/知识图谱/生产事实）→ ③ 虾格基于成稿走三步向导 → ④ 虾塘从剧本反推建资产 → ⑤ 分镜线（虾镜/听风）。核心原则：**先成稿后摄入后分析**（虾料梳理与虾格八项/情绪曲线一次算准免重算），资产以剧本为准不从大纲猜。
  打戏学习触发词：学习打戏、打戏拉片、学习提示词、打戏提示词学习、入库学习、拉片学习、战斗学习库。
  打戏调度触发词（★ 2026-09-07 v3 新增）：打戏调度、打戏外协、战斗段生成提示词、打戏提示词调度、战斗段单独分镜。打戏职责边界（★ v3）：xia-boss 只判定战斗段并组装调度单，打戏提示词设计全部外协内置模块 modules/seedance-combat-prompt/（原封不动挂载，唯一正本）；学习官 modules/battle-learner/ 学习成果以兵器技法专项格式回灌挂载副本。
  适用场景：用户说"把这几个 skill 串起来/编排一下"、"某个 skill 完成后自动调用下一个"、"帮我看看虾系流水线现在到哪一步了"、"继续"（虾系项目语境）、"按流程依次执行这几个模块"。本 skill 提供：编排表（主线 DAG + 支线并行 + 贯穿对话）、运行模式（还原/生产）、运行时循环（读状态→执行→读交接物→简报→确认→调度）、pipeline-state.json 状态契约、用户指令处理（继续/修改/回退/跳过/并行/切虾导）、容错规则、执行纪律。
  小说入口流程（★ 2026-09-05 二版重排·**统一剧本先行**）：用户给小说项目路径 → ① **AI 先从小说自动提炼**分集梗概+人物设定+世界观+前后衔接与伏笔（关键缺项再问用户）→ ② **虾剧·剧本创作**（调度内置模块 `modules/xiaju-script/`：小说→短剧剧本化，按集情绪循环重新组织而非逐章照抄，落盘 `outputs/xiaju/ep{NNN}.md`）→ ③ **虾料摄入成稿**（梳理世界观/知识图谱/生产事实写入 docs/）→ ④ HTML 做知识图谱+世界观 → ⑤ 停下等用户确认 → ⑥ 确认后进入虾格→虾塘→分镜线。用户只给单章内容 → 先询问是否补充世界观设定（补充→按上述流程走；不补充→按单章内容推断补全）→ 剧本创作 → 虾料 → 等确认 → 后续流程。
  职责边界：本 skill 只做"调度 + 汇报 + 确认"，不碰任何业务实现；各模块内部流程由对应模块（modules/ 内）自己负责。
agent_created: true
---

# 虾集·总指挥（流水线编排）

> 把 DramaClaw 虾系 5 个功能模块串成一条自动推进的流水线：每个模块完成后，读取其 handoff 交接物、提炼简报、停下等用户确认，确认后再自动调用下一个模块。

> 📖 **使用者请先读 `使用说明.md`**（面向操作者的简明手册：上手流程 / 项目台功能 / 分镜线选择 / 排错速查）；本文件面向 AI 执行与内部机制。

## 0. 自包含架构（★ 共享打包必读，2026-08-21 用户拍板）

**本 skill 为自包含单包**：编排所需的全部 5 个虾系模块已内置在 `modules/` 目录下，不依赖外部安装的任何同名 skill。

| 模块 | 包内路径 | 角色 |
|---|---|---|
| 虾料 xialiao-ingest | `modules/xialiao-ingest/` | 内容入口：摄入剧本/世界观/知识图谱 |
| **虾剧·剧本创作 xiaju-script（★ 2026-09-05 新增编排位；★ 二版：整个 skill 内置为独立模块）** | **`modules/xiaju-script/`**（内置：【短剧分集剧本生成器】**整个 skill 原样内置、未蒸馏**——含 SKILL.md + 3 references，仅文末追加 xia-boss 交接层） | **剧本创作步（★ 统一剧本先行：位于流程第 1 步、虾料**之前**，§0.8）**：备齐输入（缺项先问）→ 五阶门控 → 90s 情绪循环+颗粒度三档写各集成稿 → 落盘 `outputs/xiaju/ep{NNN}.md` → 台词七维 → 剧本反推资产清单 → 交接虾料（虾料摄入成稿梳理世界观/知识图谱/生产事实）；**已有成稿剧本/小说也必经**——成稿做"规范化改写进虾剧格式"（不 skip，§0.8 规范化规则）。**下游消费的横切补充**：根 `references/shared-script-writing.md`（颗粒度三层贯通/可拍性红线）、`shared-screenplay-gate.md`、`shared-dialogue-doctor.md` |
| 虾格 xiage-styles | `modules/xiage-styles/` | 创作方向确认门：三步向导 |
| 虾塘 xiatang-characters | `modules/xiatang-characters/` | 资产库：角色/场景/道具/声线 |
| 虾镜 xiajing-episodes | `modules/xiajing-episodes/` | 分镜工作台：分镜脚本/制作/故事板 |
| 虾导 xiaodao-assistant | `modules/xiaodao-assistant/` | 对话驱动：任意环节可切换 |
| **场景一致性看守 scene-consistency** | `modules/scene-consistency/` | **横切闸门（★ 2026-08-31 新增）**：虾镜分镜流程强制加载——空间真理图先行 → 从图取景 → 三份可验证产物（原文-场景对照表/场景连续性折线图/矛盾扫描报告）→ 独立空间审核 → 缺资产补建闭环。**做分镜（规划/拆镜/11 字段/交付自检）时必读，保证每镜场景归属正确 + 场景资产完备**；跨镜一致性仍由虾镜 `references/continuity.md` 负责，两者衔接为完整保证 |
| **听风电影 storyboard-cinematic** | `modules/storyboard-cinematic/` | **分镜线之二（★ 2026-09-01 集成）**：电影化分段分镜（戏剧节拍分段 + 五层结构 + 15s 段 + 提示词逐字保真）——与虾镜**二选一**（虾塘确认门分支，见 §0.7 分镜主力线）；项目台模板已内置「🎬 听风电影」工作台（tingfeng 数据链与虾镜完全隔离：独立目录/独立快照/独立拓扑图） |
| **打戏外协 seedance-combat-prompt（★ 2026-09-07 v3 挂载，替代原 fight-scene-director）** | `modules/seedance-combat-prompt/` | **打戏提示词生成器（整包挂载，唯一正本；★ 2026-09-08 内部编号统一为上位原则 P1–P7 + 执行规则 R1–R32，SKILL.md 已加 version 3.0.0）**：Seedance 2.0 二次元打戏 15 秒提示词生成（完播率公式/32 条执行规则 R1–R32 + 上位原则 P1–P7/6 份兵器技法专项/角色与场景引导）——**xia-boss v3 起打戏职责只判断不设计**：分镜线判定战斗段并登记 `battle_segments` → 组装固定格式【打戏调度单】（八字段，含群战人数结构，见 `references/shared-fight-dispatch.md`）→ 调度本模块生成 15s 提示词 → 整段回流战斗段视频提示词字段（战斗段不做五层拆镜、不做 H3 双产）。**打戏学习**：`modules/battle-learner/` 学习成果以《兵器技法资料库_XX专项.md》回灌本模块根目录并登记 weapons.md |
| **打戏学习官 battle-learner（★ 2026-09-07 v3 改造版）** | `modules/battle-learner/` | **打戏学习能力（输入三通道保留：提示词解剖/成片拉片[依赖 ffmpeg]/配对对照）**——输出改为《兵器技法资料库_XX专项.md》七段式（格式对齐 seedance-combat-prompt 现有 6 份专项），写入挂载副本根目录并同步登记 weapons.md 触发条件/路径/定位速查三处；触发词"学习打戏/打戏拉片"。旧 learned/failures 库随 fight-scene-director 归档（桌面 `AI学习资料/xia-boss-archive-0907/`），不再引用 |
| **（★ 2026-09-03 二次集成）前半程创作基准蒸馏** | 分布注入 虾料/虾格/虾塘 `references/`（无独立模块，分布式吸收） | 从前半程创作基准方法论蒸馏 7 点——虾料：`production-facts.md`（生产事实六项+四级标注+空间链表）→ 虾格：`emotion-curve.md`（全片情绪曲线，③基调看图拍板）+ `p1-cinematic-baseline.md`（写实线摄影色彩声音基线）→ 虾塘：`character-dependency.md`（血缘/变异/换装/受伤/年龄依赖门禁）+ 提示词增强字段（骨相/命运气质/锚点/哑光）+ 道具识别锚点与剧情锁定状态 → 下游：scene-consistency 读空间链、听风/虾镜消费 emotion_curve 与 p1_cinematic_baseline（creation-direction.json 契约扩展） |
| **（★ 2026-09-04 集成）分镜双约束横切规范单源 ×2** | `references/shared-spatial-blocking.md` + `references/shared-duration-control.md` | **横切规范（无独立模块，听风/虾镜共用）**：①**空间拓扑可执行规格**——每场一张机位调度拓扑图、四表交付物（空间坐标描述/站位表/机位部署表/轴线与越轴预案表）、CAM1~4 分配算法、三种合法越轴、黑白简笔生图规范（替代旧彩色分区版）——听风 Stage 1 表 2/表 3 与虾镜拆镜的执行依据；②**时长控制可执行规格**——段落路由（文戏/武戏 R1R2R3/奇观）、语速三档公式+金句气口+拆镜法则、镜级节奏铁律、微步进片数即时长、溢出三板斧、两道账+语速自检卡——听风 Stage 2 与虾镜定时长的执行依据。原"每集一张拓扑图分区制"规则废止（旧项目数据向后兼容不动） |
| **（★ 2026-09-04 二次采纳；★ 2026-09-05 扩至 ×4）创作与衔接横切规范单源** | `shared-screenplay-gate.md` + `shared-dialogue-doctor.md` + `shared-transitions.md` + **`shared-script-writing.md`（★ 2026-09-05）** | **横切规范（无独立模块）**：③**剧本五阶门控**——Premise(30字前提公式+黄金3秒钩子四型)→Structure(单集三幕 30/50/20+卡点帧)→Beat(场次存在性法则+五环因果链)→Entity(时代边界+人物关系网)→Script Page(语义化排版) + 去 AI 机械感三法则——**写第一章/改写剧本前强制过门**（落点 §0.6 步骤① 虾剧 / §0.8）；④**台词七维诊断**——辨识度/潜台词层级/冲突推进力/类型语感/信息效率/节奏音乐性(咬合语速公式)/金句潜力 + 三段式重构（≥3 条维度依据）——剧本成稿与分镜台词转写时全量扫描；⑤**镜头衔接三手法六式**——换机位/切景别/跨段反应 × 六式切法 + 视听省略——分镜表"衔接式"字段的本体规范（对齐表见其 §四）；⑥**剧本写作方法论（★ 2026-09-05，短剧分集剧本生成器蒸馏+升级版颗粒度）**——90 秒情绪循环（一集=N 个循环、循环间高一级冲突硬接禁软着陆、循环独立计时）、**颗粒度三档分档可混用**（混合 9 段不等长 3/9/15/15/13/10/10/8/7 默认 / 15 秒档 6×15 与生成段 1:1 / 10 秒档快剪）、情绪六问+换挡判据+三类钩子、可拍性红线（10s 段≈AI 生视频 2~4 镜/台词≤2 短句/VFX 单镜≥2s/超载换宽档）、台词写作规范+AI 味 11 条黑名单+微表情禁用清单、内部五组自检、**颗粒度三层贯通**（剧本段→分镜组→提示词节拍功能，贪心聚合 10~15s 卡 15s 上限，<10s 段并邻）——**写作三部曲**：门控（前）→写作规范（中）→七维（后） |
| **（★ 2026-09-04 三次采纳）质检与投喂横切规范单源 ×3** | `references/shared-quality-gate.md` + `references/shared-feeding-checklist.md` + `references/shared-compliance-lexicon.md` | **横切规范（无独立模块）**：⑥**全片质检门禁 P0~P2 口径总纲**——三档分级定义（P0 阻断/P1 处置/P2 记录；脚本机械项以 check-fidelity 等脚本为准；打戏段外协产物按 `shared-fight-dispatch.md` 回流门禁校验）+ **声音三层电平**（对白 0dB > SFX -6dB > BGM -14~-18dB + Ducking 避让）；⑦**投喂前必查清单（指针序列）**——六步（时长预算/空间机位/镜头规格/对白三查/战斗档语法/出稿质检）每步指向既有规范与脚本，**投喂生成前**过完（与交付自检清单时机分工：交付时 vs 点生成按钮前）；⑧**平台拦截词合规转译词典**——击打受力/面部创伤/致命打击/环境破坏四维高危词→物理转译（保留打击感；不承诺 100% 过审——打戏外协提示词回流后同样过本词典）。⑩光线防假融入 虾格 p1 基线 + 虾塘 scene-assets（无独立文件） |
| **（★ 2026-09-05 采纳）表演层横切规范单源 ×3** | `references/shared-evidence-control.md` + `references/shared-performance-realism.md` + `references/shared-dialogue-performance.md` | **横切规范（无独立模块，来源：影词精磨 skill 蒸馏，通用化改写）**：⑨**认知与证据控制**——角色知识四格账本（已知/新观察/可推断/必须未知）+ 观众-角色信息差三态（悬念/共情/惊奇）+ 反应优先呈现（默认镜头停在观察者，用画外音/视线/物件反应/迟到生理反应证明画外事件）+ 揭示顺序（证据→消化停顿→反应）——发现/揭晓/悬疑/隐藏身份/重逢类段落的前置纪律，防预知脸与重复解释；⑩**活人感表演系统**——**§〇 心理动机单源律（任何场景必守）** + 活人感**五模块**（状态驱动伴随动作/人体生物联动/接触重量/环境响应/摄影光焦点条件一致，按需选用）+ **23 种情绪→微表情映射表** + 减法式微表情三件套（强度控制+动作归因+时间顺序）+ 微表情动作库 + 长特写微表情时间轴 + 超近面部情绪弧三模板（复杂悲伤/羞怯爱意/娇嗔软拒绝）+ 情绪转折速查五式 + 30s 心理阶段时间轴 + **双轨长镜头与波浪式失守（外部任务不变量/三层表情共存/关系视线锚点/未完成表达，§十一附）** + 导演表演链（内在冲突→生理→微表情→贯穿锚点→决定性行为）+ 行为伏笔回收五型 + 非人类角色表演附录 + 单镜提示词语义节拍纪律（通用纪律 §6，与时长控制分镜层时码账分工）——**写一切"人怎么演"的执行依据**（虾镜角色演绎字段/听风 Stage 2/H3 表演段/打戏外协段非战斗表演部分；**★ 2026-09-09 补入 §一减法三件套/§七混合情绪三行/§十速查五式/§十一附双轨长镜头，蒸馏自 emotion-performance-director，重叠处以本规范为准**）；⑪**对白表演时序**——场景级表演契约、声线身份与临时状态锚点（与脸/服装同列连续性锚点）、说话者/听者双表演轨、抢话重叠六要素、保护性口误、台词可播放性审计（字数非判据，先简化运镜最后才删词）、语义剪辑点与 J-cut/L-cut、景别随防御层级递进、情绪保护层四式——**台词"怎么演"的执行依据**。三文件与台词七维/时长控制三分：`shared-dialogue-doctor` 管台词**好坏**、`shared-duration-control` 管台词**长短**、本组管台词**怎么演**；`shared-evidence-control` 管"知道什么"、`shared-performance-realism` 管"知道之后怎么演"。**调用纪律：只在含表演/发现/台词的镜头按需加载，纯空镜与纯动作段不加载**（避免不必要上下文占用） |
| **（★ 2026-09-08 采纳）分镜手艺横切单源** | `references/shared-storyboard-craft.md` | **横切规范（无独立模块，来源：《老白的分镜课》17 集全量蒸馏，用户拍板"重叠处以老白为准"）**：⑫**两条工作流**（机位推画面 / 画面推机位）+ **主镜工作法**（必不可少画面→反推轴向机位）+ **叙事三原则与强调弱化** + **轴向形状体系**（五机位/i/l/a/a2 型/换轴借交集）+ **跳轴三类与对联补救**（与 shared-spatial-blocking 分工：本文件管"何时该跳"，那边管"怎么合法越"）+ **景别六点** + **视高六点（默认一人高）** + **构图四规则**（层次/疏密/带/焦点）+ **视觉焦点与眼线**（焦点惯性衔接/打戏"顺畅底盘+个别跳点"）+ **透视两原则**（地平线等高/视距×开角）+ **空间感公式** + **动势**（同向顺/反向节奏/先色块后 pose）+ **视角（反应+所见）** + **蒙太奇**（并置生新义/基础镜头须中性）——虾镜拆镜 ④/听风 Stage 2 镜头设计的执行依据（storyboard-method.md 词表降为术语库）；交付前过其 §十四自检清单。**★ 2026-09-09 补入**：§五附「情节型景别组接速查八式」（蒸馏自《AI视频做电影感》P11，"渐松渐紧"的整场速查版，默认起点非硬约束；**战斗段不走本表**，让位打戏外协 R31）；同批 P2 构图七法（已由 §七构图四规则 + §九透视 / §十空间感覆盖）、P7 十五种镜头角度（已由 §六视高六点 + §十二视角 POV 及 `shared-camera-movement.md` 覆盖）不纳入，避免另立第二份判断表 |
| **（★ 2026-09-08 采纳）画面语法横切单源** | `references/shared-frame-image-grammar.md` | **横切规范（无独立模块，来源：《电影感镜头构建原理》全量内联蒸馏〔《造梦师》v2.0.0，CC-BY-NC-4.0〕）**：⑬**单帧画面成立规则**——场景母版不变量（SYNTAX MAY CHANGE. SCENE LOGIC MAY NOT.，约束一切提示词形态转换：首帧图→视频/资产图→分镜引用/H3 双产/Seedance 语法适配）/五条成画原理（故事切入/物理目击位置/环境携带证据/观察构图/光合理化）/摄像机决策六步+焦段表/**光构建清单光源地图五项（全介质母法，P1 §6.4 光线防假铁律为其写实线强制裁量，不废）**/风格卡 16 张（十字段）+ 摄影卡 8 张（十二字段）决策系统（服务但不替代虾格三件套）/反 AI 味清理（无因不造+删词仍能成立检验，口径已同步 shared-quality-gate.md）——虾镜拆镜 ④⑤/听风 Stage 2 首帧与视频提示词/虾塘场景空镜图的执行依据；与老白课分工：**老白管镜头间、本源管单帧内**（裁决序见其 §〇）；导演法经拍板不纳入，视觉参考注入以虾格 infusion 契约为准。**★ 2026-09-09 补入**：§四附「六大现成光型速查」（顶/侧/逆/伦勃朗/硬/体积光 + 英文词 + 情绪 + 光源动机常例，蒸馏自《AI视频做电影感》P3）——**只是"叫什么、放什么情绪"的命名选型菜单**，光的合理化仍回 §四光源地图五项并过 `shared-quality-gate.md` 光源动机场，一次只选一型主导光、不覆盖所选卡；同批"全局青橙调色"与"V.I.D.E.O. 提示词框架"经拍板**不纳入**（前者与"色彩由光源材质推导、不做无因全局调色"冲突，后者与首帧 5 段式/STYLE LOCK 重复造框架） |
| **（★ 2026-09-08 采纳）运镜手艺横切单源** | `references/shared-camera-movement.md` | **横切规范（无独立模块，来源：《15 种 AI 影视运镜手法》学习笔记蒸馏）**：⑭**模型中立的运镜判断层**——三轴判别法（机位/镜头/焦距谁在动）、15 手法总表（中文名+动作本质+叙事用途+易混警示，**不锁英文词**）、叙事目的→运镜反推表、易混对比、环绕幅度代价提示（非禁令）、兜底写法（自然动作描述为主、拆秒数为兜底）。**单源纪律：本层只存"概念/选型/辨误/边界"（中文，不锁英文词）+ §八跨模型对照表（英文映射唯一权威，`dolly≡Truck/Push` 在此钉死）**——H3 §3.3（Truck/Push 执行方言）、`seedance-stylock.md`（dolly 执行方言）各留本模型执行拼法并**以 §八 为映射准绳**、听风本地词表已退役，**均不建第二份判断表或自建映射**（根治虾集运镜词多头、dolly/Truck 术语打架）。**★ 只管非战斗段**：战斗段运镜权威=seedance-combat **R31**（禁 360°满圈/螺旋上升、仅对峙蓄力段≤180°半圈），整段抽出不走通用拆镜（`shared-fight-dispatch`），冲突时本层在战斗段整体让位、R31 与打戏模块一字不动。分工裁决：**老白管镜头间、本源管单帧内、本层管单镜怎么动**；应用前置=**先分段(路由)→再选镜**。消费落点：虾镜拆镜④/听风 Stage2 选运镜、辨误读本层 §一~§四；写某模型英文提示词时取 §八 对应列拼法 |

**调用方式（强制）**：本 skill 内部对任何模块的"加载/调用"一律指**用 Read 工具读取 `modules/<模块名>/SKILL.md`**，按其内部流程执行；模块的 `references/`、`scripts/`、`presets/`、`templates/` 一律以 `modules/<模块名>/` 为根相对引用。**禁止通过系统 Skill 工具加载外部同名 skill**（他人环境可能没有安装，且会读到旧版本）。若执行环境中不存在 `modules/`，才允许回退为加载外部同名 skill 并提示用户本包不完整。

**共享打包（★ 用户 2026-08-21 拍板）**：给别人共享虾系流水线时，**只需打包整个 `xia-boss/` 目录**（SKILL.md + modules/ + templates/ + scripts/ + references/），无需附带任何其它 skill。接收方将整个目录放入其 skills 目录即可获得完整流水线；首次运行时按上文调用方式读取模块。

**修改纪律**：对虾系任何模块的修改都在本包 `modules/<模块名>/` 内进行；本包不负责回写外部独立安装的同名 skill（如需同步由用户自行处理）。注意：本 skill 及 modules/、templates/、references/ 中禁止出现任何具体项目的数据（见下方最高纪律）。**版本纪律（★ 2026-09-03 P1-2；★ 2026-09-09 动态化）**：本包版本的**唯一来源是根目录 `VERSION` 文件**——正文、模块、references、模板与 `pipeline-state.json` 一律**不得写死"当前版本号"**（需要引用时运行时读该文件），以免版本递增后正文滞后（曾出现正文写死旧号、实际已连升多档的漂移）。**任何结构性变更（新增/删除模块、契约字段变更、纪律增删、脚本增删）必须递增 VERSION** 并在变更处标 ★ 日期；纯文字修订不必递增。**递增时禁止裸用主版本号**——并发会话可能同时占同一个号，特性用带后缀标签（如 `3.4.4-Xuan酱速查`）命名，改前先 grep 占用防撞。

## 0.1 定位（一句话）

**虾集·总指挥 = 虾系流水线的"调度中枢"：不干活，只负责 读状态 → 调模块 → 读交接物 → 简报等确认 → 调度下一步。**

三条铁律：

1. **不碰业务**：还原/生产的实现逻辑全在各 skill 内部，本 skill 只调用它们、消费它们的产出。
2. **确认门控**：每个模块完成后必须停下汇报、等用户确认，绝不自动连跑。
3. **状态驱动**：一切以 `pipeline-state.json` 为准，中断后从断点续跑。

## ⚠️ 最高纪律：禁止项目数据污染（★ 全流程最高要求，违反即失败）

> **本 skill 及所有模板/脚本/references 中禁止出现任何具体项目的数据**（项目名、角色名、世界观词、示例消息、硬编码的 meta 等）。做项目过程中**禁止偷懒**——不得用复制粘贴/硬编码的方式把某个项目的内容带进 skill 或别的项目。这条的优先级**高于一切流程要求**。

强制规则（逐条执行，缺一即不合格）：

1. **skill 纯净**：SKILL.md / templates/ / scripts/ / references/ 内不得出现任何项目特定词（如具体角色名、世界观专属词、项目路径、示例对话）。示例一律用占位（`角色A`、`<项目名>`）。**锚定示例白名单（★ 2026-09-02 用户拍板）**：`modules/<模块>/references/` 下的锚定示例文件（为质量防复发固化的真实案例片段，如 xiajing-episodes `example-shotlist-36.md`）豁免本条"禁止项目数据"限制；**★ 2026-09-09 追加豁免（以脱敏为硬前提）**：`modules/xiage-styles/styles-library/` 的库条目文件一并豁免本条（该库的设计目的就是跨剧复用风格），但豁免前提是条目入库前已剥离剧名/角色/专有世界观词——验收必须跑 `python scripts/check-style-library.py --project <项目根>`退出码 0（项目词表从该项目 `xiaji.db` 快照动态取，脚本内零硬编码项目词；命中项目词 = P0 阻断入库）。其余位置仍严格执行，污染 grep 验收时对上述白名单目录豁免。
2. **数据只进项目目录**：一切项目数据（世界观/角色/场景/道具/章节/提示词）只允许写入该项目自己的 `docs/`、`outputs/`、`project/xiaji.db(SQLite)`、`chapters/`，**禁止写入 skill**。
3. **生成脚本零硬编码**：`build-data-js.py` 必须从项目自己的 `outputs/` 读取（项目名/风格/角色/场景/道具全部动态），禁止硬编码任何项目字段。
4. **渲染层数据驱动**：`templates/index.html` 的世界观/角色/示例消息必须从 SQLite(`GET /api/data`)读取，禁止硬编码世界观专属词（曾发生：模板残留旧项目世界观词导致他项目世界观混乱）。
5. **复制即清理**：从任意项目/模板复制代码或数据到新项目时，必须全文检查并清除该项目特有的内容（项目名/角色名/世界观词/示例）。
6. **验收自查**：每完成一个模块，用 grep 检查 skill 与项目数据是否串味（`grep -rn "<旧项目关键词>" <skill目录>` 应为空）。发现即清理并复盘根因。
7. **不做"看起来能用就行"**：宁可多写一次从数据源读取的逻辑，也不允许为省事把别的项目的数据硬编码进来。偷懒造成的污染 = 最高级别事故。
8. **模板修改铁律（★ 用户 2026-08-19 拍板固化，任何情况任何时间适用）**：**修改 `templates/`、`scripts/` 等模板内容的任何时刻，都必须保证模板的通用性，绝对禁止数据污染**——模板是跨项目通用的公共内容，不是某个项目的私有文件。
   - **修改前**：明确本次改动是"通用修复/增强"还是"项目私有逻辑"——项目私有逻辑一律不写进模板，只放项目目录；模板只接受通用逻辑。
   - **修改中**：禁止偷懒式复制——不得把项目副本（含剧名/角色名/世界观词/示例数据）直接覆盖到模板，不得"顺手"把当前项目的任何数据带进模板；示例一律用占位（`角色A`、`{{项目名}}`、`<项目名>`）。
   - **双向复制纪律（★ 第 39/40 条事故固化）**：
     - 模板 → 项目：复制后必须把 `{{项目名}}` 替换为本剧剧名；
     - 项目 → 模板：复制后必须把剧名还原为 `{{项目名}}` 占位符；
     - 两个方向复制后都必须 grep 零残留才算完成。
   - **修改后**：立即验收自查——① 当前项目词 grep 应为空（剧名/角色名/地点/世界观词）；② 旧项目词 grep 应为空；③ `{{项目名}}` 占位符分布完整（index.html 3 处：title/侧栏项目名/顶部大标题 + js/gen.js 1 处 JS 标题兜底 + server.py 3 处 + 启动项目台.bat 1 处【bat 内用 ASCII 占位符 `{{TITLE}}`，替换时一并处理】 = 共 8 处功能位【grep 计数会多出注释提及：js/gen.js 注释 1 处 + bat REM 注释 1 处，验收以功能位为准】）；④ 删除 `__pycache__` 等编译缓存（pyc 会藏字符串字面量）；⑤ 确认模板仍可被新项目直接使用（复制→替换占位符→grep 验证→可用）；⑥ **模板 JS/CSS 有改动时递增 index.html 的 `?v=` 版本号**（如 `js/core.js?v=20260821`——浏览器缓存旧 JS 会报新增函数 not defined，imgSrc 事故教训）；⑦ **模板 bat（启动项目台.bat）必须纯 ASCII 写回（去除所有中文注释/提示，占位符用 `{{TITLE}}` 这种 ASCII 形式，禁止中文"项目名"三字或 `chcp 936` 混合）**（★ 2026-08-24 实战教训：Windows 11 默认启用"全局 UTF-8"代码页时，`chcp 936` + 中文注释会令后续 UTF-8 中文字节错位、整批命令被当成外部命令报"系统找不到指定的路径"；纯 ASCII bat 在任何代码页下都安全）；⑧ **bat/ps1 换行符必须 CRLF（★ 2026-09-04 实战教训·实战教训）**：cmd.exe 对 LF-only 的 bat 会行边界崩坏——整批命令被拆成单词执行（报 `'xxx' 不是内部或外部命令`，xxx 为各行首单词），且模板源头 bat/ps1 曾全量 LF-only（每个新项目复制即坏）。**根因与修法**：用 Python 文本模式（默认 universal newlines）读写做占位符替换会把 CRLF 吃成 LF——对 .bat/.ps1 必须用**二进制读写字节级替换**（`open(f,'rb')` → replace → `open(f,'wb')`），写后验证 `CRLF 计数=行数、LF-only=0、非 ASCII=0` 三项达标；项目实例中 bat 的 `{{TITLE}}` 替换为中文剧名会在 GBK 代码页乱码（不致命但难看），稳妥做法是替换为 ASCII 形式（项目 ID/拼音/英文），中文剧名由 index.html 与 server.py 展示。另提供 `启动项目台.ps1`（PowerShell 版）作保底，彻底绕过 cmd 编码问题，用户右键"运行 PowerShell"即可。
   - **违反后果**：模板带项目数据 = 每个新项目都会继承污染（剧名串台/世界观混乱），属于最高级别事故；发现即全文清理 + 复盘根因 + 在 platform-fixes.md 记录。

---

## 0.5 权威流程：产品手册第四章「第一次跑通一集」最短路径（★ 记死）

> 来源：DramaClaw 产品使用手册（开源版）第四章。这是"小白跑通一集"的**端到端最短路径**，也是本流水线编排的**权威依据**——一切调度都对照这张表。

**定位**：最短路径 = **先跑通闭环，再逐步提高画面质量**。第一次不需要每个镜头做到完美，只要「剧情能看懂 + 角色不明显混乱 + 音视频能合成导出」就算成功。**先完成闭环，再回头精修每一步。**

### 7 步主流程（顺序锁死，不可跳）

> ★ 2026-09-05 编排调整（用户拍板，VERSION 2.7.0 初版；**★ 二版重排 VERSION 2.8.0**）：**大纲入口**（用户只给大纲/故事框架，无成稿剧本）「虾剧·剧本创作」步**从"虾料之后"前移到"虾料之前"，成为流程第 1 步**——先写成稿剧本，虾料再摄入成稿梳理世界观/知识图谱/生产事实（不从大纲猜），虾格基于成稿做八项分析（一次算准，成稿后免重算情绪曲线），见 §0.8。**小说入口同步统一剧本先行**（先剧本化再摄入）。**所有入口（含已有成稿剧本）的第 1 步都是虾剧**：小说/单章先"剧本化"，已有成稿也必经虾剧做"规范化改写进虾剧格式"（不 skip，§0.8 规范化规则），随后虾料一律摄入成稿。

| 步 | 模块 | 动作 | 对应本流水线 |
|---|---|---|---|
| 1 | 项目管理 | 创建项目 | 项目初始化（pipeline-state.json）|
| 1.5 | **虾剧·剧本创作（★ 大纲/小说/成稿入口一律必经，位于虾料**之前**）** | 调度内置模块 `modules/xiaju-script/`（【短剧分集剧本生成器】整个内置）：备齐输入（缺项先问）→ 五阶门控 → 90s 情绪循环+颗粒度三档写各集成稿 → 台词七维 → 落盘 `outputs/xiaju/ep{NNN}.md` → 反推资产清单；**已有成稿剧本/小说也必经**——成稿做"规范化改写进虾剧格式"（不 skip） | xiaju-script（§0.8 规范化规则） |
| 2 | 虾料 | **摄入成稿剧本**（`outputs/xiaju/` 或用户提供）、检查无误、导入；梳理世界观与知识图谱 | xialiao-ingest |
| 3 | 虾格 | **确认创作方向（三步向导）**：①风格类型（6 预设+自定义扩展）→ ②视觉参考（3-5 组现有影视作品）→ ③影片基调（3-5 种），均必选、全部拍板后才进入虾塘 | xiage-styles |
| 4 | 虾塘 | 建立资产：角色/场景/道具/声线 | xiatang-characters |
| 5 | 虾镜 | 规划剧集：为目标集规划分镜（shots） | xiajing-episodes（plan）**→ 必过 scene-consistency 闸门①空间真理图+补建清单** |
| 6 | 虾镜·分镜脚本 | 分镜列表：镜号/时长/景别/画面/台词/构图方式（编辑/拖动重排/增删/重置） | xiajing-episodes（分镜脚本）**→ 从空间真理图取景** |
| 7 | 虾镜·制作 | 逐镜产出首帧/尾帧/视频（左 11 字段分镜 + 右产物区，副本机制） | xiajing-episodes（制作）|
| 8 | 虾镜·故事板 | 勾选分镜生成 6/9 格故事板（一格一 beat，优先 9 格其次 6 格） | xiajing-episodes（故事板）|

> ★ 2026-08-31：虾镜规划/分镜脚本阶段强制加载 `modules/scene-consistency/SKILL.md`（场景一致性闸门）——空间真理图 → 三产物 → 独立审核，交付缺任一产物视为分镜未完成。

### 铁律（顺序提醒，原句记死）

> **"前面的文案、角色、场景或首帧错了，后面的视频通常也会跟着错。不要一开始就盲目生成视频，先把资产和脚本检查好。"**
> **"风格在资产之前定：先确认视觉风格，再建角色/场景资产——风格错了，所有资产图都要重做。"**
> **"虾集已内置所有所需内容：制作过程中绝对不能调用除虾集自身外的任何 skill 内容。"**

含义：
1. 文案/风格/资产/首帧是**质量闸门**，越靠前越致命；先检查好再往下走。
2. 制作门控（★ 2026-08-21 重构）：单镜头视频生成前必须有首帧（firstframe_prompt/firstframe_image）与 video_prompt；音频/声线按虾塘声线就绪门控；底层方法论（首帧 5 段式 / STYLE LOCK / 锚点 / 色调总表 / 音频双轨）见虾镜模块 §6 执行纪律。
3. 步骤 5「规划剧集」= 为目标集规划**身份、场景、道具**——identity_plan / scenes_plan / props_plan 三件套，不是只做身份。
4. **虾格（步骤 3）= 创作方向确认门**：必须在虾塘建资产前完成，用户通过三步向导拍板三件套（①风格类型 → ②视觉参考 → ③影片基调，均必选）后才进入虾塘。
5. **自包含铁律（★ 2026-08-22 用户拍板，最高纪律）**：本包（xia-boss + modules/ + references/）已内置所有所需内容（如分镜方法论 storyboard-method.md、各模块纪律/脚本/模板），**制作与调度过程中绝对禁止调用除虾集自身外的任何 skill 内容**（包括 02-storyboard-master、剧本转分镜等外部分镜/生图 skill）——需求一律由本包内置内容满足，外部调用视为流程违规。
6. **视频生成执行分层（★ 2026-09-04 实证）**：提示词≠成品，模型执行有方差。控制力阶梯：L0 纯文生（试水）→ L1 强约束+同条跑 3 选 1（默认）→ L2 拆 5s 分段 → L3 首帧图生视频（同条崩两次必升）。时长=该段设计时长勿超长（10s 设计段拉长到 12s 为崩卡强相关变量，单组对照实证）；新任务类型先跑**探针轮**（第一条生成目的是暴露失败模式，不是出成品）。战斗段按 `references/shared-fight-dispatch.md` 外协调度（seedance-combat-prompt 产出整段回流，控制力阶梯 L0-L3 沿用其 SKILL.md 内规范）。

### 与协作约定的交叉（AI 只产提示词时）

- 在"AI 不生成图片但产出所有图片的提示词"约定下，步骤 3/6 的**提示词层**由 AI 全量交付（资产定妆/草图/首帧/视频提示词逐项给全），**执行生成归用户**；步骤 7 合成由用户或工具执行。
- **"执行生成归用户" ≠ "提示词可以省略"**——AI 交付时必须给全，不得等用户来问（虾镜模块 §6 执行纪律同步）。
- **身份图提示词同样必须全量交付**（教训固化）：每个角色除定妆照提示词外，**每个身份图（identities）必须配套独立提示词**（与定妆照同一面容、仅变服装/场景/气质）；只给图位占位 = 未完成（虾塘模块 身份域验收清单同步）。

### 开新剧时 HTML 项目台初始化（★ 必做）

**每次开始一个新剧本/新项目（第 1 步·创建项目之后），必须先初始化 HTML 项目管理台**，之后每个模块产出都写回它：
1. 创建 `project/` 目录结构：index.html + server.py + 启动项目台.bat + assets/（characters/scenes/props/ep{NNN}）；数据存项目根 `xiaji.db`(SQLite)，不再生成 data.js
2. 复制本 skill 自带模板（`templates/` 下的 index.html 模板、server.py、启动项目台.bat、启动项目台.ps1 与 build-data-js.py）；`.ps1` 为 PowerShell 保底启动器（bat 双击异常时使用）
3. **★ 替换剧名占位符（强制）**：模板中所有 `{{项目名}}` 必须替换为**本剧的正式剧名**（如《示例剧名》）——出现在 index.html 的 `<title>`/侧栏项目名/顶部大标题/JS 标题兜底、server.py 的文件头注释与 `/api/info` 返回与启动打印、启动项目台.bat 的提示文字；同时把 SQLite `meta.name` 设为剧名（而非 project_id，否则前端标题显示英文 ID）。grep `{{项目名}}` 应为空才算替换完成。
4. 首次跑 build-data-js.py 自动建 `xiaji.db`（meta 填项目名/风格/模式，其余模块随产出写入）
5. 此后**每完成一个模块（虾料/虾剧/虾格/虾塘/分镜线）都必须同步项目台**（★ 强制，2026-09-05 事故固化）：跑 `scripts/build-data-js.py <项目根>` 重建 SQLite(`xiaji.db`)——这是唯一数据源，前端经 `GET /api/data` 读取；重建后**必须校验本模块数据已入库**（查 SQLite 或 API），并确认项目台服务存活（`curl /api/data`，服务未起则后台启动并告知端口）。**未完成三步验收不得汇报模块完成**。
   - **★ 虾格预设自动同步**：build-data-js.py 每次运行会自动把虾格模块 `presets/`（6 预设 + 已固化自定义的 风格 json + 预览图 png）同步到项目（`outputs/styles/` + `project/assets/styles/`，仅缺失时复制）——**新项目初始化后自动带齐风格库与预览图**，无需手动搬运。
   - **★ 风格解析权威链（★ 2026-09-05 修复）**：当前风格解析顺序 = ① pipeline-state.json 的 xiage-styles.handoff 指向 styles JSON → ② **`outputs/creation-direction.json` 的 `style_id`（虾格三步向导拍板结果，权威）** → ③ styles 目录按文件名兜底。兜底仅是最后防线——已拍板项目若显示错误风格=同步事故，立即重跑构建脚本修复。
6. 简报中必须给出 HTML 项目路径（`project/index.html`）与双模式说明（静态浏览 / 双击启动项目台.bat 可上传）

### 本集生产应停留的位置

- 每次调度后，简报中必须说明**当前处于 7 步中的哪一步**（如"第 5 步·脚本"），让用户时刻知道自己在最短路径上的位置。

---

## 0.6 小说项目入口流程（★ 用户给小说路径/单章时，按此走）

> 适用：用户提供**整篇小说项目路径**（如 `C:\Users\123\Desktop\小说创作\既有项目`）或**单章内容**。**统一剧本先行（★ 与 §0.5/§0.8 一致）**：本入口第 1 步是**虾剧**——先把小说/单章改写成剧本成稿，虾料再**摄入成稿剧本**（非原文）梳理世界观/知识图谱/生产事实，之后虾格→虾塘→分镜线，全程受确认门控；HTML 项目台先展示知识图谱与世界观，确认后才进入风格。

### 0.6.1 输入识别

- **整篇小说项目路径**：目录下通常有 `docs/`（设定库）、`chapters/`（正文）、`project/`（HTML 项目台）等 → 走 **流程 A**。
- **仅单章内容**（用户只贴了一章，无项目目录）→ 走 **流程 B**。

### 0.6.2 流程 A：有完整小说项目路径

```
① 虾剧·小说→剧本化（★ 第 1 步，先于虾料）  读 modules/xiaju-script/SKILL.md 执行：
               · AI 先从小说提炼 分集梗概/人物设定/世界观/前后衔接/伏笔（关键缺项先向用户要，不凭空编造）
               · 先过剧本五阶门控（references/shared-screenplay-gate.md：Gate1 前提卡…Gate4 边界卡，产出随确认材料交用户拍板）
               · 按 90s 情绪循环+颗粒度三档把小说"剧本化"（按集情绪循环重组，非逐章照抄），按新剧本格式（独立"场景："头 + 六字段秒段）落盘 outputs/xiaju/ep{NNN}.md
               · 成稿跑台词七维扫描（shared-dialogue-doctor.md）→ Gate5 排版自查
② 执行虾料     读 modules/xialiao-ingest/SKILL.md 执行，**摄入上一步的成稿剧本（outputs/xiaju/，非小说原文）**：
               · 通读成稿 → 格式检查、**场景头解析（须命中每场景的独立"场景：…"行，否则 blocking missing_scene_headers）**、章节列表、场景块、人物、事件
               · 梳理世界观和各项设定 → 写入 docs/（世界观/人物卡/主题/风格/结构/圣经等）
               · 生成知识图谱数据（角色/地点/势力/物品/概念 + 关系）→ 写入 SQLite(xiaji.db) 的 xialiao.knowledge_graph
③ HTML 先做    初始化/更新 HTML 项目台，本阶段只做两块：
   知识图谱+世界观 · 知识图谱（暗色科技风、可拖拽/缩放/点击，节点=角色/地点/势力/物品/概念，边=关系）
               · 世界观（三界格局/力量体系/核心冲突/势力表/作物体系等设定展示区）
               · 其他 Tab（虾塘资产/虾镜分镜）可留空壳占位，不展开做
④ 确认门控     向用户简报：世界观设定清单 + 知识图谱节点/关系数 + HTML 路径
               → 停下等确认（知识图谱/世界观确认通过后，才进入风格确认）
⑤ 虾格确认创作方向 ★加载 xiage-styles：先做剧本理解分析（八项，不输出大段分析），再走三步向导
   创作方向确认门     ①风格类型（6 预设+自定义扩展，3-5 候选+推荐）→ ②视觉参考（3-5 组现有影视作品，
               基于①推荐）→ ③影片基调（3-5 种，基于①②+核心矛盾与情绪曲线推荐）；三件套均必选、每步
               AskUserQuestion 拍板（推荐项排第一）；新风格可经图片反推/指定名称扩展，固化虾格模块 presets/
               → 三件套全部拍板后才进入下一步（严禁跳过/擅自代选/省略必选维度；题材时代感校验见 §1.2）
⑥ 进入制作     三件套拍板后 → 虾塘从**成稿剧本**反推建资产（角色/场景/道具/声线）→ 分镜线（虾镜 / 听风，
               战斗段走外协，见 §0.7）
```

### 0.6.3 流程 B：只有单章内容

```
① 询问补充     先问用户是否补充世界观设定：
   ├─ 补充 → 用户给设定/大纲，并入下一步虾剧的输入
   └─ 不补充 → 由虾剧从单章提取明确信息，对缺失设定（三界格局/力量体系/历史背景等）合理推断、标注【推断】随成稿带入
② 虾剧·单章→剧本化（★ 第 1 步，先于虾料）  同流程 A ①：先过剧本五阶门控（shared-screenplay-gate.md）→ 按 90s 情绪循环
               把单章改写/续写/扩写成**合规虾剧格式**成稿（独立"场景："头 + 六字段秒段），成稿跑台词七维（shared-dialogue-doctor.md）→ Gate5；
               若该章已是剧本，也**不跳过**，做"规范化改写进虾剧格式"（见 §0.8 规范化规则）
③ 执行虾料     同流程 A ②：摄入上一步成稿剧本（非原文）→ 建 docs/世界观 + 知识图谱
④ HTML 先做    同流程 A ③：知识图谱 + 世界观
⑤ 确认门控     同流程 A ④：简报 + 等确认
⑥ 虾格确认创作方向 → 进入制作  同流程 A ⑤ → ⑥（三步向导 → 虾塘 → 分镜线）
```

### 0.6.4 本入口的写回与简报要求

- **docs/**：世界观与各项设定必须落到 docs/ 文件（按 DramaClaw 设定库结构：01-seed、02-world、03-characters、04-theme、05-style、06-structure、08-bible 等）。
- **知识图谱数据源**：必须从 docs/ 设定生成，禁止凭印象创作；节点/关系与 docs 一致（本项目教训：曾凭印象生成导致角色错乱，需用户指正）。
- **HTML 先做范围**：知识图谱 + 世界观两块做完即可停下；虾塘/虾镜等模块不在本入口展开。
- **简报必须包含**：① docs/ 产出了哪些设定文件；② 知识图谱节点数/关系数/覆盖类型；③ HTML 项目路径与双模式；④ 当前所处流程步（A 或 B 的第几步，且注明虾剧成稿已产出在 `outputs/xiaju/`）；⑤ 请确认进入虾格创作方向（剧本化已在入口第 1 步完成）。
- **pipeline-state.json**：入口先记 `xiaju-script`（剧本化/规范化）→ 完成后 `xialiao-ingest`；本入口的确认门位于"虾料摄入成稿 + 图谱/世界观"之后、虾格之前。
- **资产正面统一（跨模块强制）**：任何环节交付资产类图片提示词（角色定妆/身份图/道具参考图）时，**输出视角必须统一正面**——角色定妆=正面全身/半身面向镜头；身份图=**两图制（定妆照 + 四视图角色卡，2026-08-21 拍板）**，两图均带 raw photo 质感段；道具=**五宫格（★ 2026-08-21 用户拍板：正面/背面/左侧面/右侧面/材质细节特写，上 2 下 3）**，提示词固定「产品特写」写法（英文风格段 + 「产品特写，」中文描述段 + FORBIDDEN 负面段，详见虾塘模块 `references/prop-five-grid.md`）。视角规范全文见虾塘模块 `references/character-assets.md`，各模块（虾料/虾镜/虾格/虾导）已同步引用；总指挥在验收与简报时检查资产提示词是否含显式正面视角词。

---

## 0.7 分镜主力线（★ 用户可配置，AI 开工必读，2026-09-01 新增）

- 项目台「⚙ 设置 → 🧭 分镜主力线」可配置 **听风电影（tingfeng）/ 虾镜（xiajing）**，持久化在项目配置文件（img_config.json 的 `storyboard_main_line` 字段，默认 tingfeng）。
- **AI 为某集做分镜前必读该设置**：`storyboard_main_line = tingfeng` → 只走 storyboard-cinematic（听风线）；`= xiajing` → 只走虾镜拆镜流程。**禁止两线都做**（双线是重复劳动，除非用户显式要求另一线也做）。
- AI 读取方式：直接读项目配置文件该字段（不依赖前端）；用户口头指定走哪条线时，口头指令优先于设置。
- **基调吸收横切（★ 2026-09-02 用户拍板；★ 2026-09-03 P0-2 单源化）**：无论本集走哪条线，分镜开工前均须执行**影片基调吸收**——全规范单源 `references/shared-tone-absorption.md`（输入契约含 tonal_direction + 奇观/克制清单 + emotion_curve 情绪段对齐 + p1 基线 + production_facts），节拍/段落标注情绪段与基调轨，镜头语言按轨推导，交付基调追踪表；两线差异点对照见该文件 §7（听风线落点 storyboard-cinematic §1.8；虾镜线落点 xiajing-episodes 纪律 16）。
- **时长控制与空间调度横切（★ 2026-09-04 新增）**：无论本集走哪条线，拆镜/排镜时均须执行——**时长**单源 `references/shared-duration-control.md`（段落路由决定估时公式；台词过语速公式、金句气口 0.3~0.8s 写进时间轴；时码首尾相接零缝隙；交付含语速自检卡与两道账 Σ镜=组、Σ组=总预算）；**空间**单源 `references/shared-spatial-blocking.md`（**每场一张**机位调度拓扑图四件套：空间坐标描述/站位表/机位部署表/轴线与越轴预案表；每镜机位归入 CAM1~4；越轴预写三种合法手段之一；拓扑图生图一律黑白简笔版，旧彩色分区版废止）。三横切并行不冲突：基调管"镜头语言的情绪倾向"，时长管"每镜多长"，空间管"所有东西在哪、机位在哪侧"。
- **打戏外协横切（★ 2026-09-07 v3，替代原 fight 挂载）**：分镜线分段中出现战斗段（打斗/对决/追杀/突围/守护/群战，或用户点名）时——**判定归 xia-boss，设计归外协**：Stage 1 判定并登记 `battle_segments` → 战斗段**整段抽出独立成段，禁止与文戏混段分镜**（不做五层拆镜、不做 H3）→ 按 `references/shared-fight-dispatch.md` 组装【打戏调度单】（八字段，含群战人数结构）→ Read `modules/seedance-combat-prompt/SKILL.md` 按其工作流生成 15s 提示词 → 整段回流该战斗段视频提示词字段（负面四禁并入、合规转译后投喂）；听风/虾镜线统一适用本协议（原听风 §1.10 beat 链转写流程废止，改指本协议）。
- **听风交付硬门禁（★ 2026-09-03 P0-1；★ 2026-09-06 扩至 11 项）**：听风线每集交付前必须跑 `scripts/check-fidelity.py <项目根> --ep N` 退出码 0——①保真 ②段规则/②路由 ③引用行（含按段裁剪）④H3 契约 ⑤一致性 ⑥拓扑图场一致性 ⑦战斗段（清单未登记即报错，禁止静默跳过）⑧镜头规格（camera 四段式）⑨执行块 ⑩母题锚点 ⑪合规扫描；不过 = 该集交付未完成；**虾镜线剧本保真门禁（★ 2026-09-09）**：交付前必须跑 `scripts/check-script-fidelity.py <项目根> --ep N` 退出码 0（A 剧本秒段覆盖 / B 镜头出处合法 / C 台词双向逐字），场景归属仍走 scene-consistency 三产物 + 人工。
  - **★ 门禁自检纪律（2026-09-06）**：改动脚本后必须用真实项目数据复跑，确认**报错数确实变化**（新增检出或误报消失）——报错数纹丝不动，说明门禁仍在空转或没被触发（历史事故：字段名不一致导致该检查从未执行）。
  - **★ 项目本地生成脚本必须串联门禁（★ 2026-09-06b 硬条款，堵传导断点）**：项目里的本地生成脚本（`gen_tingfeng_*.py` 等自造轮子）**不读 skill 模板文档**——文档改得再好，脚本不执行等于没改（历史事故：某项目自造的 `gen_tingfeng_ep001_v2.py` 只抄了文档名词、零契约实现，224 条问题出厂）。铁律：**任何项目生成脚本跑完，最后一步必须执行 `scripts/postprocess_tingfeng.py <项目根> --ep N`（自动跑契约修补 + check-fidelity 复验），退出码 0 才算交付**；只跑生成脚本不过后处理 = 提示词未完成，禁止直接投喂。契约执行件是 `scripts/postprocess_tingfeng.py`（剥引用包裹/按段裁剪+集级图号/母题进词/[执行约束]块/[负面]四禁/高危词转译/route 写入/H3 六段重建/seedance 同步），内容设计项（重拆镜、camera 四段式）经 `--camera-map` 设计映射注入，脚本不做机械拆分。
- **虾塘确认门的分镜线分支（★ 2026-09-01 用户拍板，强制）**：虾塘完成后、进入分镜阶段的确认门，AI 必须**显式询问用户本集走哪条线**——AskUserQuestion 二选一：「🎬 听风电影」「🎬 虾镜」，**推荐项 = 设置里的 `storyboard_main_line`，排第一**；用户拍板后才进入对应分镜流程。**禁止默认静默走某条线**（设置只决定推荐项，不代替用户拍板）；用户拍板结果可顺手写回设置；下一集确认门重新询问（随时可换线）。

## 0.8 大纲入口流程（★ 用户只给大纲/故事框架时，按此走；2026-09-05 新增 VERSION 2.7.0；★ 二版重排 VERSION 2.8.0：剧本创作前移到虾料之前，统一剧本先行）

> 适用：用户提供**战斗大纲/故事框架/梗概**（如 5 集战斗排布、分集大纲），**无成稿剧本**。核心原则（★ 二版重排）：**统一剧本先行——虾剧（剧本创作）在虾料之前，是流程第 1 步**。用户提供大纲/人物设定/世界观/每集梗概（缺项先问），先把 5 集成稿写出来，**虾料再摄入成稿**梳理世界观/知识图谱/生产事实（不从大纲猜），虾格基于成稿做八项分析（一次算准：情绪曲线/奇观克制清单不因剧本成稿而重算）。
>
> **与小说入口（§0.6）的边界**：**两个入口已统一剧本先行**——§0.6 小说入口二版重排为「AI 先从小说提炼梗概/人物/世界观 → 剧本创作 → 虾料摄入成稿」（见 §0.6）；两入口的区别只在剧本创作的输入来源（大纲直接给 / 小说由 AI 提炼），后续链路一致。
>
> **依赖链（★ 已翻转）**：**虾剧 → 虾料 → 虾格 → 虾塘 → 分镜线**。剧本创作的输入（总大纲/人物设定/世界观/每集梗概/前后集衔接/伏笔清单）由**用户提供**，**缺项先向用户要、不凭空编造**（见内置模块输入契约）；Gate4 边界卡取用户提供的世界观/大纲设定；**虾料后续摄入成稿再校验与细化**（生产事实六项/空间链/人物卡以成稿为准）。风格三要素对剧本文本零影响，剧本动笔不需要等风格拍板。

### 0.8.1 流程步骤

```
① 定位项目根   用户给大纲 → 创建项目骨架（pipeline-state.json + HTML 项目台，§1.4）
② 虾剧·剧本创作（current: xiaju-script）——**调度内置模块 `modules/xiaju-script/`**（【短剧分集剧本生成器】整个内置）：
   a. 备齐输入      用户提供的总大纲/人物设定/世界观/每集梗概/前后集衔接/伏笔清单——**缺项先向用户要，不凭空编造**（内置模块输入契约）
   b. 剧本五阶门控  读 references/shared-screenplay-gate.md——Gate1 前提卡（30 字公式+黄金 3 秒钩子）
                    / Gate2 结构卡（三幕比例+卡点帧）/ Gate3 节拍表（删场测试+五环因果链）
                    / Gate4 边界卡（取**用户提供的世界观/大纲设定**）——Gate1~4 产出随确认材料交用户拍板
   c. 写正文       按内置模块方法论执行（90s 情绪循环 + 颗粒度三档 + 锁定铁律 + 内部自检）→
                   逐集写：outputs/xiaju/ep{NNN}.md（+ chapters/ch{NNN}.md 如需）→ 同步 SQLite；
                   横切补充供参考：shared-script-writing.md（可拍性红线/台词落笔规范/颗粒度贯通下游换算）
   d. 台词七维扫描 读 references/shared-dialogue-doctor.md，全量扫描，三段式重构 → Gate5 排版自查
   e. 资产清单交接 从成稿剧本反推角色/场景/道具清单（含群众角色）写入 handoff——
                   **虾塘资产清单以剧本为准，不从大纲猜**（防"提案角色 vs 剧本角色"对不上）
   → 简报停等确认（每集可逐集确认，也可 5 集一次交付；用户拍板节奏）
③ 执行虾料     读 modules/xialiao-ingest/SKILL.md：**摄入成稿剧本**（outputs/xiaju/）→ 梳理世界观/人物卡/
               生产事实六项/空间链 → 知识图谱 → 同步项目台 → 简报停等确认（确认世界观/知识图谱/提案项）
④ 虾格确认创作方向   基于成稿剧本走八项分析 → 三步向导（①风格 → ②视觉参考 → ③基调，均必选）
                     → P1 深化（写实线）→ FS 库 → handoff-xiage.md
⑤ 虾塘建资产   从 handoff 的剧本资产清单建角色/场景/道具 → 分镜线选择门（§0.7）
⑥ 分镜         虾镜或听风，按 §0.7 执行
```

### 0.8.2 本入口的写回与简报要求

- **pipeline-state.json**：modules 登记 `"xiaju-script": {"status": ..., "handoff": "handoff-xiaju.md", ...}`；current 依次推进 `xiaju-script → confirm-xiaju → xialiao-ingest（摄入成稿）→ confirm-xialiao → xiage-styles → ...`（★ 剧本先行）。
- **handoff-xiaju.md**（虾剧交接物）：元信息 / 验收摘要（五阶门控各 Gate 状态、台词七维扫描结果）/ 数据契约（episodes 清单：集号/标题/字数/节拍档位；characters/scenes/props 反推清单；emotion_curve 重算标记）/ 产物文件清单 / 下一步（交给虾格）。
- **简报必须包含**：① Gate1~4 拍板材料（首次）；② 每集字数/节拍档位/单集时长核算；③ 剧本反推的资产清单数；④ HTML 项目路径（outputs/xiaju/ 可在项目台预览）；⑤ 请确认进入虾格。
- **项目台同步（★ 强制，纪律 20）**：虾剧成稿落盘后必须完成 §3 ⑦ 三步验收（重建 SQLite → 校验 `xiaju.episodes` 数=成稿集数 → 确认服务存活给 URL），简报的「项目台」行如实填写；未同步不得汇报完成。
- **虾格曲线消费规则**：大纲阶段若已粗算情绪曲线，剧本成稿后节拍实质变化必须重算并随③基调重新展示；三件套已拍板的不重新拍板（除非用户主动要求改）。

## 1. 编排表（内建 DAG）

### 1.1 主线（严格顺序）

> ★ 2026-09-05 编排调整（用户拍板，VERSION 2.7.0；★ 二版/0908 再调）：**xiaju-script（虾剧·剧本创作）是主线第 1 步、在虾料之前（统一剧本先行）**——所有入口先成稿、虾料再摄入成稿分析（§0.8）；**已有成稿剧本/小说也必经**该步，做"规范化改写进虾剧格式"（不 skip，§0.8 规范化规则）。

| 序 | skill | 角色 | 前置条件 | 交接物 |
|---|---|---|---|---|
| 1 | **xiaju-script 虾剧·剧本（★ 全入口必经·剧本先行，§0.8 规范化规则）** | 剧本创作 / 成稿规范化改写 | 有输入文本（大纲/小说/单章/已有成稿，缺项先问用户） | handoff-xiaju.md（含剧本反推资产清单）|
| 1.5 | xialiao-ingest 虾料 | 摄入**成稿剧本**·内容入口 | 虾剧成稿 `outputs/xiaju/` | handoff-xialiao-to-xiatang.md |
| 2 | xiage-styles 虾格 | 创作方向确认门 | 虾剧成稿（已有剧本经规范化改写后亦视为成稿） | handoff-xiage.md |
| 2.5 | xiatang-characters 虾塘 | 资产库 | 虾格创作方向确认 completed；**资产清单以剧本反推为准** | handoff-xiatang-to-xiajing.md |
| 3 | **分镜工作台（★ 虾塘确认门二选一，§0.7 分支）**：xiajing-episodes 虾镜 **或** storyboard-cinematic 听风电影 | 分镜工作台 | 虾塘 completed + **用户选线** | handoff-xiajing.md / 听风交付文档 |

### 1.2 支线（可并行 / 提前做）

| skill | 角色 | 说明 |
|---|---|---|
| xiage-styles 虾格 | 风格模板 | **主线必经确认门（主线第 3 步，虾料之后）**：虾剧成稿 + 虾料摄入成稿、知识图谱/世界观确认后、虾塘建资产前，必须走「剧本理解分析 → 三步向导」确认创作方向三件套——①风格类型（6 预设+自定义扩展）→ ②视觉参考（3-5 组现有影视作品）→ ③影片基调（3-5 种），均必选；结果注入虾料 visual_style 与虾镜生成链路；确认点 = 风格三要素 + visual_references + tonal_direction |

**虾格确认点规范（强制，创作方向必须用户拍板，禁止代选）：**
0. **时机 = 虾料（摄入成稿）确认后、虾塘前（★）**：主线先跑虾剧成稿 → 虾料摄入成稿（世界观 + 知识图谱），用户确认成稿/图谱后，进入虾格走「剧本理解分析 → 三步向导」；**创作方向三件套全部拍板后才进入虾塘建资产**。已建资产后再改风格 = 流程事故（资产图全要重做）。
0.5 **题材时代感校验（★）**：确认前必须检查小说题材的时代/科技设定（如 2044 年灵气复苏 → 现代科技建筑 + 忍者融合；古代架空 → 古风建筑）。**风格必须与题材时代感一致**——禁止"国漫幻想/古装"预设无脑套到现代题材上（教训：2044 现代科技世界观被套成古代仙侠建筑）。若预设与题材冲突，向用户提出「预设风格 / 自定义融合风格」选项。
0.6 **剧本理解分析（★ 前置必做，不输出大段分析）**：正式提供方案前，完整阅读剧本，分析八项——①核心矛盾 ②人物情感变化 ③起承转合与高潮位置 ④每场戏叙事功能 ⑤世界观/时代/地域/季节/天气 ⑥摄影/美术/色彩/光影表达 ⑦奇观场景清单 ⑧克制场景清单；分析后**只转化为三组可选方案 + 一句推荐理由**；奇观/克制清单写入项目 `outputs/`（随 handoff 注入虾镜），禁止写入 skill。
1. **三步向导（★ 顺序锁死：确定前一个，后一个才能做具体推荐）**：
   - **① 风格类型**：3-5 候选（6 预设 + 已固化自定义 + 新扩展），按题材/世界观推荐，理由 1-2 句。
   - **② 视觉参考**：基于①已定风格，3-5 组现有影视作品（参考虾格模块 `references/reference-mapping.md` 映射表），每组 = 作品名 + 一句可取之处；参考≠复刻，只提取视觉语言。
   - **③ 影片基调**：基于①② + 核心矛盾与情绪曲线，3-5 种（参考虾格模块 `references/tonal-directions.md` 基调池），每种 = 名称 + 一句说明。
   - 每步方案间差异明显；**视觉参考/基调为必选维度**。
2. **必须给出推荐默认**：每步均给推荐（依据题材/世界观，如玄幻仙侠 → guoman_fantasy；现代都市 → realistic；现代+超能力 → 自定义融合「现代科技写实 + 忍者元素」），说明推荐理由（1-2 句）。
3. **用户拍板方式**：每步用 AskUserQuestion 拍板（推荐项标注 (Recommended) 排第一）；HTML 虾格页展示风格候选（左=风格列表，右=三要素+预设图[人物近景含环境]），视觉参考/基调以列表+说明呈现；三步依次确认，全部完成才算虾格通过。
4. 三件套全部选定后才执行虾格落地（写 style 三要素 + visual_references + tonal_direction → handoff-xiage.md → 注入虾料 visual_style 与虾镜链路）。
5. 用户未明确要求时，不得直接套用默认值跳过决策；「除非用户明确要求，否则不跳过方案选择阶段」为最高纪律。
6. **新风格扩展（★ 两条路径，都在 HTML 虾格页操作）**：
   - **① 图片反推**：用户上传参考图（人物近景含环境）→ AI 分析反推风格三要素（满足格式规范）→ 新风格加到 HTML 虾格页列表 → 用户确认拍板 → 写入项目 `outputs/styles/` → 固化虾格模块 `presets/`
   - **② 指定名称**：用户指定风格名称 → AI 按格式规范生成三要素 → 加到 HTML 虾格页列表 → 用户确认拍板 → 写入项目 → 固化虾格模块 `presets/`
   - 新预设必须满足三要素规范（style_tag 标签纯净性；style_instructions/avoid_instructions 按虾格模块 `references/style-format-spec.md` 格式）＋ is_preset=True + confirmed_by_user + confirmation_note。
7. **可选风格清单（6 预设 + 已固化自定义）**：anime 日漫 / chinese_period_drama 古装 / guoman_fantasy 国漫幻想 / post_apocalyptic 废土 / realistic 写实 / republican_era_drama 民国 / **modern_urban_ninja 现代都市忍者（自定义·已固化）**；虾格模块 `presets/` 下新增的预设自动进入此清单。

### 1.3 贯穿（任意环节）

| skill | 角色 | 说明 |
|---|---|---|
| xiaodao-assistant 虾导 | 对话驱动 | 任何环节可切到虾导对话式推进；执行记录落在 handoff-xiaodao.md |

### 1.4 HTML 项目管理台（★ 每项目必建，AI 每步产出都写回）

**目标**：像 dramaclaw 项目详情页一样，给用户一个可直接打开浏览的 HTML 项目管理台，集中管理 虾料/虾格/虾塘/虾镜 四块内容；AI 每跑完一个模块，产出内容自动同步进去；用户外部生成的图片放回 assets/ 目录即可显示。

**目录结构**（项目根下 `project/`）：
```
<project>/
├── project/
│   ├── index.html          # 展示层（file:// 直接双击打开；四 Tab：虾料/虾格/虾塘/虾镜）
│   ├── （数据层 = 项目根 xiaji.db，前端 GET /api/data；不再生成 data.js）
│   ├── server.py           # 本地服务：静态托管 + POST /upload 真上传（写入 assets/ 并自动标记 *_ready）
│   ├── api_slot.py         # ★API 插槽（可插拔）：API 设置(配置读写/渠道工具) + API 调用(生图4协议+Node脚本+Agent对话+测试)；
│   │                       #   server.py 只负责路由/保存/状态，全部 API 细节在此文件；换 API 平台只改此文件
│   ├── 启动项目台.bat      # 双击启动服务并打开浏览器 http://127.0.0.1:8320（纯 ASCII，跨代码页安全）
│   ├── 启动项目台.ps1      # PowerShell 保底启动器（bat 双击异常时，右键"运行 PowerShell"）
│   └── assets/
│       ├── characters/     # 角色定妆图/身份图（用户外部生成后上传或放入）
│       ├── scenes/         # 场景图
│       ├── props/          # 道具参考图
│       └── ep{NNN}/        # 每集：sketches/ frames/ audio/ videos/
```

**双模式**：
- **服务模式**（推荐，可上传）：双击 `启动项目台.bat`（或 `python server.py [端口]`）→ 浏览器打开 `http://127.0.0.1:8320` → 页面显示「● 服务模式」→ 每张图片位有上传按钮，选图即写入 `assets/` 对应目录并自动把 SQLite 对应 `*_ready` 置 true。
- **静态模式**（仅浏览）：直接双击 index.html（file://），可看全部内容与提示词，但上传按钮降级为提示文字（浏览器禁止网页写本地磁盘）。

**数据契约（SQLite snapshots → GET /api/data 返回的 PROJECT 结构；data.js 已废弃，仅历史迁移源）**：
```js
window.PROJECT = {
  meta: { name, project_id, source, style_id, style_label, style_tag, mode, updated_at },
  xialiao: { format_check, chapters,
             scene_blocks: [{episode,scene,location,time_of_day,interior_exterior,header_line,dialogue_lines,action_lines,characters}],  // ★ 必须是数组！前端 renderXialiao 用 .map 遍历；误写成计数数字会 TypeError（2026-08-19 事故）
             total_chars, billable_chars, spine_template,
             world: {realms,timeline,power_system,factions,crops,conflicts,system_mechanics,golden_rules,environment},   // 来自 outputs/world.json
             // ★ 类型契约（前端 renderWorldView 按此渲染，违反会 TypeError）：realms/timeline/factions/conflicts/golden_rules/crops 必须为【数组】；power_system/system_mechanics 为【对象】{desc,key:value}；environment 为【对象】且 environment.city 为【对象】{name,layout,...}
             // ★ scene_blocks 数组元素字段：dialogue_lines / action_lines 必须为【数字】行数（前端显示"N 行"），非字符串内容
             knowledge_graph: {node_count,edge_count,nodes,edges} },   // 来自 outputs/world.json（43 节点 69 关系，取自 docs）
             // ★ knowledge_graph.nodes 每个节点必须含 x / y 初始坐标（如按类型分环 600/360 中心预分配）；前端无 force simulation，缺坐标会全部堆在中心（模板已加环形兜底，但数据层仍应主动预分配，保证按类型分层可读）
             // ★ 知识图谱字段契约（★ 2026-08-24 实战固化，前端 xialiao.js 强依赖）：
             //   - 节点对象必须含 `label` 字段（渲染节点文字用，NOT `name` 单独；`name` 可并存但 `label` 必填，否则节点无名称）
             //   - 边对象必须含 `from` / `to` 字段（连线匹配节点用，NOT `source`/`target`；否则 kgUpdateEdges 查不到坐标→线不显示）
             //   - 主角布局可选：将主角节点 x/y 固定为中心 (600,360)，其余按亲密度分层放射（内环→外环），视觉突出主角
             //   - 错误示例（曾发生）：节点只有 name 无 label → 点无名字；边用 source/target → 线全堆原点不显示
  xiage:   { styles: [{style_id, style_label, style_tag, style_instructions, avoid_instructions,
             recommended_for, is_preset, confirmed_by_user, image, image_ready}],   // 来自 outputs/styles/*.json
             current: "style_id",
             visual_references: [...], tonal_direction: "..." },   // ★创作方向三件套（三步向导产出）
  xiatang: { characters:[{name,role,desc,is_main,image,image_ready,prompt,identities}],
             scenes:[{name,header,time,interior,image,image_ready,prompt}],
             props:[{name,owner,reason,image,image_ready,prompt}], props_merged },
  xiajing: { episodes:[{number,title,status,shots:[{shot_number,duration,shot_type,visual,dialogue,
             composition,scene_tag,characters,camera,action,movement,sound,narrative,
             firstframe_prompt,firstframe_image,tailframe_prompt,tailframe_image,
             video_prompt,video,is_edited,original_shot}]}] },
  xiaju:  { episodes:[{number,title,body,source:"md|frontend",updated_at}] },   // ★ 虾剧·剧本创作（2026-09-05 新增）：扫描 outputs/xiaju/ep{NNN}.md 初始化；db 已有（前端编辑版）优先、md 独有集补充；tab 消费方 = templates/js/xiaju.js
};
```

**SQLite 快照库（★ 每项目必建）**：`project/xiaji.db` 存全部数据快照（保原样，JSON 原样存储零转换）。
- 表 `snapshots(module TEXT PK, data TEXT, updated_at)`，每模块一行（meta/xialiao/xiaju/xiage/xiatang/xiajing/tingfeng/pipeline/resume_guide）。
- 端点：`GET /db/status`（状态/模块清单）、`GET /db/snapshot?module=x`（查单模块）、`POST /db/migrate`（从 data.js 全量迁移）、`POST /db/verify`（与 data.js 逐模块严格对比，零差异才算 ok）。
- 数据流：outputs/ → build-data-js.py 重建（★ 会从 xiaji.db 旧快照 merge 回运行时产物：storyboards.video_prompts/video_segments、space_map_*、xiatang views/plan/dists 八字段、tingfeng storyboards/space_map——全量重建不丢运行时数据）→ 前端经 `GET /api/data` 读取；SQLite 快照 = 重建 merge 源 + 查询/备份，不再是单向镜像（2026-09-02 自检修正）。

**虾格 Tab（★ 风格确认页，必建）**：
- **左侧**：风格列表（6 预设 + 已固化自定义 + 本项目新扩展），每项显示 style_label + 推荐标记。
- **右侧**：选中风格的三要素展示（style_tag / style_instructions / avoid_instructions）+ **预设风格图片（内容为人物近景并包含环境）**（图缺时占位+提示词）。
- **交互**：①确认应用（标记 current）；②**上传参考图反推风格**（AI 分析→生成三要素→加入列表）；③**指定风格名称生成**（AI 按格式规范生成三要素→加入列表）；新风格经用户确认后写项目 `outputs/styles/` 并固化虾格模块 `presets/`。
- 图片存放：`project/assets/styles/<style_id>.png`（用户外部生成后放入/上传，人物近景含环境）。

**写回规则（强制）**：
1. **每模块完成后必须同步 HTML 项目**：虾料→写 xialiao；虾格→写 meta.style；虾塘→写 xiatang；虾镜→写 xiajing；最后跑 `scripts/build-data-js.py <项目根>`（本 skill 自带模板脚本）重建数据并同步 SQLite(`xiaji.db`)，保证数据一致。★ **build-data-js.py 双副本纪律（2026-09-02 自检固化）**：`scripts/` 与 `templates/` 下两份 build-data-js.py 必须保持一致（scripts 副本曾落后、缺 tingfeng 组装与运行时字段 merge，按其执行会丢数据）——改动任一副本后立即 cp 同步另一份并 diff 验证 SAME。
2. **提示词全量交付**：assets 图片提示词、身份图提示词、草图/首帧提示词、视频提示词全部写入 data.js（用户页面可一键复制）。
3. **图片归用户 + 就绪标记按文件自动判定（2026-08-17 增强）**：`build-data-js.py` 现按 `project/assets/` 下文件实际存在自动判定 `image_ready/sketch_ready/frame_ready/audio_ready/video_ready`（字符/身份图/场景/道具图片、`ep{NNN}/sketches|frames|audio|video/` 下文件）。**用户上传图后全量重建不再丢失就绪标记**；无需手动登记。
4. **手动增强必须固化进数据源（2026-08-17 教训·防覆盖）**：build-data-js.py 是全量重建脚本，任何**直接改 data.js 的增强**（世界观、图谱、新增场景等）在下次重建时会被覆盖。增强必须固化到数据源：①世界观/图谱 → 写 `outputs/world.json`（由 `outputs/build_world.py` 从 docs 生成，43 节点 69 关系）；②场景/道具/角色资产 → 写 `outputs/xiatang/assets-registry.json` + `asset-prompts.md`；③新集 → 写 `outputs/xiajing/ep{NNN}/`。**禁止只改 data.js 不写数据源。**
5. **汇报时必须告知用户 HTML 项目路径**（`project/index.html`），并说明双模式（静态浏览 / 双击启动项目台.bat 可上传）。
6. **状态同步**：每集 `episode_status`（9 步）与 pipeline-state.json 保持一致。

**首次创建 HTML 项目**：若项目根下无 `project/`，**复制本 skill 的 `templates/` 整个目录**（`~/.workbuddy/skills/xia-boss/templates/` → 新项目 `project/`）——含 server.py / index.html / css/ / js/ / 启动项目台.bat / img_config.json，开箱即用（888 渠道 + 任务队列 + 分集 + 最新 UI）。**禁止从已有项目（既有项目）复制**——已有项目可能带旧样式/旧数据；**UI 样式以模板为准**：改 UI 直接改 `templates/css/main.css`（或 index.html/js），新项目复制即带新样式；已有项目需手动同步——**必须走 `references/旧项目手动同步SOP.md`**（备份 → 盘点差异 → 覆盖代码层 → 替换占位符/回迁定制 → 重启验证；禁止裸 cp，img_config.json 等配置/数据层绝不覆盖）。

**触发词**：项目管理台、HTML 项目、项目页、虾集项目台、把产出放到网页、网页管理、同步到项目页、上传图片、启动项目台、服务模式。

### 1.4.0 生图任务队列与已知修复（⚙ 详情懒加载：references/project-platform.md）

- 生图任务队列（默认并发 5 / 批量上限 60，调度器模式）、资产图防缓存（时间戳命名）、分集 ep 贯穿、保存提示词（/save-prompt）、参考图 tabs、场景切换视角/视距航拍/上帝视角、**提示词中文参考·双语展示（★ 2026-09-06b：`prompt_cn` 中文理解稿只读 + `prompt` 英文执行版；弹窗「📝 中文参考稿」折叠区 + `promptBlockDual` 卡片双语；落点 index.html/gen.js/xiatang.js/tingfeng.js 四处联动，同步纪律见 references/project-platform.md）**、⚙ 设置（图片默认尺寸 + **h3_dual_product H3 双产开关（★ 2026-09-03 P2-2，缺省 true，改 file 即生效）**）、项目台已知修复与调试（症状→根因→修复，9+ 项）——**全部端点契约与实现细节见 references/project-platform.md（排查项目台问题时必读）**。
- **触发词**：任务队列、队列满了、生图队列、并发限制、任务面板、任务中、任务完成、队列已满、图片缓存、显示旧图、缓存问题、图片默认比例、默认尺寸、默认比例、黑白草图、草图前缀、尺寸列表、尺寸裁剪、尺寸太多、尺寸精简、H3 开关、双产开关、中文参考稿、提示词中文、中文稿不显示、双语提示词、prompt_cn、promptBlockDual。

### 1.5 运行模式

- **还原模式（默认）**：编排表 = 上述 5 个模块的"功能还原"流程（把 DramaClaw 各模块复刻到目标项目）。
- **生产模式（可选）**：同一编排表，但各模块执行的是"使用已还原功能生产内容"的流程。
- 模式切换：用户明确要求时切换；切换只改执行语义，不改依赖结构；在 `pipeline-state.json` 的 `mode` 字段记录。

---

## 2. 状态契约（pipeline-state.json）

每个项目一份，放在项目工作区根目录（如与 handoff 文件同目录）：

```json
{
  "project": "<项目名>",
  "mode": "restore",
  "skill_version": "<现读本包根目录 VERSION 文件逐字填入>",
  "created_at": "<ISO 时间>",
  "updated_at": "<ISO 时间>",
  "modules": {
    "xiaju-script":       {"status": "not_started", "handoff": "", "finished_at": ""},
    "xialiao-ingest":     {"status": "not_started", "handoff": "", "finished_at": ""},
    "xiage-styles":       {"status": "not_started", "handoff": "", "finished_at": ""},
    "xiatang-characters": {"status": "not_started", "handoff": "", "finished_at": ""},
    "xiajing-episodes":   {"status": "not_started", "handoff": "", "finished_at": ""},
    "storyboard-cinematic": {"status": "not_started", "handoff": "", "finished_at": ""},
    "xiaodao-assistant":  {"status": "available",   "handoff": "", "finished_at": ""}
  },
  "current": "xiaju-script",
  "history": []
}
```

- status 取值：`not_started / in_progress / awaiting_confirmation / completed / blocked / skipped / available`
- `current` = 当前应执行的模块；`history` = 执行流水（`{module, action, time}` 追加数组）
- 模块 completed 时记录 handoff 文件路径与 finished_at；每步执行后立即更新 `updated_at`
- **`skill_version` 写入口径（★ 2026-09-09 动态化）**：创建/更新 `pipeline-state.json` 时该字段**必须现读根目录 `VERSION` 文件逐字写入**（含带后缀标签，如 `3.4.4-Xuan酱速查`）——**禁止照抄本文件示例、禁止凭记忆填写、禁止只写主版本号**。示例中写死过具体数字，导致后续版本递增后新状态文件仍记旧值、§3.1 续集的"版本比对"形同虚设。
- **xiaju-script 规范化规则（★ 2026-09-08：全入口必经、不 skip）**：不管输入是大纲、小说原文、单章、还是已成稿剧本，**一律先过虾剧**——小说/单章→按 90s 情绪循环"剧本化"改写；**已有成稿剧本→做"规范化改写进虾剧格式"**（补每场景独立 `场景：<纯地点名> <时间> <内/外>` 头、秒段改六字段、循环头尾写完整"内景/外景"），**只对齐体例、不擅自重写剧情/改台词/动结构**（剧情改动仍受 §八锁定铁律约束）。仅当原文已完全合规时，虾剧这步是"规范化校订"轻量执行，仍记 `completed` 不记 `skipped`。
- **分镜线分支登记（★ 2026-09-01）**：虾塘确认门用户选「听风电影」时，在 modules 中登记 `"storyboard-cinematic": {"status": "in_progress", "handoff": "", "finished_at": ""}` 并把 current 指向它；选「虾镜」则按原流程指向 xiajing-episodes。两条线只做用户选中的一条（§0.7），另一条保持 `not_started`。

---

## 3. 运行时循环（每轮必做）

```
① 读状态     加载 pipeline-state.json（无则按 §1 编排表初始化）
② 定位断点   找 current（或第一个 status∈{not_started, in_progress} 且非 blocked 的模块）
③ 执行模块   用 Read 读取 modules/<当前模块>/SKILL.md（自包含，见 §0），按其内部流程执行
④ 读交接物   模块完成后读取其 handoff-*.md
⑤ 简报+确认  提炼简报呈现给用户，停下等确认（简报模板见 §4；简报须标注当前处于 §0.5 七步中的哪一步）
⑥ 调度       用户确认 → 更新状态 → 设置新的 current → 回到 ②，直到全部 completed
⑦ 同步项目台（★ 强制三步验收，缺一不可，2026-09-05 事故固化）：
   a) **重建**：跑 `scripts/build-data-js.py <项目根>` 重建 SQLite(`project/xiaji.db`)——SQLite 是唯一数据源，前端经 `GET /api/data` 读取（data.js 已废弃）；
   b) **校验**：读 SQLite 或 `GET /api/data` 确认**本模块数据已入库**（如虾剧→`xiaju.episodes` 数=成稿集数、虾料→知识图谱节点数、虾塘→资产数），数字对不上=同步失败，禁止跳过校验直接汇报；
   c) **服务存活**：`curl http://127.0.0.1:<端口>/api/data` 确认项目台服务活着（项目搬迁/重启后端口会变，旧端口作废）；服务未起则后台启动 `python project/server.py <端口>` 并在简报中给出新 URL。
   - **★ 改了产出文件却同步不出来 = 合并规则事故（2026-09-05 二次事故）**：构建脚本对部分模块有"db 快照优先"的合并逻辑（如 xiaju 前端编辑版优先于 md）。若磁盘文件已更新、项目台仍显示旧内容，先查该类合并规则是否把新产出挡住了——**正确规则：只有"前端真人编辑"（`source: frontend`）的条目才优先于磁盘产出，AI 产出的旧条目必须被新文件覆盖**。修好后重跑并复验。
```

> **小说入口特例（★ 剧本先行）**：若用户给的是小说项目路径/单章（见 §0.6），则**先执行「虾剧·小说→剧本化」**（写成/规范化成稿 `outputs/xiaju/`）并停在成稿确认门；确认后再执行「虾料摄入成稿 + docs 设定梳理」，随后同步 HTML 项目台**只做知识图谱 + 世界观**两块，简报后停在确认门，用户确认后才推进到虾格→虾塘→分镜线（不自动连跑）。

### 3.1 续集恢复协议（★ 跨工具/跨机器续集，强制）

**目标**：换电脑 / 换 AI 工具（任何能加载本 skill 的助手）后，只要拿到项目目录，就能读取断点直接续集，不依赖 WorkBuddy 专有环境。

**续集入口**（用户说"继续这个项目" / "续集" / 指向某项目目录时）：
1. **定位项目根**：用户给出目录，或含 `pipeline-state.json` / `project/xiaji.db` 的目录即项目根。
2. **读状态**（优先级从上到下）：
   - ① `pipeline-state.json` —— 权威断点（current / modules 状态 / history）；**`skill_version` 字段（★ 2026-09-03 P1-2）与根目录 VERSION 比对——不一致时提示用户"项目由旧版本流水线产出，注意契约差异"（旧项目无该字段 → 跳过检测，向后兼容）**
   - ② `project/xiaji.db` SQLite 快照（或服务存活时 `GET /api/data`）—— 状态与产出镜像
   - ③ 无状态文件 → 按 §1 编排表全新初始化
3. **读产出**：SQLite `snapshots` 表（xialiao/xiaju/xiage/xiatang/xiajing/tingfeng 全部产出 + resume_guide 续集引导）。
4. **读 resume_guide**：快照内置 `how_to_resume` 字段，说明当前断点与下一步——任何工具照此执行即可续跑。
5. **输出断点简报**：按 §4 模板汇报「当前第几步·七步位置·产出概况·下一步」，等用户确认后续跑。

**续集数据自包含性**：`project/` 目录（index.html + server.py + xiaji.db + assets/ + 根目录 pipeline-state.json）必须包含续集所需的全部信息——产出数据、状态、断点、风格、标签约定、提示词规范。**项目目录 = 完整项目状态**，不依赖历史对话。

**续集后写回**：续跑完成的模块同样执行 ⑦ 同步项目台三步验收，保证 SQLite 与 pipeline-state.json 始终最新。

---

## 4. 简报模板（确认门的内容）

每个模块完成后，向用户输出：

```
【第 N 步 · <模块名>】✅ 完成 / ⚠️ 部分完成 / ❌ 阻塞

做了什么：<2-3 条关键动作>
产出物：<handoff 路径 + 数据契约核心字段 3-5 个>
状态：module_status（ok / warning / blocked）
项目台：<已同步（数据校验通过：本模块关键字段计数）· 服务地址 http://127.0.0.1:<端口>> / ⚠️ 同步失败原因

关键决策点：<需要用户拍板的选项，如 项目类型 / 风格 / 主角唯一性 / **分镜线（虾塘完成后必问：🎬 听风电影 or 🎬 虾镜，§0.7）** / 是否继续生产>

下一步：<建议交给谁做什么>
请确认：继续 / 修改 / 回退 / 跳过？
```

**关键决策点必须列出**——这是"用户确认"的价值所在，让用户有依据做决定，而不是盲目点头。
**凡涉及"风格 / 项目类型 / 主角唯一性 / 是否继续生产"等拍板项，一律用 AskUserQuestion 呈现选项 + 推荐，禁止替用户默认决定。**

### 4.1 创作方向决策的呈现方式（虾格专属）

当确认门涉及虾格时，简报按三步向导逐轮呈现（顺序锁死，确定前一个后一个才做具体推荐）：

**第一步 · 风格类型**（3-5 候选，必选）：
```
可选风格（6 预设 + 已固化自定义 + 可扩展）：
| 风格 id | 中文名 | 适用 |
| anime | 日漫 | 二次元 |
| chinese_period_drama | 古装 | 中文古装剧 |
| guoman_fantasy | 国漫幻想 | 玄幻仙侠 |
| post_apocalyptic | 废土 | 末世 |
| realistic | 写实 | 现实向 |
| republican_era_drama | 民国 | 民国剧 |
| modern_urban_ninja | 现代都市忍者（自定义·已固化） | 现代都市+超能力/忍者/灵气复苏 |
（更多自定义见虾格模块 presets/；用户可上传参考图反推新风格，或指定风格名生成三要素）

推荐默认：<风格 id>（理由：题材匹配 <世界观类型>；注意题材时代感校验，如 2044 现代科技世界观禁套古装/仙侠预设）
```

**第二步 · 视觉参考**（基于第一步已定风格，3-5 组，必选）：
```
| 参考作品 | 可取之处（一句话） |
| ... | ... |
（参考池见虾格模块 references/reference-mapping.md；参考≠复刻，只提取视觉语言）

推荐默认：<参考组>（理由：与已定风格 <风格 id> 匹配的视觉语言）
```

**第三步 · 影片基调**（基于前两步 + 核心矛盾与情绪曲线，3-5 种，必选）：
```
| 基调 | 说明（一句话） |
| ... | ... |
（基调池见虾格模块 references/tonal-directions.md）

推荐默认：<基调>（理由：核心矛盾 <...> + 情绪曲线 <...>）
```

→ 每步 AskUserQuestion 拍板（推荐项排第一标注 Recommended）；三件套全部选定后才落地
→ 用户拍板后：写项目 outputs/styles/ + visual_references + tonal_direction → handoff-xiage.md
→ 奇观/克制场景清单写入项目 outputs/，随 handoff 注入虾镜

---

## 5. 用户指令处理

| 用户说 | 动作 |
|---|---|
| 继续 / 可以 / ok | 确认当前模块 → 调度下一个 |
| 确认 / 开始写第一章 | 小说入口第①步：虾剧·小说→剧本化成稿（§0.6·剧本先行）；成稿确认后进虾料摄入成稿 |
| 修改 <内容> | 回退到相关模块重新执行（或局部修正后继续） |
| 回退 / 从 X 重来 | 重置 current 到指定模块，其后模块标记 not_started |
| 跳过 <模块> | 标记 skipped（记录原因），直接进下一个 |
| 先做 <虾格 / 虾导> | 切换到支线/贯穿模块，主线挂起 |
| **选听风电影 / 开始听风** | **分镜阶段分支**：current 记为 storyboard-cinematic（听风线），加载 `modules/storyboard-cinematic/SKILL.md` 做分镜 |
| **选虾镜 / 开始虾镜** | **分镜阶段分支**：current 记为 xiajing-episodes（虾镜线），按原流程做分镜（过 scene-consistency 闸门） |
| **学习打戏 / 打戏拉片 / 学习提示词 / 入库学习** | **打戏学习（★ 2026-09-07 v3 改造版）**：路由至 `modules/battle-learner/SKILL.md` 执行（三通道：提示词解剖/视频拉片[依赖 ffmpeg]/配对对照）——学习成果产出《兵器技法资料库_XX专项.md》（七段式，对齐 seedance-combat-prompt 现有专项），写入 `modules/seedance-combat-prompt/` 根目录并登记 weapons.md 三处清单；确认门控不变；不再写 learned/failures 库（已随 fight-scene-director 归档） |
| **打戏调度 / 打戏外协 / 战斗段生成提示词** | **打戏外协调度（★ 2026-09-07 v3 新增）**：按 `references/shared-fight-dispatch.md` 执行——判定战斗段（登记 battle_segments）→ 组装八字段调度单（含群战人数结构）→ 用户确认 → Read `modules/seedance-combat-prompt/SKILL.md` 按其工作流生成 15s 提示词 → 回流+负面四禁合并+合规转译 |
| 用虾导推进 | 切换到虾导对话驱动（执行记录落 handoff-xiaodao.md） |
| 现在到哪一步了 | 只读状态，输出总览，不执行 |
| 换生产模式 | 切换 mode，继续同一编排表 |

---

## 6. 容错规则

1. **交接物缺失 / 格式错误** → 停下说明，不硬猜不跳过；提供"重新执行本模块"选项。
2. **模块内部 blocked**（验收未过）→ 标记 blocked，提供 重试 / 修复 / 回退 三选一。
3. **用户中途改需求** → 按 §5 处理，任何时刻可打断。
4. **虾导贯穿**：任何一步用户想用对话推进，立即切换，不强制走主线。

---

## 6.1 新模块接入前后端成对（★ 2026-09-01 事故固化，强制）

- **后端落库成对**：mark_ready 的 category 分支（写内存数据）与 `if changed:` 落库清单（db_upsert）**必须成对添加**——只加分支不加落库行 = 任务完成但数据永不落库，前端永远空白。
- **前端刷新成对（★ 2026-09-01 补充）**：新增模块/页签接入前端时，**三处成对缺一不可**——①侧栏入口（data-section）②`core.js renderSection` 分支 ③`gen.js rerenderCurrent` 分支（含 `markReadyInMemory` 对应 category 分支）。漏 ③ 的症状：**生图/生视频任务完成后资产位不更新，手动刷新页面才显示**（任务队列显示成功、数据已落库，唯独页面不重绘）。
- 接入自检：后端 `grep -n "db_upsert" server.py` 核对落库清单 ⊇ 全部模块；前端核对 `rerenderCurrent` 的 section 分支 ⊇ 全部页签；用真实生图任务端到端验证「任务完成 → db 快照含新数据 → 页面资产位**不刷新页面**立即更新」。
- 反面教材（同一模块两连踩）：①只加 category 分支漏落库行 → 生成成功但 db 永不更新；②rerenderCurrent 漏新 section 分支 → 生成成功、db 已更新，但页面不刷新看不到。两类症状不同，根因同源：**接入成对只做了一半**。

## 7. 与各模块的接口

- 输入：`modules/<模块名>/SKILL.md`（用 Read 读取，即获得该模块内部流程与执行纪律；见 §0 自包含架构）。
- 输出消费：各模块的 handoff-*.md（统一模板见各模块 §6：元信息 / 验收摘要 / 数据契约 JSON / 产物清单 / 下一步）。
- 本 skill 不创建新 handoff 文件；唯一持久化物是 `pipeline-state.json`。

---

## 8. 执行纪律（强制）

1. **单步推进**：一次循环只执行 1 个模块，执行完即停、等确认。
2. **顺序不跳**：主线严格按 **虾剧 → 虾料 → 虾格 → 虾塘 → 分镜线**（★ 剧本先行）；支线 / 贯穿按用户指令插入。
3. **不代劳**：不替各模块做实现决策，不修改它们的产出逻辑。
4. **状态落地**：每步更新 pipeline-state.json 后再汇报。
5. **简报不注水**：只报 handoff 中实际存在的字段（grounding），不编造。
6. **完成收口**：全部 completed 后输出流水线总结（history + 产物清单），提示可切生产模式继续。
7. **小说入口纪律（§0.6·剧本先行）**：给小说路径/单章时，**第 ① 步先经虾剧·小说→剧本化**（写成/规范化成稿 `outputs/xiaju/`）并停在成稿确认门；确认后才由**虾料摄入成稿**梳理 docs 设定 + HTML 知识图谱/世界观，再停在确认门，确认后才进虾格→虾塘。只给单章时先询问是否补世界观，不补则由虾剧按单章理解推断并标注【推断】；**知识图谱数据源以成稿 + docs/ 设定为准，禁止凭印象杜撰**。
8. **创作方向确认门不可跳过（★ 最高级）**：流程顺序 = **虾剧·剧本成稿（§0.8，全入口必经；小说/单章→剧本化、已有成稿→规范化，都不 skip）→ 虾料摄入成稿（梳理世界观 + 知识图谱 + 生产事实）→ 成稿/图谱确认 → 虾格三步向导（①风格类型 → ②视觉参考 → ③影片基调，均必选）→ 虾塘建资产 → 分镜线**。创作方向确认在**虾塘前**（剧本理解分析 → 三步向导，每步 AskUserQuestion 拍板，推荐项排第一），**禁止带默认值静默跑完、禁止跳过、禁止省略必选维度**。虾格八项分析以虾剧成稿为输入（一次算准，免重算曲线）；pipeline-state.json 中 xiage-styles 的 status 必须是 `confirmed`（用户拍板）才算通过；若是 `completed` 但无用户确认记录，视为流程事故，需停下重新确认创作方向并修正已产出提示词（尤其资产图风格错误要重做）。
9. **docs 优先**：任何涉及世界观/角色/势力的产出，先查 docs/ 设定文件再动手，避免与既有设定冲突。
12. **身份图锁脸（★ 强制）**：身份图与主图必须是同一张脸。身份图 prompt 必须含锁脸指令（same face as reference image / 五官一致），生成时主图必须作为 ref 图传入（images[0]）。虾集项目台前端自动读取主图作 ref；虾塘身份图铁律 + build-data-js 自动校验（详见 xiatang-characters `references/character-assets.md` §6 锁脸铁律）。
11. **角色图像规格（★ 强制）**：角色主图（定妆照）= **证件照式标准像（3:4 竖版）**——正面居中/中性表情/纯色背景/均匀光/无姿态动作（3:4 portrait, ID/passport-style front-facing standard headshot, neutral expression, plain solid background, even studio lighting, no pose, 85mm f/1.8）；身份造型图 = **两图制（★ 2026-08-21 用户拍板）**——**① 定妆照**（该身份正面基准图，与主图同一面容）＋ **② 四视图角色卡**（2×2 四格：上排正面/正侧面部特写 + 下排无头全身正面 + 全身背面，纯色背景单侧侧缘逆光，固定框架见 xiatang-characters `references/character-sheet-4view.md`）。主图定"脸"、身份图定"造型"（两图同面容同服装），同角色身份图与主图同一面容。虾塘产出、虾镜引用都必须遵守（详见 xiatang-characters `references/character-assets.md`）。
10. **场景提示词禁人物（★ 强制）**：**场景资产/场景图的提示词里永远不要出现人物和角色**——只描述环境本身（建筑/空间/光线/氛围/天气/植被/道具陈设等），禁止出现人物、人群、角色动作、人物服饰等描述（如 "thousands of students gathered"、"a man walking" 等一律禁止）。人物表现属于角色资产与分镜（虾镜 Beat）的职责；场景图是"空镜"。虾塘产出场景提示词、虾镜引用场景锚点时都必须遵守。**关键场景例外（★ 2026-08-21）**：独立资产类别「关键场景」= 关键画面素材（可含人物，属剧情画面），不受本条限制——它不属于场景资产（详见虾塘「关键场景图片提示词规范」）。
13. **英文纯净范式（★ 强制，2026-08-19 用户确认）**：**所有资产类生图提示词（角色定妆/身份图/场景/道具）必须用英文纯净描述**——只写镜头拍得到的画面内容（人物/服装/五官/动作姿态/场景/光线/道具细节）+ 质感/氛围段；**禁止把中文六要素设定段、台词、人物性格说明、剧情叙事、音效词写进提示词**。中文只允许出现在资产名称/标签/元数据字段（six_elements/desc 等），**不进提示词正文**。教训：既有项目 13 主图 + 18 身份图曾把中文六要素整段拼进英文提示词（中英混杂），AI 生图时中文段被忽略或干扰——已全部重写并加自动校验。
14. **资产提示词自动校验（★ 强制，2026-08-19 固化）**：**每次虾塘产出资产提示词后，必须运行 `scripts/check-assets.py <项目根>` 自检**，全部通过才算合格：① 英文纯净（无中文长串）② 主图证件照 9 要素 + 无违规词 ③ 身份图两图制（定妆照正面全身 + 四视图卡）④ 场景空镜（正向段无人物词）⑤ 道具五宫格 ⑥ **三要素内联完整性**（style_instructions 首句指纹 + avoid 关键禁令，弱风格词拦截）⑦ **视觉参考注入标记**（creation-direction.json 有 infusion 时强制）。**构建脚本（build_xiatang.py 等）必须使用证件照式模板**（禁止 full body shot / standing pose / dark gradient background 等旧写法），产出后立即跑校验，失败即修复重跑，禁止跳过。 **群像资产（★ 2026-08-19 新增）**：多角色合影类资产（如「多角色群像」）在 registry 中加 `group: true` 标记，提示词用群像定妆（多人并排正面、纯色背景、50mm f/5.6、每人特征逐位列出、`Negative prompt:` 前缀），校验脚本对 group 资产**只查英文纯净、跳过证件照/四视图**；群像提示词**顶部必须加单人资产引用行**（如 `角色B=图1，角色C=图2，角色D=图3，角色E=图4`，用户生图时垫 4 张单人图保证人物一致），校验脚本对 group 资产**豁免顶部引用行的中文检查**（剥离第一行后查）。
16. **首帧/视频提示词结构（★ 强制，2026-08-19 用户确认定稿 + 2026-08-21 升级）**：`firstframe_prompt` = **顶部资产引用行**（`角色A=图1，示例大殿=图2`，按角色→场景→道具编号，`图N` 占位待换参考图，只引用 xiatang 资产库真实资产）+ **5 段式正文**（①风格与质感 ②镜头 ③色彩 ④画面内容 ⑤限制，英文纯净，①~③ 按项目 FS 库编号填充）；`video_prompt` = 顶部资产引用行 + **Seedance STYLE LOCK 导演模板**（风格锁/分镜画面/声音设计/关键约束，中英术语并写 + HEX 色值 + 一人一图锚定 + 硬切 only + real-time speed）。全量细节见 xiajing-episodes `references/prompt-5section.md` 与 `references/seedance-stylock.md`。
15. **草图提示词规范（★ 强制，2026-08-19 用户两轮拍板定稿）**：**sketch_prompt 用中文**，固定模板 `黑白草图，{画面描述}。；快速铅笔线条，未完成感，干净纸面。`——`{画面描述}` 段 = **场景+人物+姿态动作+镜头语言+场景特效**：**保留姿态动作**（跪地/抱拳/俯视/抬手/转身/递接/注视/蹲下/拨算盘——构图骨架必须可画）；**只剥离表情神态**（震惊/嘴角微扬/似笑非笑/目光/神色/愣住）、心理活动、台词引号、音效拟声、程度修饰（猛地/轻描淡写）。**首帧/视频提示词仍英文纯净**。全量细节见 xiajing-episodes §6 纪律 11。
17. **视觉参考前置资产提示词（★ 强制，2026-09-02 用户拍板）**：虾格三步向导拍板的**视觉参考必须在虾塘资产图阶段就消费**，不得只留给虾镜——资产图是最终视频的视觉源头（"前面的图错了，后面的视频跟着错"）。落地方式：把已定视觉参考作品的视觉语言转化为**可执行英文描述段**（`Visual reference infusion: ...`，参考≠复刻，只提取色彩体系/材质质感/构图氛围），按资产类型分版幂等注入每条资产提示词——**角色版**（主图/定妆照：只影响服装色彩/材质/整体色调，注明 keep composition and plain solid background unchanged，不改证件照构图）；**场景版**（空镜：全量生效——灰调/负空间/极简构图/氛围）；**道具版**（限材质与色调）。注入位置=渲染介质句（style_instructions）之后、负面段之前；校验（check-assets）通过后须复核每条提示词含 `Visual reference infusion` 标记。影片基调（tonal_direction）仍留给虾镜镜头语言消费，不进资产提示词。**infusion 段的生成与存储契约见虾格 SKILL.md §6.2**（note 一一对应转化 + 角色版生效范围限定句）；check-assets 第 ⑥ 项校验指纹/禁令/注入标记；配套闸门见纪律 18。
18. **资产图回传验证闸门（★ 强制，2026-09-02 用户拍板，方案 E）**：提示词合规 ≠ 出图合格——用户上传/生成任何资产图后，AI **必须主动核对图片渲染介质与 style_tag 一致性**（如 guoman_fantasy → 必须 3D CG 渲染质感，照片级写实即不合格），不合格须明确告知「不符合三要素 + 根因 + 修复建议」并回查提示词，**禁止沉默通过**；合格才标 image_ready 进下游。完整闸门规范见 xiatang-characters SKILL.md「资产图回传验证闸门」。
19. **前半程创作基准（★ 2026-09-03 集成，分布式）**：① 虾料——世界观确认材料就绪后强制补齐**生产事实六项**（国家年代地域/季节时间轴/语言声音文化/世界威胁规则/人物小传含声音气质与资产依赖/状态链视觉母题，四级事实标注：剧本事实/强推断/导演提案/待定冲突，提案不伪装成原文事实）+ **空间链表** `outputs/scene-chain.md`；国家年代/主要人物关系/核心世界规则/人物小传未确认前虾格不得出正式三件套、虾塘不得出正式角色确认稿。② 虾格——八项分析后**自动算全片情绪曲线**（8-16 节点 0-10，呼吸区/BGM 重点复用节点编号，与③基调一起拍板不加门）落盘 `emotion_curve`；**写实线 P1 深化**（参考+维度/摄影系统/风格锁定段/环境色彩/声音气质/声画圣经）落盘 `p1_cinematic_baseline`，非写实自动跳过。③ 虾塘——**依赖门禁**（血缘/变异/换装/受伤/年龄有向依赖，上游未定稿不写下游提示词，变异程度只问一次 H1/H2/H3）+ 提示词增强字段（骨相锁定仅写实线/命运气质/身份细节锚点含左右方向/哑光压高光/千人千面禁模糊词）+ 道具 `continuity_anchors` 与 `story_state`（锚点逐条进词，禁终态图冒充早期状态）。④ 下游——scene-consistency 读空间链；听风/虾镜情绪段标注与 BGM 取舍对齐 `emotion_curve`，风格段以 `p1_cinematic_baseline` 锁定段为母本；check-assets 第 ⑧ 项校验依赖与锚点。蒸馏纪律：来源方法论已通用化改写，全链路禁止出现来源 skill 的授权/终止话术；写实限定规则不得混入非写实项目。
20. **项目台同步纪律（★ 最高级，2026-09-05 事故固化，用户拍板"加强每一个环节必须同步到项目台上"）**：**每一个环节（虾料/虾剧/虾格/虾塘/虾镜/听风）完成后，必须执行 §3 运行时循环 ⑦ 的三步验收**——① 重建（跑 `scripts/build-data-js.py <项目根>`）→ ② 校验（查 SQLite 或 `GET /api/data`，本模块数据计数与产出一致才算入库）→ ③ 服务存活（curl /api/data；项目搬迁/换目录/重启后必须重查端口，旧端口一律作废）。**三步全过才允许汇报"模块完成"并输出简报**；简报必须含「项目台」行（§4 模板）。**禁止以下事故重演**：写完产出不跑构建脚本就汇报 / 项目搬迁后不重启服务给死链 / 风格等元数据与拍板结果不一致（权威链见 §0.5 步骤 5：creation-direction.json > handoff > 字母序兜底）/ 构建脚本改了 skill 源但项目副本未同步（三处必须同步：scripts/ + templates/ + 项目 project/，改完 py_compile 验证）。
21. **批量改文件先校验后写入（★ 2026-09-09 补丁脚本约定）**：任何用脚本批量修改本包或项目文件的动作（替换锚点、插入章节、递增版本），一律 **① 全量读入 → ② 只在内存里做断言（每个锚点命中数必须为 1、VERSION 当前值必须等于本次开始读到的值、被改路径必须存在、改后表格列数一致）→ ③ 断言全过才统一落盘**；**禁止边替换边写文件**——中途断言失败会留下「半套契约」（正文改了、版本没改，或反之），比不改更难排查。失败路径：不写任何文件、逐条打印 MISS、退出码非 0，修好锚点后整体重跑，再复跑一次全量自检（含投毒对照，确认门禁没空转）。**并发处置**：VERSION 起点与预期不符说明有别的会话在改，让自己的号让位到未占用的带后缀号并在 CHANGELOG 写明让号理由，禁止覆盖对方内容。 **留痕要求（★ 同批补）**：任何一次 VERSION 递增都必须在同批改动里写 CHANGELOG 条目（哪怕只有两行）——只在 VERSION 换号、包里不留档，事后既查不到那次改了什么，也无法用脚本检出缺口（本次实测：并发的 3.4.5-情绪双轨 只留了号，包内零标签，自动化检测完全看不见）。
