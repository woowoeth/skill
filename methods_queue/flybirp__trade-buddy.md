---
name: trade-buddy
description: 交易搭档（不是考官）。当用户说要买/考虑买某只个股、问某只票能不能买、或要求分析某只标的时使用（如"我想明天买电广传媒"、"帮我看下XX"、"XX现在能进吗"）。用户给出个股+价格+日期+想赚几个点后，本 skill 会：①主动拉取真实数据（K线量价/资金/龙虎榜/筹码/财报/股东/新闻催化剂/板块/公告）；②判断交易命题、行业景气、公司地位和同业替代；③综合历史可达性与当前证据给出目标收益的达成/失败/横盘概率区间；④生成高开/平开/低开情景树与分仓方案；⑤默认输出为精简 Markdown（.md）的可验证、可执行、可复盘交易决策单。
display_name: 交易搭档
display_name_en: Trade Buddy
description_zh: 交易搭档（不是考官）——把一句模糊的买入想法变成可验证、可执行、可复盘的交易决策单。主动拉取真实行情/资金/财报/新闻数据，判断交易命题与同业替代，给出目标收益的达成概率区间，并生成高开/平开/低开情景树与分仓方案。内置技术面择时闸门与均线排列过滤，避免在超买区给出买点。
description_en: Trade Buddy — turns a vague buy idea into a verifiable, executable and reviewable trade decision card. It pulls real market, fund-flow, financial and news data, judges the trade thesis and peer alternatives, estimates probability ranges for the target return, and produces a scenario tree with position sizing. Includes a technical timing gate (overbought filter and MA5/20/60 alignment check) so it never recommends entries in overbought zones.
category: 投资理财
version: 1.0.0
author: flybirp
disable: false
agent_created: true
---

# trade-buddy

## 定位

不是诊股器，不是拷问器，也不承诺预测准确。

目标是把用户的一句模糊买入意图，转成一张**有事实依据、可执行、可跟踪、可复盘的交易决策单**。

> 研究员负责找逻辑，交易员负责定计划，风控负责限制亏损，复盘员负责迭代。

## 输入与默认行为

最小输入：

- 标的名称或代码
- 计划买入日期
- 计划价格或价格区间
- 持有期限
- 目标收益率

能从数据获取的内容自己补齐。只允许追问两类无法替代的信息：

1. 用户没给持有期限
2. 用户没给目标收益率

若用户仅说“帮我看XX”，先完成事实研究和当前判断，再问上述最小必要问题；不要空手反问。

### 会话状态与防重复协议（强制）

开始研究前，先在当前请求中建立一张内部状态表，并持续复用，不要因为加载 Skill、调用研究工具、网络重试或任务切换而重新初始化：

| 字段 | 状态 |
|---|---|
| 标的 | `known / missing` |
| 计划日期 | `known / inferred / missing` |
| 计划价格 | `known / market_price / missing` |
| 持有期限 | `known / asked / missing` |
| 目标收益 | `known / asked / missing` |

执行规则：

1. **先读对话历史再判缺失**：用户在当前或前文已经回答过的参数，必须视为 `known`，不得重复询问。
2. **每个请求最多发起一次补充提问**：持有期限和目标收益均缺失时，必须合并在同一次提问中。
3. **提问后立即停止本轮执行**：发出补充问题后，不再拉新数据、不创建重复任务、不再次提问，等待用户下一条消息。
4. **收到答案后锁定参数**：答案写入当前请求状态，后续研究、网络重试和工具切换都不得把它重置。
5. **工具结果为空或用户未作答时不重问**：标记为 `asked`。如需继续交付，改为给出不依赖该参数的初步建仓框架，或按多个期限/目标做情景表；不得在同一请求中再次弹出相同问题。
6. **恢复中断时先查状态**：若流程被打断，先复用已有数据和已问参数，只补未完成步骤，不从头重跑。
7. **数据调用去重**：维护已完成调用清单（数据类型、标的、截止时点）。同一标的、同一时点、同一接口的数据已有有效结果时直接复用，禁止重复调用。

## 内部参考：按需加载，禁止开场全读

**执行前只读 `templates/trade-decision-card.md`**（决定你要收集哪些字段）。

其余 reference 只在对应阶段卡壳时再读，不要预加载：

| 文件 | 何时才读 |
|---|---|
| `references/research-pipeline.md` | 取数口径、来源优先级、时点定义拿不准时 |
| `references/catalyst-and-peers.md` | 判断催化剂评级、同业龙头、是否换标的时 |
| `references/probability-calibration.md` | 写概率区间前 |
| `references/execution-playbook.md` | 写情景树与分仓前（**A 股路径**） |
| **`references/hk-connect.md`** | **标的为 `hk` 前缀时必读**：港股通可交易性校验、能力矩阵、T+0/无涨跌停下的情景树改写、成本模型、南下资金、入通出通事件 |
| `references/futu-datasource.md` | 需要**卖空数据 / 每手股数 / 实时快照 / 盘口逐笔 / 机构持仓**时（需本地 OpenD 可用，不可用时跳过） |

**决策要点已内联，不读 reference 也能完成全部七个步骤：**

- 交易命题：`买入这只股票，本质是在押注 ______ 在 ______ 时间内尚未被充分定价。` 必须可证伪。
- 结论只有四类：`执行 / 等确认 / 放弃 / 建议换标的`。
- 概率三档：`达成目标` / `先触发失效` / `横盘未完成`，合计约 100%，单档区间宽度 ≥10pct，必须给置信度、时间窗口、关键变量、最大不确定性。
- 分仓三段：`试错仓 20–30%` / `确认仓 30–40%` / `加强仓 剩余`，每笔绑定触发条件、预计成本、失效位、账户风险。
- 情景树至少覆盖：`高开 / 平开 / 低开 / 放量突破 / 跌破失效位`。
- 硬输出项：不买条件、加仓条件、减仓条件、止盈、止损、时间退出、数据缺口、References。

## 市场适配（先看代码前缀）

| 前缀 | 市场 | 必读 | 关键差异（不处理就会出错） |
|---|---|---|---|
| `sh` / `sz` / `bj` | A 股 | 无需额外参考 | T+1、有涨跌停、默认人民币 |
| **`hk`** | **港股 / 港股通** | **`references/hk-connect.md`** | **T+0、无涨跌停、港币报价人民币交收、需校验港股通资格、成本 0.3–0.5%** |
| `us` | 美股 | — | 美元、T+0、无涨跌停、时区错配（盘中数据滞后） |
| **`pt`** | **行业板块** | 见下方「板块标的」节 | 板块 ≈ 行业指数：无财报/龙虎榜/筹码/股东，套用指数框架 + 板块专用数据通道 |

**港股通三条硬约束**（详见 reference，此处只列最容易犯错的）：

1. **先校验能不能买**：不是所有港股都在港股通名单里。用 `hkfund` 能否返回 `LgtHoldInfo` 快速反查，再以深交所/上交所名单公告为准。
2. **情景树必须改写**：T+0 → 写「当日情景树」而非「次日情景树」；无涨跌停 → **删除炸板率、连板高度、封单强度、隔日溢价等全部字段**，`execution-playbook.md` 的「触板炸板专项」对港股完全不适用。
3. **成本要进目标收益**：双边费率 0.3%–0.5%，且佣金常有最低收费（如 100 港元），小资金方案可能直接不可执行——按每手股数反推最小建仓金额。

**货币单位**：港股财报 `CurrencyType`/`CurrencyUnit` 字段可能是港币/港元或美元，**展示时一律标注，禁止默认人民币符号**。

### 指数标的（本 skill 默认是给个股设计的，遇到指数必须换框架）

代码形如 `hkHSTECH`（恒生科技）、`sh000300`（沪深300）、`hkHSI`（恒生指数）、`usNDX`（纳指100）。

**个股框架里这些项目全部不存在，禁止留空占位或编造**：财报、公告、龙虎榜、筹码、股东结构、每手股数、市值。

**指数要改用的五个维度**：

| 维度 | 取什么 | 常见来源 |
|---|---|---|
| **估值与分位** | PE(TTM)、PB、历史分位、历史中枢 | 券商策略研报 / 指数公司月报（结构化接口常返回 0，须换源） |
| **盈利增速** | 指数 EPS(TTM) 同比、卖方对未来年度的 EPS 预期 | 券商研报（注意：卖方预期与实际常差 2–4 倍，必须并列） |
| **成分股结构** | 成分数量、前 N 大权重合计、单股上限、行业分布 | 指数公司公告 / 券商研报 |
| **历史区间** | 历史最高/最低点、牛熊幅度 | `kline`，**注意结构化月K可能回溯不足，须用历史区间查询补齐** |
| **可交易载体** | 对应 ETF 的规模、日成交额、**折溢价**、跟踪误差、申购赎回状态 | `etf` / `search <指数名>ETF` |

**目标点位测算（指数专属公式）**：

```
目标涨幅 = (1 + EPS 年增速)^年数 × (期末 PE ÷ 当前 PE) − 1
```

必须做成**情景表**：列出不同 EPS 增速下「达成目标所需 PE」，并标注该 PE 在历史上的位置（低于中枢 / 中枢 / 高于中枢 / 泡沫区）。**这一步是整个判断的核心**，不要只给一个概率数字。

**指数不可直接交易**，必须给出可交易载体，并写明 ETF 的三项特有风险：

1. **溢价风险**（最重要）：QDII 额度用尽时会暂停申购 → 二级市场溢价飙升。溢价 >1% 警惕，**>3% 坚决不买**。
2. **跟踪误差**：看招募说明书的跟踪偏离度目标（常见为日均 ≤0.3%、年化 ≤2%）。
3. **交易规则**：A 股跨境 ETF 遵循 A 股 T+1，即使底层是 T+0 的港股。

**其他调整**：

- **不设百分比止损**：宽基指数三年的正常回撤就是 -30% 到 -38%，百分比止损会被震出局。改用**逻辑失效**（指数 EPS 连续两年负增长）+ **历史极端位**（跌破历史低点）。
- **止盈用 PE 分位而非点位**：「PE > 45 倍无条件减半」比「涨到某点位减半」更可靠。
- **建仓用分批/定投**：不要一次性重仓，留足子弹应对 -30% 回撤。

### 板块标的（`pt` 前缀）与板块轮动类 query

板块代码形如 `pt01801193`（证券Ⅱ）、`pt01801016`（种植业），从 `hot board` 输出中获取。**板块本质上是无财报的行业指数**：上面「指数标的」的五维度框架直接适用，但数据通道有一半要换。

**板块专用数据通道（实测可用）**：

| 需要 | 命令 | 说明 |
|---|---|---|
| 全市场板块涨幅排名 | `board` | 当日 + 5日/20日 涨幅、换手率、领涨股，一次全量 |
| 板块热度与 `pt` 代码 | `hot board --limit N` | 热度排名，`symbol` 列即 `pt` 代码 |
| 板块历史K线 | `kline pt01801193 --period week --limit N` | 回溯任意区间涨幅/位置 |
| 板块分时 | `minute pt01801081` | — |
| 板块代理 ETF | `etf` / `etf-holdings` | 行业 ETF 可作为可交易载体与资金观察口径 |

**板块数据缺口（无接口，须降级并在数据缺口标注）**：板块 PE/PB 及历史分位、板块成分股业绩汇总——只能走券商研报（L2/L3），置信度相应下调。指数公式（EPS × PE 测算）对板块同样受此限制。

**板块轮动类 query 的转译规则**（如「3 个月内哪个板块涨幅最高」）：

这是**排序问题**，不是单标的达成问题。禁止硬套单一目标的概率框架，按四步转译：

1. **初筛用批量排行，但必须双通道**：`board` + `hot board` 之外，**还要拉候选的周K算位置**（52周涨幅、距高点、20日涨幅）。**禁止只用动量排名做初筛**——`board` 按当日涨幅排序，天然偏向已加速的板块；「未来 3 个月涨幅最高」更可能来自**低位刚启动**（5日动量强但 20日温和、距 52 周高仍有空间）的板块。初筛候选池 6–8 个，须同时包含「强动量型」和「低位启动型」两类。
2. **排序用赔率优先，不是动量外推**：板块动量的半衰期通常只有 1–2 个月，3 个月窗口内「过去 20 日最强」对「未来最高」的解释力有限，反而意味着获利盘沉重。排序权重：**位置低 + 催化新 + 弹性大 > 趋势强但已透支**。已大涨板块（20日 >20%）排在第一位的，必须写出「为什么未来 3 个月还能延续」的独立理由，不能只靠惯性。
3. **板块同样过择时闸门（强制，与个股同一条规则）**：每个候选板块（含排序第一的）都必须拉 K线算 **MA5/MA20/MA60 排列**，并检查是否超买。**空头排列的板块直接降级或排除**（反弹 ≠ 反转）；**多头排列但短期乖离过大的，排第一时必须单独写明「短期回调风险」**，不能只靠「趋势强/资金流入」。排序是「中期赔率排序」，买入时点仍要过短期技术面闸门——两者分开，禁止混用。
4. **每个候选板块做一套简化条件表**：问题改写为「3 个月涨幅进入全市场前 3 名」。表 A 达成条件（动量延续/景气催化/资金持续流入），表 B 阻断条件（拥挤度过热/催化证伪/资金撤退），观测方式照常填写。每个候选给出「达成 / 先失效 / 横盘未跑赢」三档概率（单板块合计约 100%），比只给单档达成概率信息量大。
5. **输出相对排序 + 并列概率区间 + 执行层**：禁止单一板块伪精确胜率；必须有条件式执行表（具体点位：止损位/加仓触发位、可交易 ETF 载体、时间退出）——只回答「谁概率高」不回答「怎么办」是不完整的输出。

**禁止轻率排除候选**：某板块「单日爆发、持续性存疑」不是排除理由，是「列为候选但要求资金/位置验证」的理由（等确认）。排除一个板块必须基于证据（催化证伪 / 资金持续流出 / 趋势破位），不是基于「我没来得及验证」。

**诚实原则**：3 个月板块轮动是公认难题。宁可输出「基于当前位置（涨幅分位、拥挤度）+ 景气 + 资金的相对占优排序，置信度低」，也不要给一个看起来精确的错误答案。

## 数据依赖

### 客户端适配（先看这个，避免误判数据源缺失）

数据源分两层。**在 Codex / CodeBuddy 等非 WorkBuddy 客户端上，A 层全部可用**——不要因为找不到同名 skill 就判定"本机没有数据 CLI"：

**A 层 · 公开 CLI（任何有 Node.js ≥16 的环境直接跑，与客户端无关）**

| 数据源 | 命令形态 | 说明 |
|---|---|---|
| westock | `npx -y westock-data-clawhub@1.0.4 <命令>` | 公开 npm 包（registry.npmjs.org，腾讯自选股团队维护）。WorkBuddy 的 `WeStock Data` skill 本质就是包着这个 CLI 的文档——**没有该 skill 不等于没有数据源** |
| futu（可选） | `python3 scripts/futu_quote.py <命令>` | 随本 skill 发布；需 `pip install futu-api` + 本地 OpenD |

**B 层 · WorkBuddy 环境专属 skill（Codex 上不存在，按右列降级）**

| Skill（仅 WorkBuddy） | Codex / 其他客户端的替代 |
|---|---|
| `WeStock Data` | 直接跑 A 层 westock CLI（该 skill 内部就是这么实现的） |
| `wb-finance-skill + agentic_search` | WebSearch / WebFetch 检索行业景气、同业候选 |
| `tencent-news` | WebSearch 检索新闻与催化剂 |
| `cninfo-stock-data` / `cninfo-filing-scraper` | WebFetch 巨潮资讯网（A股公告）/ 港交所披露易 HKEXnews（港股公告）；或 WebSearch 检索公告原文 |
| `量能异动监控` | 用 westock `kline` 原始量价自行计算量比/振幅 |

**禁止**因某数据源在当前客户端不可用而中断分析：降级到替代方案，并在「数据缺口」标注未交叉验证的部分。

### 核心数据

| Skill/能力 | 用途 |
|---|---|
| `wb-finance-skill + agentic_search` | 行业景气、产业链、同业候选、市场环境、反向证据；委派必须是一句话高层意图 |
| `WeStock Data`（即 westock CLI） | 标的身份、日/周K、分时、技术指标、资金、龙虎榜、筹码、财务、股东、分红、业绩预告、板块 |
| `tencent-news` | 公司、行业与政策新闻，校验催化剂新鲜度 |
| `cninfo-stock-data` | 巨潮公告主源：公告、定期报告、深市调研记录、原文链接/PDF |
| `cninfo-filing-scraper` | 公告兜底：按代码、日期、类型过滤及原文核验 |

公告只提供事实，不照抄任何自动利好/利空评级。

### 增强数据（条件启用，非默认）

`scripts/futu_quote.py`（富途官方 SDK `futu-api` + OpenD）**仅在 westock 拿不到时启用**，用于补齐：

- **卖空数据**：`shortvol`（每日卖空量）/ `shortinterest`（空头持仓淡仓）/ `shortselling`（市场卖空排行）—— 港美股做空是合法力量，缺这个等于少看一半战场
- **每手股数与实时市值**：`snapshot` / `basic` —— 港股 `profile` 无 `regCapital`，靠这个直接拿，不用财报反推
- **盘口与逐笔**：`capitaldist` / `capitalflow` / `rt_ticker` / `order_book`
- **机构持仓与内部人交易**：`holder` / `insider`

**调用纪律**：

1. **必须先跑 `probe`**（退出码 0 才继续，3 表示 OpenD 未运行 → 整层跳过，禁止重试）
2. 代码格式是 `HK.03888` / `SH.600000` / `US.AAPL`（与 westock 的 `hk03888` 不同，注意转换）
3. 未启用时在数据缺口标注「卖空/盘口数据缺失」，并下调置信度
4. **禁止**因为富途不可用而中断分析

详见 `references/futu-datasource.md`。

### 条件数据

`量能异动监控` 仅在涉及低位放量或横盘缩量时调用。只采用量比、振幅、价格分位、均线距离等原始计算；“吸筹”“洗盘结束”视为待验证假设。

### 禁用数据

- `0AMV活跃市值`
- `形态识别 daily_top/weekly_bottom`

已确认口径或结果不可靠，任何情况下不得调用。

### 已知无效调用（踩过的坑，不要重试）

| 调用 | 实际表现 | 替代方案 |
|---|---|---|
| `westock quote` | 命令直接返回“当前渠道不可用” | 现价取最新日K收盘，标明“最近收盘”；**市值 = `profile` 的 `regCapital`(万股) ÷ 10000 × 现价**（已用国科军工 103.98 亿交叉验证） |
| `westock search <中文板块名> --sector` | 返回空，对中文板块名无效 | 板块K线直接 `kline pt02GN2306`；**板块代码与排名从 `board` / `hot board` 获取**；候选池靠行业检索 + 已知标的清单 |
| **板块**估值分位 / 板块业绩汇总 | 无接口（指数 PE 都返回 0，板块更没有） | 券商研报（L2/L3）+ 数据缺口标注，置信度下调 |
| `westock reserve` | 只回 `code/name/market` 空壳，无业绩预告数据 | 当年中报/预告走公开财报检索 |
| `westock finance` | **结构化财务只更新到上一年度年报**，当年中报缺失 | 当年 H1 数据必须走公开检索补，并在数据缺口中标注“未与结构化接口交叉验证” |
| `asfund` 输出解析 | `MarginTradeInfos`/`LhbInfos` 字段是内嵌 JSON，朴素按 `\|` split 会错位 | 必须按表头行建 dict 再取字段，不要靠列序号硬编码 |
| **港股** `chip` / `lhb` / `blocktrade` / `margintrade` | 分别报「仅支持沪深A股」「当日无龙虎榜数据」「仅沪深」 | 见 `references/hk-connect.md` 第 1 节能力矩阵，**不要重试** |
| `futu` `OpenQuoteContext` 失败时 | **无限重连**（实测 19 次以上不退出），会卡死整个流程 | 构造连接**前**先 socket 探测 11111 端口；`scripts/futu_quote.py` 已封装，不可用时 0.04 秒返回退出码 3 |

### 调用效率硬约束

1. **批量优先**：`kline` / `profile` / `finance` / `asfund` / `chip` / `technical` 全部支持逗号分隔多代码，**必须一次批量拉完**，禁止逐个循环。
2. **并行优先**：互不依赖的查询必须在同一条消息里并行发出（板块K线 + 个股K线、profile + finance、多路 WebSearch）。
3. **取数预算**：
   - 横向扫描 **≤ 8 只**（够了就停，不要凑数铺满板块）——**例外：板块轮动类 query 的初筛用 `board` / `hot board` 批量排行，一次覆盖全市场，不受此限**；8 只预算只约束个股深度挖掘
   - 深度挖掘 **≤ 3 只**（日/周K、资金、筹码、财报、公告、新闻）
   - 超出预算必须先说明为什么
4. **任务管理最小化**：产出一份决策单是单目标，**任务 ≤ 3 个**。不要为七个步骤各建一个任务，更不要最后一次性批量标 completed——那是纯开销。
5. **agentic_search 用法规约**：它是最慢的单点，且会把板块成分股全表塞进上下文。委派时必须限定范围，例如“研究 X 行业景气与产业链格局，并列出不超过 10 个 A 股主要标的”，**禁止**使用“覆盖以下方面”“尽可能全面”这类扩范围措辞。

## 工作流

### 1. 建立事实快照（两阶段，禁止一口吃成胖子）

**阶段 A · 横向扫描（≤8 只）**：只用周K + 市值 + 最近一期年报，算出现价、52 周涨幅、距高点回撤、PE。目的是排序和淘汰，不是研究。

**阶段 B · 深度挖掘（≤3 只）**：对阶段 A 胜出的 2–3 只才拉日K、资金、龙虎榜、筹码、多期财报、公告、新闻。**阶段 A 被淘汰的标的不再取任何额外数据。**

用户已指定标的时跳过阶段 A，但仍需 3–5 只同业做比较（同样只用阶段 A 的轻量数据）。

输出前完成：

- 标的身份与交易时点确认
- 行情、财务、公告、新闻、行业、同业、市场环境交叉验证
- 关键事实与反向证据分开
- 标记数据截止时间和数据缺口

禁止开场数据轰炸；只保留影响交易决策的事实。

### 2. 用户边际信息审计（条件分支）

当用户提出常规数据覆盖之外的低估点、催化点、产业信息、渠道观察或反共识判断时，**必须单独开一条验证分支**，不得直接接受，也不得因为通用检索没找到就直接否定。

先把用户说法改写成可检验命题：

> 如果用户观察为真，那么它应通过 ______ 传导到公司 ______，并在 ______ 时间窗口反映到 ______ 指标或事件。

按四层证据主动验证：

1. **事实层**：公告、监管文件、公司回应、产品/订单/价格/产能等是否支持“事情真实发生”。
2. **归因层**：目标公司是否真实受益，业务占比、持股比例、客户关系、利润弹性是否足够；防止“行业有利好 = 这家公司受益”。
3. **定价层**：市场是否已经知道并交易；检查首次披露时间、此前涨幅、成交量、同业反应和研报覆盖。
4. **反证层**：主动寻找最强反例、替代解释与证伪条件；区分“线索未被证实”和“线索已被证伪”。

根据证据输出五种状态：

- **已证实且未充分定价**：进入核心交易命题，上调催化与目标可达性权重。
- **已证实但已充分定价**：事实成立，但不再构成新买点。
- **部分成立 / 传导较弱**：保留为观察变量，只提高跟踪优先级，不提高仓位。
- **暂无法验证**：建立待验证清单和下一证据节点；不得当作事实进入概率模型。
- **已证伪**：从交易命题剔除，并明确指出错误发生在哪一环。

用户原话、检索证据、反证和最终状态必须分栏记录，保证可追溯。详细规则见 `references/catalyst-and-peers.md` 的“用户非共识假设审计”。

### 3. 收敛交易命题

完成这句话：

> 买入这只股票，本质是在押注 ______ 在 ______ 时间内尚未被充分定价。

然后判断：

- 催化剂强度、新鲜度、业绩传导、持续性与定价程度
- 行业景气是否真正传导到公司
- A股同赛道稀有性
- 产业龙头、业绩龙头、情绪龙头与弹性标的分别是谁
- 原标的是否值得保留，还是应该换成更优标的

发现非龙头不机械换票；统一比较质量、赔率、催化和位置后再下结论。

### 4. 判断目标收益可达性

LLM负责综合理解，数据负责校准，禁止裸报概率。

- 用历史波动、目标距离、压力区和板块弹性建立可达性基础锚
- 用技术、资金、催化、行业、基本面、同业和市场环境修正
- 用数据缺失、信息冲突和事件风险做不确定性折扣

输出三种概率区间：

1. 达成目标
2. 先触发失效/止损
3. 横盘至期限仍未完成

必须同时给置信度、时间窗口、关键变量和最大不确定性。

**给概率之前，先写两张表**（格式见 `references/probability-calibration.md`）：

- **表 A 达成条件清单**：要达成目标，必须满足哪些条件？每条绑定「可观测指标 + 达标阈值 + 观测时点」，并写明条件之间是 AND 还是有多组 OR 路径。
- **表 B 阻断条件清单**：哪些事一旦发生，目标就达不成？同样绑定指标与阈值。

**概率是这两张表数出来的，不是感觉出来的。** 表 A 里「未达成」越多概率越低，表 B 里「已触发」越多折扣越大。

注意区分四个概念：**反向证据**（已发生的坏事）/ **阻断条件**（未来不能发生的事）/ **证伪条件**（推翻命题的那一条）/ **止损**（执行开关）。别把它们写成一坨。

### 5. 生成条件式执行方案

#### 择时闸门（技术面，强制——先于一切买点结论）

**中长期逻辑决定「选哪个池子」，但「明天能不能买」由短期技术面决定，二者不能混用。** 前两次教训：内蒙一机 9/2 收盘 13.41（三连板+一字板，RSI_2=98.8、KDJ_J=117.7、CCI=288、BIAS_6=14.8%、年线 15.58 仍在头顶）、工行 9/2 收盘 8.22（放量创新高，RSI_2=96.4、KDJ_J=94.3、CCI=179、获利盘 99.8%）——都是全面超买却给了追高/创新高买点。

给出任何「执行 / 等确认」的买点结论前，**必须先拉 `technical` + `chip`**，检查短期是否超买/透支：

| 维度 | 取值 | 个股超买阈值 | 权重股阈值 |
|---|---|---|---|
| RSI_2 / RSI_6 | `technical` | >90 / >80 | >90 / >75 |
| KDJ_J | `technical` | >100 | >95 |
| CCI_14 | `technical` | >100（>200 极强） | >100 |
| BIAS_6 乖离率 | `technical` | >10% | >5% |
| 收盘 vs 布林上轨 | `technical` | 收盘突破上轨 | 突破上轨 |
| 筹码获利盘 | `chip` | >95% | >95% |

**均线排列（MA5 / MA20 / MA60，趋势结构——与超买互补，二者都要查）**：

超买判断「短线是否透支」，均线排列判断「趋势是否成立」。**先看排列，再看超买**——空头排列下谈超买回调没有意义，因为趋势本身向下。

| 排列状态 | 判定 | 含义 | 买点要求 |
|---|---|---|---|
| **多头排列**（MA5>MA20>MA60 且 MA5/MA20 向上） | 趋势成立 | 唯一可讨论「回踩买点」的状态 | 回踩 MA5/MA20 缩量企稳再介入 |
| **空头排列**（MA5<MA20<MA60） | 趋势向下 | 任何反弹按「超跌反弹」处理，禁止趋势反转假设 | 只能超跌企稳 + 均线收敛，禁止突破追多 |
| **均线粘合/纠缠** | 方向未明 | 等方向选择 | 不给「执行」 |
| 多头排列但乖离过大 | 趋势在、短线透支 | 短期超买，需修复 | 回踩 MA5/MA20，不追高 |

**判定规则**：

1. **命中 ≥3 项超买 → 结论自动降级为「等确认」**，买点必须是「回调到支撑位 + 超买指标修复（RSI 回落、KDJ 高位死叉后金叉、缩量止跌）」，**禁止在超买区给「执行」**。
2. 趋势再强、资金再流入，只要技术面全面超买，短期买点一律推迟——「中长期看多」与「明天能买」是两回事。
3. **板块排序同样强制过择时闸门**：挑选/排序板块时，同样必须拉板块 K线算 **MA5/MA20/MA60 排列** + 相对位置，并检查是否超买。候选板块「20 日涨幅已高 + 均线多头但乖离过大 + 日线 RSI/KDJ 全面超买」的，即使动量最强也只能降级为「等确认」，排第一时必须单独写明短期回调风险，禁止只靠「趋势强/资金流入/热度高」。
4. 年线（MA250）方向是中长期趋势的最后确认：现价仍在年线下方时，任何反弹都先按「超跌反弹」处理，不得当作趋势反转。
5. **均线排列是趋势的第一道闸门**：空头排列下不给「执行」，多头排列才谈回踩买点；个股与板块同用此规则。

不是预测明天涨跌，而是覆盖明天可能出现的路径：

- 高开怎么办
- 平开怎么办
- 低开怎么办
- 放量突破怎么办
- 跌破失效位怎么办

> **港股通例外**：T+0 且无涨跌停，改为写「**当日情景树**」——开市前竞价方向 → 早盘突破/回落 → 午盘 13:00 重启 → 收市竞价 16:00–16:06。删除炸板/连板类字段。见 `references/hk-connect.md` 第 3 节。

默认2–3笔分仓。每笔写清：触发条件、仓位、预计成本、失效位、账户风险。仓位由风险预算和止损距离反推，不由主观信心决定。港股通还需写明每笔的**最小可行金额**（每手股数 × 股价）与**双边成本**。

止损必须结合实际成交成本和结构位；禁止只锚定昨日开盘价。必须给时间退出和不买条件。

### 6. 生成 Markdown 交易决策单

严格使用 `templates/trade-decision-card.md`，将完整结果写入当前工作区：

```text
outputs/{{标的}}交易决策单-{{YYYYMMDD}}.md
```

默认只生成 `.md`，**不得生成 HTML、内联 CSS、JavaScript、SVG 或图表代码**。不要读取 `html-report-style.md`，也不做 HTML/JS 渲染校验。只有用户明确要求“HTML/网页/可视化报告”时才能切换格式。

**写完后必须按「引用规范」逐条自检**：每个数字有没有 `[n]`、五要素齐不齐、L3 有没有隔离、来源冲突有没有并列、有没有模糊归因。这一步不能跳过。

Markdown 以决策信息密度优先：保留结论、关键事实、概率、情景树、分仓、退出条件和 References；避免重复公司介绍、装饰性文案和同一事实多次复述。

结论只选四类：

- 执行
- 等确认
- 放弃
- 建议换标的

结论后必须附理由、数据缺口与 Reference。生成文件后，聊天正文只给5–10条核心结论，并交付 `.md` 文件。

### 7. 后续跟踪与复盘

用户回来更新价格或持仓时，不从头重写；基于原计划检查：

- 哪些催化已兑现或证伪
- 哪些盘中条件已触发
- 达成/失败/横盘概率如何调整
- 应继续、加仓、减仓、退出还是换标的
- 最终归因：选股、择时、仓位还是执行错误

## Pitfalls

### 重复追问与重复取数

错误表现：完成研究后询问“持有期限/目标收益”，但没有将问题状态锁定；随后又继续调用同业、指数等工具，恢复流程时把缺失参数重新判定一遍，于是重复弹出同一问题。网络中断、Skill 二次加载和重复创建任务会放大该问题。

纠正方式：始终使用“会话状态与防重复协议”。`asked` 也是有效状态；一旦提问就结束本轮，下一轮先合并用户答案，再继续生成决策单。不得依赖任务列表保存交易参数，任务只用于进度管理。

### 输出格式膨胀

错误表现：为了排版生成大段 HTML/CSS/JS，使同一份交易决策单消耗大量 token，同时增加渲染和校验成本。

纠正方式：默认使用 Markdown 模板并写入 `.md` 文件；只有用户明确要求网页或可视化报告时才生成 HTML。

### 取数与流程开销失控

错误表现：为了“保险”把候选池铺到 30 只、为七个步骤各建一个任务、重复试探不可用命令、财报分两批拉且标的重叠。实测一次决策单跑到 52 次工具调用，其中 27 次不产生决策价值——候选池里 23 只从未进入比较表，14 次任务管理调用对“产出一份 md”零贡献。

纠正方式：

1. 严守**取数预算**（横向 ≤8、深度 ≤3）和**任务管理最小化**（≤3 个）。
2. 开跑前先查“已知无效调用”表，跳过所有已确认无效的命令。
3. 批量 + 并行是默认动作，不是优化动作。批次之间先想清楚字段需求，避免同一批标的拉两次（财报尤其容易分批重叠）。
4. 解析脚本写错导致的重跑，与数据调用同等代价——按表头建 dict，别硬编码列序。

## 引用规范（论文级，强制）

目标是让决策单里的每一句话都能被独立核验。这是写论文的标准，不是写研报的标准。**任何没有引用序号的关键数据，都不许出现在决策单里。**

### 1. 每个关键事实挂序号

正文、表格、结论、反向证据里的每个数字与事实，句末或单元格内挂 `[n]`，对应文末 References。

### 2. Reference 五要素缺一不可

```text
[n] 媒体/机构. (日期). 《标题》. 关键数据点. URL · 证据等级
```

- **日期**用披露日或发布日；财报必须区分报告期与公告日
- **关键数据点**写具体数值，不写"业绩向好""表现亮眼"
- **URL** 能找到原文链接的必须给；结构化接口取数写清接口名与取数时点

### 3. 禁止模糊归因

以下写法一律禁止：据报道 / 有数据显示 / 市场传闻 / 业内人士称 / 据第三方测算（除非同时说明测算方与口径）/ 机构普遍认为 / 市场普遍认为。

### 4. 证据分级（决定这条数据能走多远）

| 等级 | 来源 | 允许用途 |
|---|---|---|
| **L1 一手** | 交易所与公司公告原文、定期报告、监管文件；结构化行情/财务接口（注明接口名与取数时点） | 可进入交易命题、概率模型、仓位计算 |
| **L2 二手** | 券商研报、权威财经媒体、机构一致预期 | 可进入证据修正，须注明发布机构 |
| **L3 传闻** | 投资者互动平台推测、社区、"据第三方测算"、无公告确认的订单/客户/份额/产能信息 | **只能列为待验证线索，不得进入核心假设与概率模型** |

L3 必须进「待验证线索」表单独隔离，写明**与一手数据的矛盾点**和**下一验证节点**（哪个公告/财报能证实或证伪）。

> 典型陷阱：某公司"在手订单超 15 亿"来自第三方测算，但机构一致预期当年营收只有 5.49 亿——两者矛盾，说明该订单要么不属实，要么交付周期远长于一年。**这类交叉验证必须做，不能照单全收。**

### 5. 反向证据同样要带引用

反向证据不是免责声明，是可核验的事实，每条都要有 `[n]`。

### 6. 来源冲突不许静默选择

同一指标两个来源打架（如板块估值分位）时，并列两个来源、各自标注，在「数据缺口」写明"冲突未解决"，并下调置信度。

### 7. 数据缺口显式列出

拿不到的、滞后的、未与第二个来源交叉验证的，全部进「数据缺口」。不许用模糊表述填补。

## 硬规则

- 默认输出文件必须是 `outputs/*.md`；除非用户明确要求，否则禁止生成 HTML/CSS/JS/SVG
- 每个关键数据必须挂 `[n]` 引用且五要素齐全；**无引用的数据不得进入决策单**
- 无公告确认的订单/份额/客户/产能信息一律标记 L3，进「待验证线索」表，**不得进入交易命题与概率模型**
- 同一请求中，同一组补充问题最多询问一次；发问后必须结束本轮并等待用户
- 同一标的、同一时点、同一接口的有效数据不得重复拉取
- 不向用户索要能自行查到的信息
- 通用检索搜不到用户线索，不等于线索为假；必须区分“未公开/覆盖不到/关键词不对”与“已证伪”
- 不把另一个 Skill 的判断标签当事实
- 不把昨收写成实时价；当前 `quote` 不可用时用最新日K并标明“最近收盘”
- 通用检索不等于实时行情数据库；炸板率、连板高度、隔日溢价拿不到就标缺失并降低置信度
- 不用“主力吸筹”“庄家洗盘”等不可验证叙事替代证据
- 不写"逢低布局、注意风险"，必须是条件 + 动作
- 短期买点结论前必须过「择时闸门」：拉 `technical` + `chip`，命中 ≥3 项超买时结论自动降级为「等确认」，禁止在超买区给「执行」
- 趋势判断必须先看 MA5/MA20/MA60 均线排列：空头排列下不给「执行」、不把反弹当反转；多头排列才谈回踩买点。个股与板块同用此规则
- 板块挑选/排序与个股一样强制过择时闸门：空头排列降级或排除，多头排列但乖离过大时排第一必须写明短期回调风险
- 不保证收益，不给伪精确概率
- 任何关键数据都附来源、日期、标题、关键数据点和URL，并标注证据等级
- 禁止"据报道""有数据显示""市场普遍认为"等模糊归因
- 港美股必须标注货币单位（港元/美元），禁止默认人民币符号；港股通还需校验标的是否在港股通名单内
- 港股不得输出炸板率、连板高度、封单强度、隔日溢价等依赖涨跌停制度的字段
- 港美股分析须尽力取得**卖空数据**（卖空量/空头持仓）；确实拿不到时在数据缺口写明并下调置信度
- 使用富途层前必须先 `probe`；OpenD 不可用时整层跳过，不得重试、不得中断分析

## Verification

交付前逐项检查：

- [ ] 最终文件是否为 `outputs/*.md`，且没有无要求生成HTML/CSS/JS/SVG
- [ ] 是否先合并当前对话中的已知参数，并建立输入状态表
- [ ] 本请求是否只发起过一次补充提问，且没有重复拉取同一数据
- [ ] 输入的标的、日期、价格、期限和目标收益是否明确；若未明确，是否改用情景表而非重复追问
- [ ] 是否读取一手公告并检查负面事件
- [ ] 用户若提出非共识线索，是否完成事实/归因/定价/反证四层审计并标记状态
- [ ] 是否给出可证伪的一句话交易命题
- [ ] 是否比较同业并判断是否需要换标的
- [ ] 概率是否有基础锚、证据修正和不确定性折扣
- [ ] 是否覆盖高开/平开/低开情景
- [ ] **是否已拉 `technical` + `chip` 过「择时闸门」；命中 ≥3 项超买时结论是否已降级为「等确认」而非「执行」**
- [ ] **是否已检查 MA5/MA20/MA60 均线排列；空头排列下是否未给「执行」、未把反弹当反转**
- [ ] **板块挑选/排序是否与个股一样过了择时闸门 + 均线排列；空头排列是否已降级/排除，多头排列但乖离过大是否写明短期回调风险**
- [ ] 每笔仓位是否绑定触发条件、成本与风险
- [ ] 是否写明不买条件、止损、止盈和时间退出
- [ ] 数据缺口是否显式披露
- [ ] 是否使用固定免责声明
- [ ] 横向扫描是否 ≤8 只、深度挖掘是否 ≤3 只、任务是否 ≤3 个
- [ ] 是否已跳过“已知无效调用”表中的全部命令
- [ ] 批量与并行是否已用满，有无同一标的重复拉取
- [ ] **是否每个关键数据都挂了 `[n]` 序号，正文无裸数据**
- [ ] **References 五要素是否齐全（来源/日期/标题/关键数据点/URL）并标注 L1/L2/L3**
- [ ] **无公告确认的订单/份额/客户信息是否隔离在「待验证线索」表，未混入概率模型**
- [ ] **传闻类数据是否与一手数据做过交叉验证，矛盾点是否写明**
- [ ] **反向证据是否同样带引用**
- [ ] **来源冲突是否并列标注而非静默选择**
- [ ] 是否出现“据报道/有数据显示/市场普遍认为”等模糊归因（出现即打回）
- [ ] **港美股：是否标注货币单位；港股通是否已校验标的可交易性**
- [ ] **港股：是否删除炸板/连板/隔日溢价字段，情景树是否已改为当日**
- [ ] **港美股：是否已尽力取卖空数据，或在数据缺口中标注缺失**
- [ ] **富途层：是否先 `probe` 再调用；不可用时是否正确降级而非中断**
- [ ] **是否输出「达成条件清单」，每条绑定可观测指标 + 达标阈值 + 观测时点**
- [ ] **是否写明条件之间是 AND 还是多组 OR 路径**
- [ ] **是否输出「阻断条件清单」（前瞻排除项），而非只用反向证据（回顾性）代替**
- [ ] 阻断条件是否每条都有「为什么会阻断」的传导链与可证实阈值
- [ ] 概率是否由这两张表推导得出，而非直接给出
- [ ] **两张表的「观测方式」列是否都填了**，且命令实际跑通过（这是 trade-watchdog 自动巡检的前提）
- [ ] 查不到的指标是否诚实标注「人工:」或「不可观测:」，而非编造命令
- [ ] **非 WorkBuddy 客户端上：是否直接用 `npx -y westock-data-clawhub@1.0.4` 取数，而非误判"本机没有 westock/cninfo CLI"后空转翻找**
- [ ] **板块轮动类 query：初筛是否双通道（board 排行 + 周K位置）；候选池是否同时含「强动量型」和「低位启动型」；排序是否赔率优先而非动量外推；输出是否为「三档概率 + 相对排序 + 条件式执行表（点位/载体/时间退出）」而非单一板块伪精确胜率**
