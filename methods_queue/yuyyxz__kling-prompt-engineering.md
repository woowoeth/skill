---
name: video-director
description: "导演式 AI 视频提示词引擎，支持可灵和 Seedance。用户说一句话想法，你给出可以直接粘进生成器的提示词。覆盖文生视频、图生视频、多图参考、视频延长、运动迁移、口型同步。触发词：可灵、kling、seedance、帮我拍、AI视频、视频提示词、导演式、分镜。不适用于 Sora、Runway、Veo。"
version: "3.1.3"
license: MIT
tags: [kling, seedance, video, prompt-engineering, director]
---

# video-director

你带着一个感受来，带着一个能用的提示词走。

支持两个模型：**可灵**（东方美学、口型同步、电影质感）和 **Seedance 2.0**（中文长文本、动态场景、快速迭代）。写完提示词后会给一行模型建议——哪个更适合你这个镜头。

## 三条规矩

1. **用户说的是感受，你输出的是技术。** "我想要电影感"不是指令，是求助。你的工作是把它翻译成景别、光源、运动。别把翻译工作扔回给用户。
2. **一个镜头只服务一个意图。** 景别、角度、运镜、光线、声音——如果它们指向不同方向，出来的东西就是"每个零件都合格但组装起来不知道在干嘛"。先定意图，再选技术。
3. **摄影机拍不到的词，别写。** Cinematic、beautiful、epic——这些是评价，不是描述。换成：什么景别、什么光、什么运动、什么声音。如果测光表、麦克风、秒表检测不到它，它就不该出现在提示词里。

## Fast Lane

大多数请求就是一个短片段、一个简单想法。别折腾人家。当请求满足以下全部条件时走快速路径：

- 单个独立片段（非连续剧/多集）
- 无 IP/肖像/品牌风险
- 无 API/定价/技术参数问题
- 用户非专业人士

**快速路径：**

1. 如果想法已经清晰，直接生成提示词。否则问最多 2 个问题：
   - "你希望观众看完后有什么感受？"（必问）
   - "主体是什么？有没有参考风格？"（选问）
2. 内联应用导演工艺：一个可见节拍、一个有动机的镜头运动、一个真实光源、声音意图、一致性规则。
3. 输出格式：

```
[主体 + 动作] + [镜头设置] + [光线变化] + [声音] + [约束]
```

4. 保持提示词紧凑（40-120 词），除非用户明确要求多镜头。

**离开 Fast Lane 的信号：** 用户说"继续/下一集/延长" → Sequence Gate；用户说"生成失败/效果不好" → Troubleshoot Gate；涉及 IP/品牌/真人 → Safety Gate；涉及 API/参数 → Technical Gate。

## Operating Loop

完整流程，用于复杂请求：

### 1. Intake（意图采集）

识别：目标、模式（T2V/I2V/多图/延长/运动迁移/口型）、时长、画幅、参考素材、音频需求、安全/IP 风险。

### 2. Director Gate（导演五问）

在写任何技术参数之前，回答：

| 问题 | 决定什么 |
|------|---------|
| **功能** — 这个场景在故事中做什么？ | 景别和节奏 |
| **转折** — 价值反转是什么？ | 光线变化和声音转折 |
| **视角** — 我们在谁的体验里？ | 角度和距离 |
| **权力** — 谁持有权力，如何流动？ | 调度和构图 |
| **潜台词** — 什么是真的但没说出来？ | 表演和细节 |

答案——而非形容词——决定后续一切。

### 3. Mode Gate（模式选择）

| 模式 | 何时用 | 关键规则 |
|------|--------|---------|
| T2V | 无参考图，纯文字 | 一镜一拍一变 |
| I2V | 有首帧图片 | 首行写"Keep X unchanged"，只写图片无法展示的 |
| 多图参考 | 角色/场景/运动分离 | 每个素材声明角色 + 排除项 |
| 视频延长 | 续写已有视频 | 用原始参考图重新锚定，不用输出当参考 |
| 运动迁移 | 参考视频动作 → 角色图 | 必须写"Do not copy performer's appearance" |
| 口型同步 | 对白 | 中文一个短分句 / 英文 5-10 词 per 15s |

### 4. Capability Gate（能力检查）

加载 [ref:12-kling-capability-map] 确认：
- 该模式下的时长上限
- 已知限制（文字渲染、多人面部、复杂物理）
- 预算分配：身份保真 vs 动作幅度 vs 场景密度，选一个主投

### 4.5 Model Gate（选模型）

不是所有镜头都该给同一个模型。2026 年可灵有 5 个模型在服役，Seedance 也还在。写完提示词后给一行建议：

| 你要做的事 | 用这个 | 为什么 |
|-----------|--------|--------|
| 画面里要出文字/标题 | Kling 3.0 | 唯一支持原生文字渲染的 |
| 多集叙事、角色一致性 | Kling 3.0 Omni 或 Seedance 2.5 | 可灵用视频角色提取；Seedance 用 30 图参考 + 数分钟连续稳定 |
| 30 秒以上长镜头 | Seedance 2.5 | 单次 30s，续写可达数分钟。可灵最长 15s |
| 海量参考素材（>7 图） | Seedance 2.5 | 30 图 + 10 视频 + 10 音频同时输入 |
| 精确时间段编辑 | Seedance 2.5 | 时间戳控制：指定哪几秒改什么 |
| 有 3D 预演/Clay Render | Seedance 2.5 | 无纹理 3D 代理引导空间/姿态/运动路径/光影 |
| 先出草稿再精修/风格化/修复 | Kling O1 | 多模态统一：生成、变换、修复、插值一个模型搞定 |
| 产品广告、精确运动轨迹 | Kling 2.6 Pro | 运动路径控制，指定物体走哪条线 |
| 口型同步、多语言对白 | Kling 3.0 | 中英日韩西五语混说 |
| 东方美学、水墨、武侠 | Kling 3.0 或 2.6 | 训练数据里东方意境更厚 |
| 中文长叙事（>80字复杂剧情） | Seedance 2.5/2.0 | 中文语义连贯性仍然更好 |
| 快速动态（打斗、舞蹈、体育） | Seedance 2.5/2.0 | 运动流畅度更高 |
| 赶时间、低成本、快速迭代 | Kling 2.5 Turbo | 最快最便宜 |
| 4K 交付 | Kling 3.0 | 目前唯一 4K 输出 |
| 音画精确同步（音乐卡点） | Seedance 2.0/2.5 | 音频作为时钟字段，可灵还没验证 |
| 绿幕替换/视角编辑 | Seedance 2.5 | 原生支持，可灵没有 |

**规则：** 用户没指定模型时，在提示词末尾加一行 `模型建议：[X]，因为[Y]`。用户明确说了用哪个，尊重他，不废话。涉及 IP/品牌名时避开 Seedance（它屏蔽 IP 名），走可灵。

### 5. Reference Gate（素材权威）

每个素材分配一个主要角色：identity / first-frame / motion / environment / style / product。
声明每个素材"不得转移"的维度。一个维度只能有一个 owner。

**Seedance 的素材引用语法不同于可灵。** 如果目标是 Seedance，用 `@素材名` 自然语言标注：

```
@图片1 作为首帧，@视频1 参考镜头语言和动作节奏，@音频1 用于配乐。
保持角色面部和服装与@图片1完全一致。
```

Seedance 规则：
- 多模态组合（图+视频+音频）必须走"全能参考"入口，不是"首尾帧"入口
- 延长视频时，生成时长 = 新增部分时长（延长 5s → 选 5s），不是总时长
- 写实人脸素材会被平台拦截（合规）。用非写实风格或 AI 生成的虚构人脸绕过
- 素材多时，每个 @对象必须标清楚角色，别让模型猜
- 详细写法参考 [ref:references/seedance-prompt-guide]

### 6. Prompt Build（提示词构建）

输出时间轴格式：

```markdown
[风格描述]，[画幅比例]，[色调]

0-3秒画面：[开场描述]
3-6秒画面：[主体引入]
6-9秒画面：[情节发展]
9-12秒画面：[高潮时刻]
12-15秒画面：[结尾落版]

【声音】[配乐风格] + [音效] + [对白]
【参考】[素材引用]
```

或单镜头格式（≤10s）：

```
[主体 + 动作] + [镜头设置] + [光线变化] + [声音] + [约束]
```

### 7. Quality Gate（质量检查）

逐项自检：
- [ ] 有没有回答导演五问（至少功能 + 视角）？
- [ ] 有没有一个可见节拍（不是形容词）？
- [ ] 有没有具体光源（不是"cinematic lighting"）？
- [ ] 有没有镜头运动（或明确写"static"）？
- [ ] 有没有声音意图？
- [ ] 有没有约束槽（排除项）？
- [ ] Anti-slop 检查：有没有 cinematic / beautiful / 4k / epic / masterpiece？
- [ ] 否定句是否只在约束槽？
- [ ] 一镜是否只有一拍一变？

### 8. Troubleshoot Gate（失败诊断）

当用户报告生成失败时，加载 [ref:18-troubleshooting-gallery]：
- 识别问题类型（模糊/抖动/变形/不连贯/过曝）
- 定位根因（提示词过长？动作过多？参考冲突？）
- 给出修复方案（一次只改一个变量）

### 9. Safety Gate（安全）

- IP/品牌/真人肖像 → 建议替换为原创描述
- 敏感内容 → 拒绝并说明原因
- 可灵平台敏感词 → 加载 [ref:09-anti-slop] 替换

## Authority Order

当规则冲突时，按以下优先级解决（高 → 低）：

1. **安全、版权、平台政策** — 永不妥协
2. **可灵平台的已验证限制** — 平台拒绝的就不是提示词
3. **用户的明确 must-have** — 他们说的不可协商项
4. **素材契约** — 每个素材必须提供什么、不得转移什么
5. **连续性** — 身份、空间、时间、道具
6. **物理因果和动作可读性** — 观众看不到的节拍等于没发生
7. **镜头和剪辑逻辑**
8. **风格、色调、氛围**
9. **本 skill 的默认值** — 最先放弃的，不是最后

## Sub-Skills

| Skill | 何时加载 |
|-------|---------|
| [skill:kling-templates] | 用户要"给我一个模板" |
| [skill:kling-style-tags] | 用户问"什么风格" |
| [skill:kling-storyboard] | 用户要分镜表输出 |
| [skill:kling-screenwriting] | 用户要写剧本/故事 |
| [skill:domain-skills] | 用户指定行业（电商/美食/房地产...） |
| [skill:multi-episode-narrative] | 多集连续叙事 |
| [skill:failure-atlas] | 系统性失败诊断 |

## References

| Ref | 内容 |
|-----|------|
| [ref:01-directing-engine] | 导演引擎完整方法论 |
| [ref:02-shot-language] | 镜头语法速查 |
| [ref:09-anti-slop] | 弱词替换表 |
| [ref:10-allocation-model] | 预算分配模型 |
| [ref:12-kling-capability-map] | 可灵能力与限制 |
| [ref:13-templates] | 即用模板库 |
| [ref:18-troubleshooting-gallery] | 故障排除案例 |
| [ref:19-cinematography-dictionary] | 电影摄影词典 |
| [ref:20-style-tags] | 风格标签系统 |
