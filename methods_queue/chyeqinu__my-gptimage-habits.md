---
name: my-gptimage-habits
description: 复刻用户个人的 gpt-image-2 创作习惯（视频封面加字/带参考图封面设计/字幕条人名条挂角批量/绿幕抠图提取/透明底直出设计/多图人物合成/就地改字/PPT单页封面/分镜宫格图/风格复刻）：按用户的提示词模板与参数习惯自动生成中文 prompt，调用 API 直接出图，再按“短句修改+其他不变”的节奏迭代。用户说“按我的习惯/我的风格做…”、“做个视频封面”、“结合我的文案设计封面”、“字幕条/人名条/挂角”、“抠图/绿底/透明底”、“设计片头字幕/开头字幕/透明底字幕”、“把图X的人物加到图Y”、“改一下图里的字”、“PPT封面（单张）”、“分镜图”时使用。整套多页 PPT 若装有 gptimage-ppt 技能则用它。
whenToUse: 用户要求按其个人创作习惯生成图片：视频封面（加字或结合文案纯设计）、字幕条/人名条/挂角批量、绿幕或透明底素材提取、纯文字素材透明底直出设计、多参考图合成、已有图片上的就地改字、单张 PPT 封面、分镜宫格图、仿照参考图风格设计。
---

# My GPT Image Habits（个人创作习惯复刻）

**数据版本 v1.3.1**：312 条真实任务（2026-07-21 → 2026-09-03）提炼 + 新端点实测核实（2026-09-04），规则见 `references/`。
（技能由使用数据驱动，随数据增长迭代，见 `CHANGELOG.md`。）

按用户的历史使用习惯：写 prompt、选参数、出图、迭代。
核心事实：**用户 76% 的任务是基于参考图改图（图生图）**；`quality=auto`、`png` 是默认；
**当前中转站忽略 size 参数，比例靠 prompt 写“（16：9）/（9：16）”控制**；
透明底：**文生图** prompt 写“透明底”（直接返回真透明 PNG），**图生图**“透明底”不生效，
改绿底 + `--unkey`；抠图默认**绿幕**（prompt 写“方便抠图的纯绿色背景”）。

**每条工作流的生成类型（312 条全量核实）：**
| 工作流 | 类型 | 参考图 |
|---|---|---|
| W1 封面 / W3 提取 / W4 合成 / W5 改字 / W8 风格 | **图生图（必带图）** | 封面=截图/素材图；提取=原图；合成≥2 图 |
| W2 字幕条 | 混合 | 首版纯设计=文生图；风格参考/批量改字=图生图 |
| W6 PPT / W7 分镜 | 混合 / 文生图为主 | PPT 带素材图才传；分镜纯设计 |
| **W9 透明底直出** | **文生图（唯一纯文生图设计类）** | 不传图，透明直出只在这里成立 |

→ **W1/W3/W5/W8 永远图生图，prompt 写“透明底”无效**；要透明的封面/字幕文字素材用 W9。

## 依赖

- `python3`（3.8+，无第三方依赖；macOS/Linux 自带或 `apt/brew install python3`）
- 接口：`https://ai-pixel.online/v1/images/generations`，模型 `gpt-image-2`。
  API key 首次使用需配置（`--set-key` / `--configure` 或环境变量 `GPTIMAGE_API_KEY`）。
  若本机装过 `gptimage-ppt` 技能并配过 key（`~/.gptimage-ppt/config.json`），会自动复用，无需再配。

调用脚本前先拿到本 skill 的绝对目录：

```sh
export SKILL_DIR=~/.agents/skills/my-gptimage-habits   # Windows 下用实际绝对路径
```

## 工作流程（严格按顺序）

### 0. 配置 API（首次使用）

```sh
python "$SKILL_DIR/scripts/gimg.py" --show-config
```

- api_key 非“未设置”：继续。
- 未设置：向用户要 key，执行
  `python "$SKILL_DIR/scripts/gimg.py" --set-key <key> [--set-base-url <url>]`
  （保存到 `~/.gptimage/config.json`；也可用环境变量 `GPTIMAGE_API_KEY`。）
  key 未配好之前不出任何图。

### 1. 识别任务类型（对号入座）

| 用户意图 | 工作流 | 参考 |
|---|---|---|
| 视频封面加字 | W1 | `references/prompt-recipes.md` W1 |
| 字幕条/人名条/片头片尾字（含批量） | W2 | 同上 W2 + `references/workflows.md` SOP-2 |
| 提取元素/抠图/绿底/透明底 | W3 | 同上 W3 + SOP-3 |
| 把图B人物合进图A/群像/换人 | W4 | 同上 W4 + SOP-4 |
| 图里的文字改成别的 | W5 | 同上 W5 + SOP-5 |
| 单张 PPT 封面 | W6 | 同上 W6 + SOP-6 |
| 分镜图（N 宫格） | W7 | 同上 W7 + SOP-7 |
| 仿照某图风格设计 | W8 | 同上 W8 + SOP-8 |

整套多页 PPT（.pptx）→ 若本机装有 `gptimage-ppt` 技能则转用它；没有则告知用户本技能只做单张封面。

### 2. 按模板写 prompt（中文、短句、文字逐字照抄）

模板要点（完整版在 `references/prompt-recipes.md`）：

- 封面（加字，**图生图**，传截图）：`把以下文字加到画面中作为我的视频封面（16：9/9：16）↵[主标题]↵[副标题]↵可以设计字体颜色排版等等，要有高级感设计感`
- 封面（设计，**图生图**，必传截图/素材图）：`结合我的文案设计一个高级的视频封面（16：9）↵[主标题]↵[副标题]↵有设计感，让人眼前一亮`
- 字幕条（首版纯设计=**文生图**；风格参考/批量改字=**图生图**）：`设计一个视频用字幕条（主题是…，一行排版）↵[人名]↵[头衔]↵注意字体颜色排版等等，要有高级感，设计感，尽可能紧凑一点↵[透明底|背景为方便抠图的绿色背景]`
- 透明底直出（片头字幕/封面字幕，**文生图，不传图**）：`设计一个视频开头用字幕↵[主标题]↵[副标题]↵要有高级感设计感（符合主题，[风格]，[色调]），输出真透明底`
- 抠图（**图生图**，传原图）：`将[元素]完整的提取出来（不改变布局排版等等），背景设置为方便抠图的纯绿色背景`（+`整体降噪处理`；带人物画面加`不要人物等任何其他画面元素`）
- 合成（**图生图**，≥2 图）：`将图二人物完美的融入到图一中（位置），人物都保持在一个水平面（脚要在一个水平面，身高尽量匹配），注意脸部细节`
- 改字（**图生图**，传要改的图）：`字改成[新文字]，其他不变`（绿底素材加`背景依旧为纯绿色底，整体降噪处理`）

风格词按需用：`高级感 / 设计感 / 醒目 / 大气 / 紧凑 / 清晰明亮，让人眼前一亮 / 不要太花哨`。
用户给的文字**逐字照抄**，不改写；多行用换行。

**四条实测铁律（当前端点 2026-09-04 验证）：**
1. **比例写进 prompt**：“（16：9）”“（9：16）”“比例21:9”——size 参数被中转站忽略，prompt 是唯一控制方式。
2. **透明底分文生图 / 图生图**（重要）：
   - **文生图（无参考图）**：prompt 写“透明底”二字 → 端点直接返回带 alpha 的真透明 PNG，
     且**排版设计质量比绿底版更好**，首选。
   - **图生图（有参考图，如从图里提取字幕条/纯标题要透明）**：prompt“透明底”**不可用**
     （端点虽返回 alpha 通道，但提取结果严重劣化：元素模糊成污渍或整图空白）→
     改用绿底链路：prompt 写“背景设置为方便抠图的纯绿色背景”+ `--unkey`（本地抠键色出透明 PNG）。
   - 绿幕抠图（交付绿底给剪辑软件）：只写“方便抠图的纯绿色背景”，不加任何选项。
3. **失败/效果不好 → 直接汇报，绝不自行重试**（2026-09-04 用户规则）：
   - **生成失败**（脚本报错：HTTP 错误/超时/`multipart EOF`）→ 把错误原样汇报给用户，
     等用户决定（重试/换图/稍后），**不要自己重发**（每次重试都是真实生成消耗）。
   - **出图了但效果不好**（错字/排版差/元素混入）→ **先把原图发给用户**+指出具体问题，
     用户确认修改方向后再改 prompt 重出，**不要静默重生成**。
   - 唯一例外：n>1 单请求被网关拒（HTTP 4xx，未产生生成、无消耗）→ 脚本立即串行 n=1，
     这是同一任务内部的实现细节，不算重试。

### 3. 选参数（默认即习惯）

| 参数 | 默认 | 何时改 |
|---|---|---|
| `--size` | `auto`（恒 auto） | **当前端点忽略 size 参数**，比例一律写进 prompt（“（16：9）”“（9：16）”“比例21:9”）；无输入图且不改比例时也 auto |
| `--quality` | `auto` | 基本不改；明确要求“特别清晰”才 `high` |
| `--n` | `1` | **首次设计**封面/字幕条/PPT 封面用 `2`（挑一版）；定稿/改字/抠图永远 `1`。中转站对 n>1 行为不稳：文生图脚本先试再串行，图生图脚本直接串行发 2 次 n=1 |
| `--unkey` | 不传 | **图生图要透明底**时传：prompt 写“方便抠图的纯绿色背景”，出绿底图后本地抠键色输出真透明 PNG（文生图不要用它，直接写“透明底”） |
| `--transparent` | 不传（兜底模式） | 仅当**文生图**“透明底”没生效（返回图无 alpha，脚本会警告）才传：追加平台同款 [背景指令] 块 + 本地键色抠除兜底。绿幕抠图、图生图透明都不用 |

参考图：`--images 图1 图2 …`，**顺序必须等于 prompt 里 @图1/@图2 的引用顺序**，最多 16 张。
带参考图时脚本自动走 `images/edits`（multipart）端点；prompt 中的 `@图N`（含零宽标记）
会自动转成接口认识的 `[image N]`，直接照用户原文写 `@图N` 即可。

### 4. 出图（一次只发一个请求）

```sh
python "$SKILL_DIR/scripts/gimg.py" --prompt "<按模板写好的中文提示词>" \
  [--images 图1.png 图2.png] [--size auto] [--n 1] [--transparent] \
  --out-dir <输出目录> --name <资产名>
```

- 账号有并发上限：**串行**执行，上一条完成再发下一条；`--n 2` 由脚本内部串行完成，勿再手动拆并发。
- 复杂任务（多图/长 prompt）脚本默认 timeout 1000s，勿调低。
- **脚本不自动重试**（2026-09-04 用户规则）：任何失败（HTTP 错误/超时/`multipart EOF`）
  立即报错退出 → 把错误汇报给用户，等用户决定是否重试。已实测的接口行为：
  - 单请求 n>1 时而被拒（中转站账号随机分配）：脚本先试一次，被拒（无消耗）立即串行 n=1
  - size 参数被忽略（比例靠 prompt）
  - prompt“透明底”只在**文生图**出透明 PNG（无 alpha 时脚本警告，加 `--transparent` 兜底）；
    **图生图**透明用“绿底 prompt + `--unkey`”
  - `multipart: NextPart: EOF`（上传被拒读）：**同一图片短时间反复上传**会触发中转站
    临时限制（实测窗口约 1 小时、自行恢复，非内容拉黑）；脚本报错后**告诉用户稍后再发**
    （10~30 分钟）；窗口期急用可重编码参考图（改字节）绕开

### 5. 给用户确认（强制，不可跳过）

出图后把 PNG 给用户看，明确问：文字对不对、风格是否满意、哪一版保留。
**未确认前不进入批量/多页。**
**效果不好（错字/排版差/元素混入）→ 先把这张原图发给用户 + 指出具体问题，
等用户确认修改方向再重出；绝不静默重生成。**
迭代时按 `references/iteration-and-errors.md`：
每轮只改一个维度，prompt 只写改动 + 保护句（`其他不变` / `保留板式`），绿底状态每轮重申。

### 6. 批量（仅限字幕条/封面等已确认模板）

- 字幕条批量：`字改成[新名字] [新头衔]，保留板式，背景依旧为纯绿色底，整体降噪处理`，逐个串行。
- 复用风格：`跟这个字幕条相同风格，帮我在@图1上加入一个人名字幕条[文字]`（@图1=已定稿的那张）。

### 7. 交付

- 逐张核对关键文字与用户原文（gpt-image-2 中文偶有错字），绿底图确认纯绿无杂色。
- 透明任务：确认 PNG 带 alpha 通道（脚本会自动检查并警告）。
- 回报每张 PNG 的绝对路径、实际宽高、所用参数；说明哪些是绿幕素材（后期剪辑软件里一键去背）。

## 出问题时的处理

**总原则（2026-09-04 用户规则）：失败 → 汇报错误；效果不好 → 发原图+说问题；
绝不自行重试/静默重生成。以下所有“重试”都指用户点头后再发。**

- 中文错字：先发给用户确认，再按改字模板修（缩短/换关键短词）。
- 风格漂移：上传定稿图作 @图1 + “跟这个相同风格”。
- 文字挡人/抢画面：加 `不要遮挡人物` / `占比画面五分之一到四分之一（不要抢占画面）`。
- 比例不对：检查 prompt 里是否写了“（16：9）”等比例（size 参数无效，别指望它）。
- 透明没生效：分情况——
  - **文生图**（无参考图）：把图发给用户说明无 alpha，用户同意后加 `--transparent` 兜底重出。
  - **图生图**（有参考图）：prompt“透明底”本身不生效 → 改“背景设置为方便抠图的纯绿色背景”+ `--unkey`。
- 上传报 `multipart EOF`：**同一图片短时间反复上传**触发中转站临时限制（实测窗口约 1 小时、
  自行恢复，非内容拉黑、非 size 问题）：脚本直接报错（不自动重试）→ **告诉用户等 10~30 分钟再发**；
  窗口期急用可重编码参考图（裁剪/旋转/换压缩，改字节）绕开。
- 接口失败（HTTP 错误/超时）：脚本直接报错（不自动重试）→ 把错误原文汇报给用户；
  用户要重试时先检查 key（`--show-config`）、网络，或换 base_url
  （历史备用：`https://api.ai-pixel.online` / `https://speed.ai-pixel.online`，超时保持 1000s）。
